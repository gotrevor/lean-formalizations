# Handoff: choquet route paused-for-import → pivoted to the FastGrowing A3 crux

**Date**: 2026-06-19 16:58 UTC · **Branch**: `kakeya-davies` · **HEAD**: `a4d91ad`

## 🎯 What we're doing
Repo = planar-Kakeya `davies_kakeya_2d` expedition (headline DONE + axiom-clean, untouched). The
"chosen work" coming in was the **`jvn` 2nd route → its sole hole `choquet_core_range`** (Choquet
capacitability, `wip/GeometricMeasureTheory/Capacitability.lean:110`). This lap I discovered that
thread is **owner-paused** and pivoted to the other open math crux: the **fast-growing-hierarchy
index-monotonicity step** `fastGrowing_fundSeq_step` in `wip/Logic/FastGrowing/Basic.lean`.

## 🧠 Context to carry forward (read this before re-attacking choquet)
- **The choquet thread is PAUSED by Trevor, not open.** I harvested `ON-LINE-FINDINGS-2026-06-19-
  import-not-vendor.md` (now in `archive/findings/`): Trevor decided to consume `RemyDegenne/
  brownian-motion`'s proven Choquet stack as a **direct `.lake` dependency** (import, not vendor,
  not from-scratch). That needs infra a no-egress lap **cannot** build (mathlib bump v4.29→v4.31,
  shared store, network clone of BM). So do **NOT** sink local laps into from-scratch
  `choquet_core_range` — it's the documented fallback only. After import the only work left is a
  small bridge `mathlib.AnalyticSet s → BM.IsMeasurableAnalytic s`. PENDING_WORK.md top now records
  all this. The Aristotle `choquet` job (UUID `1b34087b-5d6e-419e-87d4-1bfa803616fa`) is still
  RUNNING (~2h+) on the from-scratch version; low EV (measure theory + superseded). If it ever
  returns something usable, verify + `#print axioms` before trusting; otherwise it's harmless.
- **Why FastGrowing is the right pivot, not a retreat:** with the choquet crux owner-paused and
  infra-blocked, the charter says take another open `sorry` in-repo. `fastGrowing_fundSeq_step` is
  the only other genuine math crux, unblocked, self-contained.
- **Key finding I proved by hand (kills a tempting wrong path):** general fixed-argument index
  monotonicity `a < b ⟹ ∀ m≥1, fₐ(m) ≤ f_b(m)` is **FALSE**: `5 < ω` yet `f₅(2) ≫ f_ω(2)=f₂(2)=8`.
  So `fastGrowing_fundSeq_step` is **not** reducible to plain index-mono. The value comes from the
  *coupling* of index level `n` with argument `n+1`. The correct tool is the **Bachmann property**
  of the fundamental-sequence system (then all FGH levels are monotone — Fernández-Duque–Weiermann
  arXiv:2203.07758; Buchholz). Confirmed via literature search.
- **The precise next prerequisite = the Bachmann inequality** `fundSeq_bachmann`: for limit `o` with
  fund seq `f`, if `f(n+1)` is itself a limit with fund seq `g`, then `f n ≤ g(n+1)`. I verified it
  by hand on `o = ω²` (`f i = ω·(i+1)`, `g i = ω·(n+1)+(i+1)`, so `ω·(n+1) ≤ ω·(n+1)+(n+2)` ✓ via
  `le_self_add`). The `+finite` limits (e.g. ω, ω·2) hit the **successor** branch of the step, which
  is already proven (`fastGrowing_fundSeq_step_of_succ`). With `fundSeq_bachmann` + a conditional
  index-mono by well-founded recursion on the larger index, the crux closes — the descent's only gap
  is "`a ≤ g m`", which Bachmann supplies.

## ✅ State (verified this lap)
- `lake build` 🟢 **green, 8300 jobs** (pre-commit green-gate passed on commit `7e4830c`). `src/`
  untouched, still sorry-free + axiom-clean.
- `wip/Logic/FastGrowing/Basic.lean` kernel-checks (`lake env lean`) with **exactly 1 sorry** =
  `fastGrowing_fundSeq_step` (the crux). Two NEW lemmas, both `#print axioms`-clean
  (`[propext, Classical.choice, Quot.sound]`):
  - `fundSeq_strictMono` — fund seq of a limit notation is `StrictMono`.
  - `fastGrowing_fundSeq_step_of_succ` — the crux when `f(n+1) = succ(f n)`, via
    `fastGrowing_le_succ_index`.
- Commits this lap: `7e4830c` (FastGrowing lemmas + roadmap + harvest), `a4d91ad` (Bachmann Aristotle
  problem file).

## 🎬 Next actions
1. **Prove `fundSeq_bachmann`** (`wip/aristotle/bachmann/Problem.lean` has the exact statement +
   ω² worked example + attack notes). It's pure ONote/`repr` ordinal arithmetic: case-analyse the
   recursive `fundamentalSequence (oadd e m b)` def (`Mathlib/SetTheory/Ordinal/Notation` lines
   924–935), most cases reducing to `le_self_add`/`oadd`-tail monotonicity. Carry `o.NF`.
2. **Submit that file to Aristotle** the moment the `choquet` slot frees (`aristotle list`; if it's
   gone/idle, `aristotle submit wip/aristotle/bachmann/Problem.lean`). It's far better suited to
   Aristotle than choquet (ordinal case-bash vs measure theory). Per the one-job rule I did NOT run
   a 2nd concurrent job this lap.
3. Then assemble: `fundSeq_bachmann` → conditional index-monotonicity (WF recursion on the larger
   index) → discharge `fastGrowing_fundSeq_step` → `fastGrowing_le_succ`/`fastGrowing_monotone`
   become axiom-clean.

## ⚠️ Gotchas
- `wip/` is deliberately OUT of the `lake build` target (keeps the headline endpoint sorry-free).
  Kernel-check wip files with `lake env lean <path>`, not `lake build`.
- Don't kill the running `choquet` Aristotle job to free the slot unless it's clearly stuck — it has
  ~2h invested and fallback value; just wait for it to finish, then submit Bachmann.
- ONote `<`/`≤` are the `repr`-order Preorder; `fundamentalSequence_has_prop` (no NF needed) is the
  entry point for fund-seq facts; the `.NF` clauses are conditional (`o.NF → (f i).NF`).

## 📁 Key files
- `wip/Logic/FastGrowing/Basic.lean` — growth theory; sole sorry = `fastGrowing_fundSeq_step` (crux),
  with the full Bachmann roadmap in its docstring.
- `wip/aristotle/bachmann/Problem.lean` — ready-to-submit `fundSeq_bachmann` statement.
- `PENDING_WORK.md` (top) — import-decision banner + FastGrowing section + retained choquet fallback.
- `wip/GeometricMeasureTheory/Capacitability.lean` — choquet reduction PROVEN; hole = `choquet_core_range`
  (paused for import). `CantorScheme.lean` in mathlib is the from-scratch fallback's missing infra.
- `archive/findings/ON-LINE-FINDINGS-2026-06-19-import-not-vendor.md` — the harvested import decision.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other KB projects — the FastGrowing A3 crux (`fastGrowing_fundSeq_step`
via the Bachmann inequality) IS the chosen thread; choquet is owner-paused for import. Absorb the
context, then start at Next action #1: open `wip/aristotle/bachmann/Problem.lean` and prove
`fundSeq_bachmann` (and submit it to Aristotle once the choquet slot frees).**
