# Handoff: von Neumann selection built — `jvn` route reduced to capacitability ALONE

**Date**: 2026-06-19 14:18 UTC · **Branch**: `kakeya-davies` · **HEAD**: `da439ae`

## 🎯 What we're doing
Expedition = planar Kakeya `davies_kakeya_2d : KakeyaSetConjectureDim 2` (Davies 1971), lane
`src/LeanFormalizations/GeometricMeasureTheory/Kakeya2D/`. **The headline is DONE, axiom-clean, untouched
this lap** (elementary open-cover route). This lap attacked the one genuinely-open mathematical target:
discharging the abstract `jvn` hypothesis of `Selection.kakeya_aeMeasurable_selection_of_jvn` — the
**independent 2nd route** to the headline (faithfulness value; moot for the headline but a real,
publishable mathlib contribution: the first measurable-selection theorem in mathlib).

## 🧠 Context to carry forward
- **`jvn` = the von Neumann / Jankov–von Neumann measurable-selection theorem.** mathlib genuinely lacks
  it (and KRN, capacitability, the Souslin operation, any capacity theory) — confirmed by grep. It HAS
  `AnalyticSet` + Lusin separation + the `PiNat` Baire-space machinery. So this is a from-scratch build.
- **I worked entirely in `wip/`** (kernel-checked via `lake env lean <path>`, NOT in the `lake build`
  target) so the verified sorry-free `src/` headline stays pristine. When capacitability lands, port the
  whole thing into `src/` and feed `jvn_of_measurableSelection` to `kakeya_aeMeasurable_selection_of_jvn`.
- **Decomposition I built and PROVED (all kernel-checked, no sorry except where noted):**
  `jvn ⟸ measurableSelection_aemeasurable ⟸ {capacitability bridge} + {analytic→Baire reduction} +
  {brick A capacitability} + {brick B leftmost-branch}`. Brick B splits into construction (`phi_branchN`)
  + measurability (`measurable_branchN`) — **both PROVEN axiom-clean** (`#print axioms` =
  `[propext, Classical.choice, Quot.sound]`). I verified by `#print axioms` that
  `jvn_of_measurableSelection` depends on exactly ONE `sorryAx` = capacitability — no hidden holes leaked.
- **The leftmost-branch construction** (the part I feared would be hard): greedy lexicographically-least
  prefix `Wlist φ x n` (append `sInf` of extendable children), `branchN` = its entries; `phi_branchN`
  shows `φ(branch) = x` via the extendability invariant + a closed-fibre convergence lemma
  (`mem_of_forall_prefix_mem`). Measurability: strong induction, fibre `{Wlist φ x n = w}` cut by the
  first `n` branch coordinates (`Wlist_eq_iff`, proved with `getElem?`/`List.ext_getElem?` to dodge
  dependent-proof `rw` failures), `branchN` on a fibre = `sInf{k | x∈φ''cylL(w++[k])}` measurable via
  `measurable_sInf_indices` (sInf of an indicator family is measurable) + `measurableSet_image_cylL`
  (`cylL w` open ⟹ image analytic ⟹ a `generateFrom{AnalyticSet}` generator).
- **Capacitability (brick A) is the sole remaining hole.** It's a coherent multi-lap theorem with NO
  mathlib scaffolding — do NOT expect to one-shot it. I scaffolded the Souslin scheme (`Capacitability.lean`)
  and wrote the full proof architecture in its header + `PENDING_WORK.md`. Aristotle is a poor bet
  (corpus: weak on measure theory) and was busy with another project's jobs.

## ✅ State (verified this lap)
- `lake build` 🟢 green, 8300 jobs (pre-commit gate passed on every commit). `src/` UNCHANGED, still
  sorry-free + axiom-clean; `davies_kakeya_2d` untouched.
- `wip/GeometricMeasureTheory/VonNeumannSelection.lean` kernel-checks with **exactly 1 sorry**
  (`analyticSet_nullMeasurableSet`, capacitability). All else proven; brick B axiom-clean (verified).
- `wip/GeometricMeasureTheory/Capacitability.lean` kernel-checks with 1 sorry (the Choquet core); the
  scheme lemmas `cylImg_zero`/`cylImg_decomp`/`analyticSet_cylImg` are proven.
- Commits this lap: `4975e41` (decompose+glue), `35fa56f` (bridge+online-req), `5177129` (2 bricks),
  `a6d80e6` (prefix lemma), `062d01c` (brick B construction), `b5ee727` (**brick B fully proven**),
  `768536e` (capacitability scaffolding), `da439ae` (STATUS).

## 🎬 Next actions
1. **Crack capacitability** `Capacitability.analyticSet_nullMeasurableSet` (= `VonNeumannSelection`'s
   sole hole). It's THE crux now — bang on it, don't retreat. Follow the architecture in
   `Capacitability.lean`'s header + `PENDING_WORK.md` top: (i) `μ.toOuterMeasure` continuity-from-below
   gives a finite child-sub-union capturing all but `ε·2⁻ⁿ` at each scheme node (reachable, mathlib
   has the measure-continuity lemmas); (ii) the Choquet compact-extraction from the pruned finitely-
   branching subtree (the hard core — compactness in `ℕ→ℕ` + `f` continuous ⟹ `K ⊆ s`); (iii) inner+
   outer regularity ⟹ `NullMeasurable`. Reduce to finite `μ` first; only `volume` on `ℝ` is needed.
   Commit incrementally; each proven sub-lemma is real progress.
2. When (1) lands: replace `VonNeumannSelection.analyticSet_nullMeasurableSet`'s sorry by importing/
   porting `Capacitability.analyticSet_nullMeasurableSet`; then the whole `wip/` von Neumann file is
   axiom-clean. Port into `src/` and wire `jvn_of_measurableSelection` into
   `Kakeya2D/Selection.kakeya_aeMeasurable_selection_of_jvn` for the unconditional 2nd route.
3. Check `ON-LINE-REQUEST.md` answers first (a fulfiller may have found an existing Lean formalization of
   capacitability/KRN to port — that would collapse step 1 to a port).

## ⚠️ Gotchas
- `cylL`/`cylImg` use TWO cylinder encodings: `cylL` (List-prefix, in VonNeumannSelection) and
  `PiNat.cylinder` (base+length, in Capacitability). Both are fine; just don't confuse them when porting.
- `PiNat.cylinder`/`isOpen_cylinder` are dependent (`E : ℕ → Type`); pass `(E := fun _ => ℕ)` explicitly.
- The `src/`-scoped sorry-gate ignores `wip/` — that's why this lives there. A sorry in `src/` would block
  allow-stop and dirty the headline's clean endpoint, so keep capacitability in `wip/` until it's proven.
- Dependent `getElem`/`get` proof terms break `rw` (motive not type correct) — use `getElem?` +
  `List.ext_getElem?` (see the staged memory note).

## 📁 Key files
- `wip/GeometricMeasureTheory/VonNeumannSelection.lean` — the whole `jvn` route; 1 sorry (capacitability).
- `wip/GeometricMeasureTheory/Capacitability.lean` — brick A scaffolding + the Choquet-core sorry + plan.
- `PENDING_WORK.md` (top) — full decomposition + capacitability build plan.
- `ON-LINE-REQUEST.md` — request for an existing Lean capacitability/KRN/JvN formalization to port.
- `src/LeanFormalizations/GeometricMeasureTheory/Kakeya2D/Selection.lean` — `kakeya_aeMeasurable_selection_of_jvn`
  (the consumer of `jvn`); headline route is `exists_measurable_selection_of_isOpen` (untouched).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other KB projects — the planar-Kakeya `jvn`/von Neumann thread IS the
chosen work. The headline is DONE and axiom-clean; do NOT re-prove it or re-attack the open route. Pick
up at Next action #1: open `wip/GeometricMeasureTheory/Capacitability.lean`, read its header + the
`PENDING_WORK.md` capacitability plan, and start cracking the Choquet measure-extraction core (after a
quick check of `ON-LINE-REQUEST.md` for a portable formalization). Bang on capacitability — it's the
sole remaining hole between us and an axiom-clean independent 2nd proof of planar Kakeya.**
