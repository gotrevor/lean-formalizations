# Handoff: Davies planar Kakeya — K5 reframed to the measure-free Hausdorff content bound

**Date**: 2026-06-19 06:12 UTC (updated 06:2x) · **Branch**: `kakeya-davies` · **HEAD**: `f653917`

## 🎯 What we're doing
Prove `davies_kakeya_2d : KakeyaSetConjectureDim 2` (every planar Kakeya set has `dimH = 2`).
Upper bound done; all content is the lower bound, reduced (K1) to `μH[d] S ≠ 0` for all `d<2`.
Unbounded expedition along the K1–K5 ladder. **Stay in `Kakeya2D/` only.**

## 🧠 The big move this lap (review lap): reframed the crux, built the whole pipeline
The previous baton pinned the crux to `FrostmanMeasureExists` — *constructing a measure* — which
forces a weak-* limit of normalised tube masses, a piece of measure theory mathlib doesn't support
cleanly. **I retired that path and switched to the dual, measure-free cover route.** Engine's lone
`sorry` is now the honest, mathlib-native **Hausdorff content bound**:

> `kakeya_hausdorffContentBound`: planar Kakeya `S`, `0<d<2` ⟹ `∃ r>0, c≠0, ∀ fine cover S⊆⋃tₙ,
> `c ≤ ∑ₙ ediam(tₙ)^d`.

Everything between this and `μH[d]S≠0` is now PROVEN + axiom-clean. New `Cover.lean`:
- `hausdorffMeasure_ne_zero_of_content_bound` / `_diam_content` / `_contentBound` (+ Prop
  `HausdorffContentBound`): content bound ⟹ `μH[d]S≠0`, straight from `Measure.hausdorffMeasure_apply`.
- `thickening_subset_iUnion_thickening` + `volume_thickening_le_tsum`: cover of `S` ⟹ cover of `Sδ`,
  so `vol(Sδ) ≤ ∑ vol((Uₙ)δ')`. **Strict slack `δ<δ'` dissolves the closed-thickening `iInf`
  boundary issue** the prior baton flagged as subtle (`iInf_lt_iff` needs `<`, not `≤`).
- `volume_thickening_le_of_ediam_le`: per-piece area `vol((U)δ') ≤ ofReal((ρ+δ')²)·vol(closedBall 0 1)`;
  `volume_closedBall_one_pos`/`_ne_top` (the constant `≠0`,`≠⊤`).
- `exists_index_ge_of_tsum_lt`: weighted pigeonhole `c≤∑aₙ`, `∑wₙ<c` ⟹ `∃n, wₙ≤aₙ`.

And I refactored the K4 `L²` machine: **`CordobaL2.volume_thickening_tubes_ge`** now takes an explicit
base-point family + ANY measurable container `E` (drops `IsKakeya`); `volume_thickening_mul_ge` is
its `E:=Sδ` corollary. This is the reusable entry point for the localized Córdoba count.

**Also landed this lap (the prior next-lap entry, executed early):**
`TubeFractional.volume_tube_ge_frac` — `ofReal(2δ‖v‖) ≤ vol(tube a v δ)` for `v≠0`, the fractional
(length-`‖v‖`) tube area bound, via the `e=‖v‖⁻¹•v` frame box (reuses `volume_frame_box`/`frame_decomp`
verbatim). This is the localized-Córdoba **numerator** for sub-unit covered segments.

## ✅ State (all verified via real `lake build` this lap)
- Full library **green, 8295 jobs**. 7 commits (`cb786e7`..`40b6c14`).
- All new lemmas `#print axioms` = `[propext, Classical.choice, Quot.sound]` (clean).
- `davies_kakeya_2d` axioms = `[propext, sorryAx, Classical.choice, Quot.sound]` (single `sorryAx`,
  now = the content bound, NOT a measure construction).
- Lone Kakeya `sorry`: `Engine.kakeya_hausdorffContentBound`. (Separate dormant `sorry` in
  `Logic/FastGrowing/Basic.lean` — out of lane, do not touch.)
- 0 custom axioms anywhere; no axiom-smuggling (the crux is a disclosed `sorry`).

## 🎬 Next actions (the multi-scale Córdoba estimate, multi-lap — `PENDING_WORK.md` §A)
The obstruction is **mixed scales**: single-scale bricks bound only the *count* of pieces, never
`∑ediam^d` (pieces can be arbitrarily small). Plan = dyadic double-pigeonhole + localized Córdoba.
**All the geometric/L² inputs are now built** (`volume_thickening_tubes_ge`, `volume_tube_ge_frac`,
`volume_thickening_le_of_ediam_le`, `exists_index_ge_of_tsum_lt`). Remaining = the **bookkeeping +
pigeonhole assembly** (`PENDING_WORK.md` §A, sub-bricks (a)-(c)):
1. **Next-lap entry (a): per-direction covering ⟹ a covered sub-segment.** For a net direction `θ`
   with `ℓ_θ ⊆ ⋃Uₙ`, formalize `∑ⱼ Lⱼ(θ) ≥ 1` and pigeonhole (`exists_index_ge_of_tsum_lt`) to a
   dominant scale `j(θ)` + covered sub-segment of length `≥ wⱼ`. Needs the clean 1D brick
   `|ℓ_θ ∩ Uₙ| ≤ ediam Uₙ` (`Real.volume`/length of a segment ∩ set ≤ its diameter).
2. (b) direction pigeonhole over the net ⟹ a single dominant `j*`; (c) assemble: scale-`j*` pieces
   = container `E` for `volume_thickening_tubes_ge`, numerator via `volume_tube_ge_frac`, `vol(E)`
   bound via `volume_thickening_le_of_ediam_le` ⟹ `M≳2^{2j*}/poly` ⟹ `∑ediam^d ≳ 2^{j*(2-d)}/poly`.
3. Online ref requested (`ON-LINE-REQUEST.md`): cleanest write-up / existing formalization of
   Córdoba/Davies-for-Hausdorff — harvest `ON-LINE-FINDINGS-*` first next lap if present.

## ⚠️ Gotchas (flushed)
- `Measure.hausdorffMeasure_apply` / `Measure.addHaar_closedBall'` need the `Measure.` prefix under
  `open MeasureTheory`. `ediam` in `hausdorffMeasure_apply` = `Metric.ediam` (open Metric).
- Closed-thickening covers: never use equal radii on an infinite cover (`iInf` unattained); always a
  strict slack `δ<δ'` so `iInf_lt_iff` gives a single witness.
- Aristotle: slot occupied by another project's RUNNING job all lap (`083292d5`); remaining Kakeya
  pieces are measure-theoretic (Aristotle-weak, per reference corpus) — correctly not fed.

## 📁 Key files
- `…/Kakeya2D/Cover.lean` — **NEW**, the K5 measure-free route (reduction + covering geometry +
  pigeonhole + constants).
- `…/Kakeya2D/Engine.lean` — `kakeya_hausdorffContentBound` (the lone `sorry`) + the wiring.
- `…/Kakeya2D/CordobaL2.lean` — `volume_thickening_tubes_ge` (explicit-family L² entry point) + K4.
- `STATUS.md` (refreshed), `PENDING_WORK.md` §A (rewritten attack plan), `ON-LINE-REQUEST.md`.

---
**→ Next session: absorb this, then start sub-brick (a) — the per-direction covering ⟹ covered
sub-segment, needing the 1D `|ℓ_θ ∩ Uₙ| ≤ ediam Uₙ` brick — and assemble the pigeonhole. ALL the
geometric/L² inputs are already built and axiom-clean. Don't relitigate the measure-construction
route — it's retired for cause.**
