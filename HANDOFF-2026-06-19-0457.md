# Handoff: Davies planar Kakeya — K2 ladder complete, K3 started

**Date**: 2026-06-19 04:57 UTC · **Branch**: `kakeya-davies` · **HEAD**: `75b5003`

## 🎯 What we're doing
Prove `davies_kakeya_2d : KakeyaSetConjectureDim 2` (every planar Kakeya set has Hausdorff
dim 2). Upper bound `dimH_le_two` was already done; the whole job is the lower bound
`two_le_dimH`, in `src/LeanFormalizations/GeometricMeasureTheory/Kakeya2D/`. Unbounded
expedition (see `DIRECTION.md`) — no stop until the crux is closed. **Stay in this lane only.**

## 🧠 Context to carry forward
- The proof follows the **K1–K5 ladder** in `Kakeya2D/PLAN.md` (Córdoba `L²`/bush). This lap
  finished K1 + the **entire K2** (the geometric heart) + a K4 prereq + K3 brick 1.
- **The one remaining `sorry`** is `Engine.lean : hausdorffMeasure_pos_of_isKakeya`
  (`∀ d<2, μH[d] S ≠ 0`). K1 reduced `two_le_dimH` to it (axiom-clean wrapper). Everything else
  in the Kakeya thread is proven + axiom-clean.
- **Faithfulness call I made**: the two-tube overlap separation MUST be `s = |v₀w₁−v₁w₀| = |sin∠|`,
  NOT the chord `‖v-w‖`. `‖v-w‖` is *false* near-antipodal (v, w≈−v are nearly-parallel *lines*
  with overlap ≈ δ, but `‖v-w‖≈2`). `|det|` is invariant under `v↦−v` and is exactly the area
  Jacobian — so it's both faithful and easier. I added `hδ1 : δ ≤ 1` to `volume_inter_tube_le`
  (faithful — Córdoba uses δ→0; needed for the single-tube fallback regime).
- **Mental model of K4** (next big rung): `vol(Sδ) ≥ (∑∫1_{tubeᵢ})² / ∑ᵢⱼ overlapᵢⱼ` by
  Cauchy–Schwarz. Numerator `≳1` (each tube area `≍δ` — both bounds now proven; `δ⁻¹` tubes).
  Denominator `≲ ∑ₖ δ·δ²/(kδ+δ) ≈ δ·log(1/δ)` by `volume_inter_tube_le`. ⟹ `vol(Sδ) ≳ 1/log(1/δ)`.
  **The overlap bound K4 runs on is done** — that was the dependency in real doubt.
- K5 is the deepest/subtlest: a `1/log` *Minkowski content* lower bound at every scale does NOT
  directly give Hausdorff dim (box dim ≥ Hausdorff dim always); you need a Frostman/mass-distribution
  construction. Don't conflate them. Multi-lap.
- Aristotle was busy with a *fastgrowing* job (other thread, prior lap) and is weak on measure
  theory anyway — I worked locally. Don't feed it the geometry leaves.

## ✅ State (all verified this lap via real `lake build`)
- Full library **green**, 8290 jobs. 12 commits this lap (see `git log`).
- `Tube.lean` — **100% sorry-free, axiom-clean**. Proven: tube infra, thinness
  (`tube_transverse`/`tube_longitudinal`), `volume_tube_le` (≤6δ), **`volume_inter_tube_le`**
  (overlap ≤12δ²/(s+δ)), `volume_tube_ge` (≥2δ), plus the frame/`volume_two_slab`/`perp` machinery.
- `Discretize.lean` (new) — K3 brick 1: `exists_tube_subset_thickening` (Sδ contains a δ-tube of
  every direction). Axiom-clean.
- `Engine.lean` — one disclosed `sorry` (`hausdorffMeasure_pos_of_isKakeya`).
- `#print axioms davies_kakeya_2d` = `[propext, sorryAx, Classical.choice, Quot.sound]` (sorryAx
  only via that one crux, as expected). All sub-lemmas individually axiom-clean.

## 🎬 Next actions
1. **K3 brick 2 — the δ-net of directions.** Build a finite δ-separated family of unit directions
   on the circle (compactness of `{v | ‖v‖=1}`) and, via `exists_tube_subset_thickening`, the
   matching tube family inside `Sδ`. Index by `k` so pairwise separation `s ≈ kδ`. Attack options:
   (i) explicit `Finset` of angles `kδ` with `EuclideanSpace.single`-style unit vectors; (ii)
   abstract `∃ finite family, pairwise-separated, all ⊆ Sδ`. Land it sorry-free.
2. **Then K4** (Córdoba `L²`): assemble `vol(Sδ) ≳ 1/log(1/δ)` from the tube family + the two
   area bounds + `volume_inter_tube_le`. This is `lintegral`/Cauchy–Schwarz work.
3. Keep `Engine.lean`'s crux disclosed; only collapse it once K3→K5 actually feed it.

## ⚠️ Gotchas
- EuclideanSpace measure lemmas need `import Mathlib.Analysis.Normed.Lp.MeasurableSpace` (+
  `…Measure.Haar.InnerProductSpace` for `repr`/Haar). Without it: "failed to synthesize
  MeasurableSpace". (Flushed to KB staging.)
- Real scalar inner: `⟪a,b⟫_ℝ = b*a` by **rfl** (not `a*b`); `RCLike.inner_apply` won't fire.
  Use `have hr : ∀ a b:ℝ, ⟪a,b⟫=b*a := fun _ _=>rfl` in simp sets. (Flushed.)
- Linear-map determinant on the plane: `Matrix.toLpLin 2 2` (not deprecated `toEuclideanLin`),
  `LinearMap.det_toLpLin`, `Measure.addHaar_preimage_linearMap`. (Flushed.)
- `push_neg` is deprecated here → `push Not at h`. `Real.volume_Icc` (namespaced).
- `abbrev Plane := EuclideanSpace ℝ (Fin 2)` (can't name it `ℝ²` — superscript not an identifier).
- Repo uses **master `HANDOFF.md`** (kept current) + dated session docs like this one. The master
  is up to date; this dated doc is the per-session record.

## 📁 Key files
- `…/Kakeya2D/Engine.lean` — K1 wrapper + the lone crux `hausdorffMeasure_pos_of_isKakeya`.
- `…/Kakeya2D/Tube.lean` — all of K2 (done). The reusable frame/area/overlap machinery.
- `…/Kakeya2D/Discretize.lean` — K3 (started). `thickening`/`exists_tube_subset_thickening`.
- `…/Kakeya2D/PLAN.md` — the K1–K5 ladder. `PENDING_WORK.md` — three attack paths per open item.
- `HANDOFF.md` — master state (current).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb
the context above, then pick up at Next actions #1 (K3 δ-net of directions) and start working.**
