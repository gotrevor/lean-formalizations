# Handoff: HJSW `3N/2` lower bound — **COMPLETE, axiom-clean** (crux discharged)

**Date**: 2026-06-19 07:28 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `59b1fd8`

## 🎯 Headline — the target is DONE
`hjsw_lower : 3*(p−1) ≤ maxNoThreeInLine (2*p)` (Hall–Jackson–Sudbery–Wild 1975 lower bound) is now
**fully proven with no `sorry` and no custom axiom**:
`#print axioms hjsw_lower = [propext, Classical.choice, Quot.sound]`. Full `lake build` GREEN
(8285 jobs, observed; pre-commit green-gate passed on all commits).

This lap discharged the lone remaining `sorry`, `shearSel_cross_diag` — the genuine HJSW
combinatorial kernel (slope-`±1` case) that had been the open crux for months.

## ⭐ What was proved this lap
The crux: two kept lifts `P,Q` of one column (a slope-`±1` diagonal/antidiagonal pair) and a kept
lift `R` of a *different* column are never collinear. Discharged via two new partner lemmas
(`Hyperbola.lean`), both axiom-clean:

- **`shear_diag_partner`** (slope `+1`): `R` on the line `Y−X = sₐ−a` ⇒ (factoring the curve, NOT
  Vieta) `(c−a)(2c+2sₐ+1) ≡ 0 (mod p)`. `c≠a` ⇒ partner relation `2c+2sₐ+1 ≡ 0` and (symmetrically)
  `2s_c+2a+1 ≡ 0`. These pin `c ≡ pl−sₐ`, `s_c ≡ pl−a`, and the closed-form drop tie-break then
  forces `R` to be column `c`'s **dropped** corner — contradiction with `R` kept.
- **`shear_anti_partner`** (slope `−1`): antidiagonal analogue, partner relations `2sₐ ≡ 2c+1`,
  `2s_c ≡ 2a+1`.

Plus helper bricks: `shearKept_ne_drop`, `shearKept_corner_drop`.

## 🧠 Key techniques (for the corpus / future laps)
- **Polynomial factoring beats Vieta**: `f(R.1)≡0 ∧ f(a)≡0 ⇒ (R.1−a)·g ≡ 0` via
  `linear_combination h1 − hacur`, gets the partner column with no symmetric-function machinery.
- **omega cannot handle `%` / `∣` by a VARIABLE modulus `p`.** Two consequences solved this lap:
  - `x = y` from `x%p = y%p` (x,y<p): do NOT `omega`; use
    `rwa [Nat.mod_eq_of_lt hx, Nat.mod_eq_of_lt hy] at h`.
  - divisibility `p ∣ X` with `X < 4p` odd: `obtain ⟨m,hm⟩ := hd; have : p*4 ≤ p*m := Nat.mul_le_mul …;
    interval_cases m <;> omega` to get `X = p ∨ X = 3p` as a *linear* disjunction omega can use.
- **Convert ℤ line-equations to clean ℕ before the corner omega** (`R.2 + a = R.1 + sₐ` etc.) and
  `clear` the ℤ version — omega silently drops mixed ℤ/ℕ + disjunction hypotheses otherwise.
- Corner pinning = one `refine Prod.ext ?_ ?_ <;> split_ifs <;> omega` once everything is ℕ
  (`(p−1)/2` is div-by-literal-2, which omega *does* handle).

## ✅ Integration done this lap (`Statement.lean`, audit surface)
- `hjsw_lower_bound` — the `3(p−1) ≤ max(2p)` headline (= `3N/2 − 3` at `N=2p`).
- `maxNoThreeInLine_ge_three_quarters` — for **all `N ≥ 4`**: `3·⌊N/4⌋ ≤ maxNoThreeInLine N`
  (largest prime `p ≤ N/2` via Bertrand; `shearSel p ⊆ [0,2p)² ⊆ [0,N)²`). Lifts the Erdős `Θ(N)`
  lower constant from `1/2` (`maxNoThreeInLine_gt_half`) to `3/4`. Both axiom-clean.
- Stale "still a sorry" docstrings refreshed (Hyperbola header, Statement frontier note).

## 📊 Repo state
- **Zero `sorry`** anywhere in `src/` (verified by regex). All headlines axiom-clean.
- The NTL formalization is now: upper `2N` (done), Erdős `Θ(N)` (done), **HJSW `3N/2`/general `3N/4`
  (done this lap)**. The only thing left in the problem is the **Main Conjecture (open math)** — not
  a formalizable proof target.
- `Anchors.lean` still native_decide-certifies `shearSel` at p=7,11,13 (off the headline axiom path).

## 🔭 Aristotle
Jobs `1c2a55b7` (`aris-hjsw-shear`) and `77c99f0e` were submitted last lap on the now-CLOSED crux
(superset `shearSel_intdet`). They are **redundant** — do NOT act on them as confirmation (same Lean
statement ⇒ zero faithfulness signal, per charter). No new job submitted: nothing is genuinely open
in this repo, and the charter says idle Aristotle is correct when nothing is open.

## 🎬 Next actions (this repo is essentially complete)
The sole stated target is achieved. If a future lap wants more in THIS repo:
1. **Faithfulness**: an independent NL→Lean of the HJSW *prose* statement (headline only) would be a
   legitimate one-shot Aristotle cross-check that `maxNoThreeInLine`/`NoThreeCollinear` encode HJSW
   faithfully. (Optional; the kernel + `Statement.lean`/`Anchors.lean` already cover correctness.)
2. **Sharper general-`N`**: pushing `3/4 → 3/2` for general `N` needs a prime in `(N/2−o(N), N/2]`,
   i.e. prime-gap results stronger than Bertrand (Nagura/Baker–Harman–Pintz) — not in mathlib; a
   genuine infrastructure project, not a quick lap.
3. **Polish** (cosmetic only): deprecated `push_neg`→`push Not`; two dead
   `first | linear_combination hZ | -hZ` branches; unused signature vars `hPQ/hPR/hQR` in
   `shearSel_cross_diag` (interface hyps — prefix `_` if desired). None affect correctness/axioms.

Otherwise: the productive frontier here is exhausted in the honest sense (no open obligation). A
fresh lap with a new target would need Trevor to point at a different problem.

## 📁 Key files
- `…/NoThreeInLine/Hyperbola.lean` — `shearSel`, all bricks, the two partner lemmas,
  `shearSel_cross_diag`, `hjsw_lower`. No `sorry`.
- `…/NoThreeInLine/Statement.lean` — audit surface; `hjsw_lower_bound`,
  `maxNoThreeInLine_ge_three_quarters`.
- `SELECTION-RULE-FOUND.md` — the closed-form rule + how it was found.
- `ON-LINE-REQUEST.md` — marked FULLY DONE (host may remove).
