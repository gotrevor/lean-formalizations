# Handoff: HJSW general-`N` constant — Chebyshev θ-lower infra built, Nagura wall mapped

**Date**: 2026-06-19 08:45 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `acaf293`

## 🎯 What we're doing
The umbrella repo's six threads (Curtis, power-tower, constructibles, e/π-transcendence, Goodstein,
no-three-in-line) are all proven & axiom-clean. This lap chipped the **one** remaining piece of 🟡 debt:
the no-three-in-line **general-`N` lower constant**. It was only `3/4` (Bertrand-limited), while HJSW's
real theorem is `3N/2 − o(N)` for all large `N`. The lever is purely prime-gap strength: Bertrand→3/4,
Nagura→5/4, PNT→3/2. Goal this lap: open and advance the `5/4` (Nagura) rung.

## 🧠 Context to carry forward (read this — it saves you the dead ends)
- **All headlines stay axiom-clean.** The new frontier lives in `PrimeGap.lean`, isolated; no headline
  depends on it. The ONE open `sorry` in `src/` is `nagura_prime` — that's the frontier, not a regression.
- **The payoff is already wired.** `maxNoThreeInLine_ge_five_fourths` (`3⌊5N/12⌋ ≤ max N`, `N≥60`)
  follows from `nagura_prime` + `maxNoThreeInLine_ge_of_two_mul_prime_le` (the prime-gap interface). So
  the ONLY thing standing between us and a real `5/4` general-`N` theorem is `nagura_prime`.
- **I built the whole elementary Chebyshev lower-bound stack** (mathlib has only θ/ψ *upper* bounds):
  `four_pow_lt_mul_lcm` → `log_lcm_Icc_eq_psi` (von Mangoldt ↔ lcm bridge) → `psi_lower`/`theta_lower`.
  All axiom-clean. These are genuinely PR-worthy mathlib gaps.
- **CRITICAL dead-end I already ruled out — do NOT repeat it.** The crude elementary Chebyshev bounds
  are *provably insufficient* for Nagura's `6/5`. With `theta_lower` (`θ(x) ≳ (log4/2)x ≈ 0.69x`) the
  central-binomial split gives `C(2n,n) ≤ (2n)^√(2n)·4^(31n/15)`, and `31/15 ≈ 2.07 > 1` ⇒ NO
  contradiction with `4ⁿ ≤ n·C(2n,n)`. The elementary lower constant ceiling is `log4/2`; the true
  `θ(x)~x` needs PNT. So `nagura_prime` genuinely requires **Nagura's precise *tuned* numerical
  inequality** — the analogue of mathlib's `Bertrand.real_main_inequality` re-derived for `6/5`, NOT a
  reparametrization (I checked `real_main_inequality`; its function/threshold are Bertrand-specific).
- **Why I didn't just write the central-binomial bound this lap:** the full
  `centralBinom_le_of_no_nagura_prime` is a ~60-line adaptation of mathlib's intricate
  `centralBinom_le_of_no_bertrand_prime` + `centralBinom_factorization_small`, and even when done it
  doesn't close Nagura without the tuned inequality (which I don't have). It's a fresh-lap task, not a
  lap-end cram. The `theta_lower` infra IS the reusable part of that work.

## ✅ State (all observed this lap)
- `lake build` GREEN: **8286 jobs** (full repo, confirmed). Tree clean at `acaf293`.
- 12 commits this lap (`69066c1`→`acaf293`). Headlines kernel-verified axiom-clean; new lemmas too.
- Axiom-clean & landed in `PrimeGap.lean`: `maxNoThreeInLine_{mono, ge_of_two_mul_prime_le}`,
  `centralBinom_dvd_lcm_Icc`, `four_pow_lt_mul_lcm`, `factorization_finset_lcm`,
  `primePow_dvd_lcm_Icc_iff`, `log_lcm_Icc_eq_psi`, `psi_lower`, `theta_lower`.
- `maxNoThreeInLine_ge_five_fourths` carries `sorryAx` (via `nagura_prime`) — honest, not a headline.
- Aristotle job `1644a603` (`aris-nagura`) RUNNING — attempting `nagura_prime` self-contained.
- `ON-LINE-REQUEST.md` filed for Nagura 1952's tuned inequality. Old HJSW ask closed.

## 🎬 Next actions
1. **`aristotle list`; if `1644a603` is IDLE, harvest it**: `aristotle download 1644a603 --destination
   /tmp/aris-nagura-out`, kernel-verify + `#print axioms`, port onto `nagura_prime`. (If it 500s like
   `72891d77` did, retry / move on.)
2. **Check for `ON-LINE-FINDINGS-*.md`** in the repo root — if the Nagura inequality landed, formalize it.
3. **Otherwise build `centralBinom_le_of_no_nagura_prime`** (the scaffolding, mirroring
   `Mathlib/NumberTheory/Bertrand.lean:162` `centralBinom_le_of_no_bertrand_prime` but keeping the
   `(6n/5, 2n]` primes), then the tuned numerical inequality (the analytic core, `real_main_inequality`
   analogue for `6/5`, `n≥N₀`; small `n∈[25,N₀)` by `decide`). This closes `nagura_prime`.

## ⚠️ Gotchas
- `open ArithmeticFunction` makes `id` ambiguous (`ArithmeticFunction.id` vs `_root_.id`) → breaks
  `Finset.lcm id`. Use **`open scoped ArithmeticFunction`** to get the `Λ` notation without the clash.
- `ALLOW_STOP=1` this run, BUT the project is **not** complete (`nagura_prime` open) — do NOT write a
  stop sentinel. The governor re-checks `src/` is clean and would decline anyway.
- Aristotle `show`/`download` 500'd server-side on `72891d77` (the headline faithfulness check) this
  lap — that job is not load-bearing; the real faithfulness defense is `Statement.lean` + anchors.

## 📁 Key files
- `src/.../NoThreeInLine/PrimeGap.lean` — the entire frontier (interface, Chebyshev infra, `nagura_prime`).
- `src/.../NoThreeInLine/Statement.lean` — headlines audit surface (all axiom-clean).
- `PENDING_WORK.md` (top section) — three attack paths + the definitive crude-insufficiency finding.
- `STATUS.md` — living overview + axiom ledger. `ON-LINE-REQUEST.md` — the Nagura request.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context above, then start at Next action #1 (`aristotle list` to check `1644a603`).**
