# Handoff: FINISH-AND-STOP honored — Dirichlet divisor side-quest built, verified, then reverted

**Date**: 2026-06-19 ~20:30 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `6cf2275`

> Supersedes `HANDOFF-2026-06-19-1639.md`. The Mertens/NTL headlines remain COMPLETE & axiom-clean.

## ⚠️ Read this first — an external directive landed mid-lap
While this lap was running, an external (host/Trevor) session **edited the working-tree `STATUS.md`**
(after my first commit) to inject a `🛑 FINISH-AND-STOP` directive (now committed, `STATUS.md` line 3):
the Dirichlet divisor problem is a **non-headline adjacent side-quest** — abandon/revert it, wind down
to the headline-only repo, confirm headlines axiom-clean, and **self-stop**. Git confirms this was NOT
in `STATUS.md` at my prior commit and names the exact files I had just created, so it is a genuine,
targeted operator intervention. **I honored it.**

## What this lap did (chronological)
1. Took the prior handoff's "Next action #1" (pick a fresh mathlib-absent classical theorem) and chose
   the **Dirichlet divisor problem** `∑_{n≤N} d(n) = N·log N + (2γ−1)·N + O(√N)` — mathlib-absent, with
   an explicit `--TODO: hyperbola method` in `ArithmeticFunction/Misc.lean`.
2. **Fully proved it, axiom-clean** (`[propext, Classical.choice, Quot.sound]`): the hyperbola identity
   `∑_{a≤N}⌊N/a⌋ + ⌊√N⌋² = 2∑_{a≤⌊√N⌋}⌊N/a⌋` via lattice-point inclusion–exclusion
   (`card_union_add_card_inter`), a sharp harmonic estimate `|H_n−log n−γ| ≤ 1/n` (squeezing γ between
   mathlib's two monotone Euler–Mascheroni sequences), and the `O(√N)` assembly. **Committed at `d356584`**
   (`src/LeanFormalizations/NumberTheory/DivisorProblem.lean`, ~280 lines, full repo green 8298 jobs).
   Aristotle job `04afa410` (the hyperbola identity) was submitted then cancelled once proved locally.
3. **Then discovered the FINISH-AND-STOP directive** and reverted the side-quest (`6cf2275`): removed the
   file + its root import. **The complete, verified theorem is preserved verbatim in git history at
   `d356584`** — `git show d356584:src/LeanFormalizations/NumberTheory/DivisorProblem.lean` (or
   `git checkout d356584 -- src/.../DivisorProblem.lean`) restores it instantly if Trevor wants it.
   NOTE: Trevor wrote the directive referencing `aristotle_hyperbola.lean`, i.e. he saw my *first*
   checkpoint (unfinished sorry-stub), not the completed axiom-clean version — but his classification
   ("adjacent, not a headline") stands regardless, so the revert is the faithful action.

## ✅ Current state — headline-only, green, axiom-clean
- `lake build` = **8297 jobs green** (pre-commit gate passed on `6cf2275`).
- Re-verified axiom-clean this lap: `maxNoThreeInLine_ge_three_halves_sub` (flagship `3/2−o(N)`) and
  `mertens_third_classical_eGamma` — both `[propext, Classical.choice, Quot.sound]`.
- `grep '^axiom' src/` = none. The three open `sorry`s are exactly the pre-existing off-headline ones.

## 🟡 Outstanding part of the directive (NOT executed — surfaced for Trevor)
The directive also asks to "clear or quarantine those off-headline sorries so `src/` is sorry-free."
I deliberately did **not** do this, because it conflicts with the charter's hard rule **"never delete
structure"** and risks the green build at self-stop. The three sorries (all gating NO headline; headlines
confirmed axiom-clean):
- `Wiener.lean:242` `prelim_decay_2`, `Wiener.lean:249` `prelim_decay_3` — a **dead-code island**
  (`prelim_decay_3` → `decay_alt` only; `decay_alt` is unused; verbatim faithful PNTAnd port). Safely
  removable together with `decay_alt` (+ maybe `AbsolutelyContinuous`) if Trevor confirms deleting the
  faithful port is acceptable.
- `PrimeGap.lean:1119` `nagura_prime` — the documented **active-frontier crux** (feeds the off-flagship
  rung `maxNoThreeInLine_ge_five_fourths`). Should be **quarantined, not deleted** (it's the crux).
**Decision needed from Trevor:** do you want these deleted/quarantined (breaking the "never delete
structure" rule) for a strictly sorry-free `src/`, or left as documented disclosed sorries? I left them
intact pending that call.

## 🧠 Bonus advance recorded this lap (kept — about a real headline-supporting crux, not the side-quest)
`PENDING_WORK.md` item 3 now carries a **refined nagura diagnosis**: Nagura is **elementary** (the prior
"needs effective PNT" was imprecise). The repo's refined Chebyshev stack uses the classical Sylvester–
Chebyshev `{2,3,5,30}` function; its constants are exactly Chebyshev's 1852 bounds — lower `A≈0.9213`,
upper `(6/5)A` — ratio **exactly 6/5**, so the method gives `c>6/5` (hence the 5/4 `exists_prime_in_five_fourths`)
but cannot attain Nagura's `6/5`. **Soft wall:** ANY strict improvement to either constant breaks it.
Concrete attack: replace `{2,3,5,30}` with a sharper elementary combination (longer period) whose
Chebyshev constant exceeds `0.9213` — re-do `floor_comb_bounds` (`decide` over the new period),
`logFactorial_comb_{lower,upper}`, and the telescoping. Bounded multi-lap port.

## 🎬 Next session
Per the directive, the lap **self-stopped** here. If Trevor lifts FINISH-AND-STOP, the live options are:
(1) restore the divisor theorem from `d356584`; (2) execute the nagura `{2,3,5,30}→sharper` constant
attack (PENDING item 3); (3) resolve the sorry-free wind-down (the structure-vs-sorry-free call above).
Do **not** start new side-quests while FINISH-AND-STOP stands.
