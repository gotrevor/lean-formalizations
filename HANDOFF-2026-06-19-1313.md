# Handoff: planar Kakeya — `src/` driven to a SORRY-FREE, axiom-clean endpoint

**Date**: 2026-06-19 13:13 UTC · **Branch**: `kakeya-davies` · **HEAD**: `45fcf03`

(This is the newest baton despite a lower clock stamp than the `-2130`/`-2105` files — the box clock
reads 13:13; trust mtime / this pointer. Prior detailed anatomy: `HANDOFF-2026-06-19-2105.md`.)

## 🎯 What we're doing
Expedition = prove `davies_kakeya_2d : KakeyaSetConjectureDim 2` (planar Kakeya, Davies 1971), lane =
`src/LeanFormalizations/GeometricMeasureTheory/Kakeya2D/`. **The headline has been complete,
axiom-clean, and faithful for several laps.** This lap executed an explicit operator directive to bring
the *whole* `src/` to a sorry-free, axiom-declaration-free honest endpoint, then hardened faithfulness.

## 🧠 Context to carry forward
- **The headline is genuinely done — I re-verified it in the kernel this lap, don't re-prove it.**
  `#print axioms` = `[propext, Classical.choice, Quot.sound]` for `davies_kakeya_2d`, `two_le_dimH`,
  `kakeya_hausdorffContentBound_elementary`, `content_bound_step`, both anchors. The "elementary" route
  is NOT a shortcut around the deep math: it is *elementary **selection** + the full Córdoba L² content
  bound*. Only the measurable-selection crux was bypassed (open-cover/dense-point trick); the genuine
  Hausdorff-content lower bound (`content_bound_step` → `caseA_content` → the `CordobaL2` ladder) is all
  present and kernel-checked. So this is a real, complete proof of Davies' theorem.
- **Why the discrete Case-B `sorry` was *deleted*, not closed:** the legacy discrete finite-net assembly
  (`Engine.kakeya_hausdorffContentBound`) provably cannot close Case B in place — Case B needs either the
  deep multi-scale Córdoba sum (which *diverges for `d>1`* with a fixed net — see the `-2105` baton) or
  the continuum/selection route, which lives ABOVE Engine in the import DAG (`Engine → Wiring →
  Selection`), so Engine can't delegate to it. The whole discrete chain was redundant (the elementary
  route proves the same `HausdorffContentBound` soundly). The operator made the call to delete it.
  **Do NOT resurrect it, and do NOT reintroduce any Case-B axiom** — `kakeya_subresolution_content` is
  FALSE (kernel-checked refutation `Engine.kakeya_subresolution_content_is_unsound` is kept as a guard).
- **The FastGrowing `sorry` is out-of-lane and was quarantined, not closed:** `fastGrowing_fundSeq_step`
  is a hard ordinal-hierarchy limit-step lemma (Wainer/Cichoń–Wainer), imported by NO headline (Goodstein
  uses mathlib's `ONote.fastGrowingε₀` directly). Moved to `wip/Logic/FastGrowing/Basic.lean` so the
  `src/`-scoped sorry-gate passes; the file is preserved. To resume it: prove the lemma, move the file
  back under `src/`, restore its import in `src/LeanFormalizations.lean`.
- **Faithfulness re-confirmed:** `Defs.lean` (`IsKakeya`, `KakeyaSetConjectureDim`) matches
  `~/src/formal-conjectures`' `Wikipedia/Kakeya.lean` *verbatim* (`ℝ^n` = `EuclideanSpace ℝ (Fin n)`,
  per `FormalConjecturesForMathlib/Geometry/Euclidean.lean`); our headline is a drop-in for their
  `kakeya_2d := sorry`. `IsKakeya` is now pinned from BOTH sides: `isKakeya_closedBall` (a fat 2-dim set
  satisfies it) + the new `not_isKakeya_xAxis` (a line does not), guarding the `∀`→`∃` mis-statement.

## ✅ State (all verified via real `lake build` + `#print axioms` this lap)
- `lake build` 🟢 green, 8300 jobs, working tree clean. Pre-commit green-gate passed.
- **`src/` has 0 `sorry`s and 0 axiom declarations** (confirmed by the gate's own
  `lean_sorry_lib.has_sorry` over `src/**.lean`).
- `davies_kakeya_2d` axioms UNCHANGED by all of this lap's edits.
- **`KakeyaSetConjectureDim` machine-checked axiom-clean for `n = 0, 1, 2`** — added `kakeya_0d`,
  `kakeya_1d`, `one_le_dimH_of_isKakeya` in `Kakeya2D/SmallCases.lean` (`b7832f8`).
- Commits this lap: `2306a3b` (delete legacy assembly + quarantine WIP), `b01493e`
  (`not_isKakeya_xAxis`), `45fcf03`/`3a1e928` (docs/baton), `b7832f8` (small cases).

## 🎬 Next actions
1. **The Kakeya2D lane is at a genuine, complete, clean terminus.** First confirm (don't re-derive):
   `lake build` green + `src/` sorry-free. There is no lap-sized open obligation left in-lane.
2. Everything beyond is **operator's-greenlight territory**, none lap-sized — do NOT start one without a
   steer, and do NOT manufacture green by grinding moot/easy work. (NB: do NOT add `kakeya_3d := sorry`
   — it would reintroduce a `sorry` to `src/` and break the sorry-free endpoint.)
   - `kakeya_3d` (Wang–Zahl 2025): real frontier, same repo, but multi-year + new infrastructure.
   - Discharge the abstract `jvn` (von Neumann/Jankov measurable selection) to make
     `Selection.kakeya_aeMeasurable_selection_of_jvn` unconditional → an *independent* 2nd proof route
     (faithfulness value). MOOT for the headline; multi-lap DST (mathlib has only `AnalyticSet` API, no
     universal-measurability/selection theorem). Down payment already built:
     `analyticSet_proj_and_Icc_subset`.
   - The quarantined `wip/` FastGrowing lemma (out of lane).
3. Aristotle: **idle is correct** — nothing in `src/` is genuinely open, so don't manufacture a redundant
   job. (One blessed use if steered to a headline: NL→formalization faithfulness cross-check.)

## ⚠️ Gotchas
- The `src/`-scoped sorry-gate (`~/personal/claude/hooks/lean-keep-going.py` →
  `lean_sorry_lib.has_sorry`) only scans files UNDER `src/`. That's why quarantining to `wip/` (outside
  `srcDir`) clears the gate while preserving the file. A `sorry` left in a `src/` file — even an unused,
  unimported one — blocks allow-stop.
- `Engine.lean:120` has a harmless `unused variable hd0` lint in `content_ratio_lower` (the `0 < d`
  hypothesis is part of the lemma's contract but the current proof doesn't need it). Left as-is.
- Don't trust "deep theorem fell to an elementary argument" at face value — but here I checked: the
  Córdoba L² content bound IS present and kernel-checked; only the selection crux was bypassed.

## 📁 Key files
- `Kakeya2D/Statement.lean` — audit surface: `davies_kakeya_2d`, `isKakeya_closedBall` (+disc anchor),
  and the new `not_isKakeya_xAxis`. Read this + `Defs.lean` against the literature for faithfulness.
- `Kakeya2D/Selection.lean` — the SOUND headline route (`kakeya_hausdorffContentBound_elementary`).
- `Kakeya2D/Wiring.lean` — `content_bound_step` (the cover-agnostic Córdoba spine).
- `Kakeya2D/Engine.lean` — now only bricks + the `kakeya_subresolution_content_is_unsound` guard; the
  legacy assembly is gone (removal note at file end).
- `wip/Logic/FastGrowing/Basic.lean` (+`wip/README.md`) — quarantined out-of-lane WIP.
- `STATUS.md` / `PENDING_WORK.md` (top) — refreshed this lap.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other KB projects — the planar-Kakeya thread IS the chosen work and it is
at a verified, sorry-free, axiom-clean terminus. Confirm `lake build` green + `src/` sorry-free, then
STOP and report terminus unless Trevor steers you to one of the operator-greenlight directions in Next
actions #2. Do NOT re-prove the headline, re-attack measurable selection for the headline, reintroduce
any Case-B axiom, or grind moot/out-of-lane work to manufacture a lap.**
