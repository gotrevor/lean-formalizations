# Handoff: BV-Fourier decay family COMPLETE & axiom-clean — lap budget reached

**Date**: 2026-06-20 17:10 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `d06a0a4`

## 🎯 What we're doing
Repo is in **finish-and-stop mode** (HJSW no-three-in-line ladder + Mertens trilogy headlines all
COMPLETE + axiom-clean; de-vendor + mathlib v4.31.0 upgrade DONE). This lap re-verified that clean state,
then — pushed by the keep-going hook — drove the one genuinely-open thread (`prelim_decay` BV-Fourier
decay) **to full completion** and added it to the build as a bonus.

## 🧠 Context to carry forward
- **The operator explicitly authorized stopping this lap** ("You're allowed to stop this treadmill if
  you've de-vendored and done the 4.31.0 upgrade" — both done). The automated stop-hook kept firing after
  that anyway; I honored the human authorization + spent budget. Successor: do **not** treat the hook as a
  mandate to manufacture new side quests — finish-and-stop holds.
- **Key math insight (reusable):** the L¹-translation-by-total-variation bound `∫‖f t−f(t+h)‖ ≤ |h|·V` is
  provable in mathlib **without** the (absent) *signed* variation/Stieltjes measure: use the **monotone
  variation function** `W(t)=(eVariationOn f (Iic t)).toReal`. Pointwise `‖f t−f(t+h)‖≤W(t+h)−W(t)` via
  `eVariationOn.add_le_union` (superadditivity on `Iic t ⊎ Icc t (t+h)=Iic(t+h)`) + `eVariationOn.edist_le`;
  then the finite window **telescopes** `∫_{−N}^{N}(W(·+k)−W)=∫_N^{N+k}W−∫_{−N}^{−N+k}W ≤ k·V−0` (oriented
  `intervalIntegral.integral_add_adjacent_intervals` — no N-size constraint! — + `W≤V`/`W≥0`), uniform in N,
  so the nonneg integrand is integrable (`integrable_of_intervalIntegral_norm_bounded`) and
  `∫_ℝ=lim_N∫_{−N}^{N}≤k·V` (`intervalIntegral_tendsto_integral`+`le_of_tendsto`).
- **(b1) half-period shift** trick: write `𝓕` via `fourier_eq'` in the `Complex.exp(↑r·I)` phase form
  (norm 1), shift by `1/(2u)`, phase picks up `exp(−πI)=−1` ⇒ shifted integral `=−𝓕 f u`, add the two reps.
- **prelim_decay_3** is just `prelim_decay_2` on `ψ'` composed with mathlib's `Real.fourier_deriv`
  (`𝓕(deriv ψ) u = 2πI·u·𝓕 ψ u`).
- **Aristotle job `59a9a2c2`** (the (b2) statement, submitted while genuinely open) is **superseded** by
  the local proof. Next lap: glance + discard, do NOT re-feed or port it.

## ✅ State (all verified from real output this lap)
- **`lake build` GREEN — 8621 jobs** (observed repeatedly, incl. pre-commit green-gates).
- **`src/` sorry-free** (0 `sorry`/`by sorry` tactics); all prior headlines untouched.
- New file `src/LeanFormalizations/RealAnalysis/BVFourierDecay.lean` (promoted from `wip/`, namespaced
  `LeanFormalizations.RealAnalysis.BVFourierDecay`, root-imported). Three new axiom-clean theorems
  (`[propext, Classical.choice, Quot.sound]`), all in `.bump-axioms`:
  `prelim_decay_2_route_b` (`‖𝓕 f u‖≤V(f)/(4|u|)`), `prelim_decay_3_route_b` (`‖𝓕 ψ u‖≤V(ψ')/(8π·u²)`),
  + the (b1)/(b2) cruxes.
- Commits this lap: `38d4af8`(b1)→`bac0a9c`(b2 narrowing)→`d17e7b8`(full+promote)→`dc24e4e`(decay_3)
  →`d7699b1`(lint)→`00e76b3`/`d06a0a4`(PENDING_WORK inventory). Tree clean.

## 🎬 Next actions
1. **Re-verify clean state and SELF-STOP** (finish-and-stop). Headlines + the BV-decay bonuses are complete
   and axiom-clean; `src/` is sorry-free. Do NOT open new threads or manufacture side quests. If Trevor's
   next message gives a new direction, follow it.
2. **Only if directed to push further:** the sharp `2π|u|` constant (vs our non-sharp `4|u|`) is the one
   real open target — see `PENDING_WORK.md` item 1, path (c). I CONFIRMED mathlib has the pieces:
   `LocallyBoundedVariationOn.exists_monotoneOn_sub_monotoneOn` (Jordan decomposition `f=g−h`, g,h monotone)
   + `variationOnFromTo`. Plan: build each monotone part's `StieltjesFunction.measure`, IBP
   `𝓕 f' = ∫e(−tu)d(μ_g−μ_h)` to `2πiu·𝓕 f`, bound by `∫d(μ_g+μ_h)=V`. Multi-lap; don't start without budget.

## ⚠️ Gotchas
- **v4.31 first-from-source full builds hit macOS-side FD exhaustion** (3-file olean split × parallel heavy
  leaves on the bind mount). Build modules sequentially once to cache, then full `lake build` is a clean
  replay. No `-j` flag in this lake. (Already cached now — only relevant after a clean.)
- `lean-bump`/`relake` rules: never raw `lake update`; dedup `--from` must be `~/.lake-base/<ver>`.
- Build output is noisy with pre-existing linter warnings (deprecated `push_neg`/`Set.diff`, unused vars) —
  none are errors; look for "Build completed successfully". The dep's `Wiener.lean:323/342` carry 2
  `prelim_decay` sorries — **dead code off the WeakPNT path; `weakPNT` is axiom-clean with them present.**

## 📁 Key files
- `src/LeanFormalizations/RealAnalysis/BVFourierDecay.lean` — the completed BV-Fourier decay family.
- `.bump-axioms` — faithfulness footprint (now incl. the two `prelim_decay_*_route_b`).
- `STATUS.md` / `PENDING_WORK.md` — top banners record this lap; PENDING_WORK has the sharp-constant route.
- `HANDOFF-2026-06-20-1620.md` — fuller narrative of this lap's work.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. The headlines
AND the BV-Fourier-decay bonus are complete + axiom-clean and `src/` is sorry-free: re-verify clean state
and self-stop (finish-and-stop), unless Trevor directs otherwise. Do NOT manufacture new side quests.**
