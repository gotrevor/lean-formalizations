# Handoff: Davies planar Kakeya — headline now rests on a Kakeya-AGNOSTIC selection axiom

**Date**: 2026-06-19 11:15 UTC · **Branch**: `kakeya-davies` · **HEAD**: `1f7dbfb`

## 🎯 What we're doing
Prove `davies_kakeya_2d : KakeyaSetConjectureDim 2` (planar Kakeya ⟹ `dimH = 2`, Davies 1971). Whole
job = the lower bound `two_le_dimH`. This lap drove the lone axiom from a murky bespoke residual all
the way down to a **single textbook, Kakeya-agnostic measurable-selection axiom**.

## 🧠 Context to carry forward
Read `STATUS.md` (top block) + `ON-LINE-REQUEST.md` UPDATE 6 first — they're current as of this lap.

**The big mental model shift this lap (the key insight):** the continuum route's downstream pigeonhole
(`MeasurableRoute.exists_continuum_dominant_scale`) only ever consumes the **a.e. / measure** statement
`1 ≤ vol{t∈[0,1] : aθ+t·dirθ ∈ ⋃ C n}` (covered length `= 1`), *never* pointwise segment-containment.
That distinction is everything: with only a.e.-coverage required, the selection graph
`G = {(θ,p) : 1 ≤ vol{t : p+t·dirθ ∈ F}}` is **Borel** (the covered length is jointly measurable in
`(θ,p)`), so the selection is **von Neumann / Jankov–von Neumann strength** — NOT the Π¹₁/Kondô
coanalytic-uniformization that *pointwise* containment (`∀t∈[0,1], …`, a co-projection over compact
`[0,1]`) would force. This is why the headline axiom is now a clean, citable, standard theorem.

**What got built (chain, bottom → top):**
- `kakeya_borel_selection` (axiom, `Selection.lean`) — the ONLY axiom. Verbatim: a Borel
  `G ⊆ ℝ × Plane` with non-empty sections over `[0,1]` has an `AEMeasurable` selector. Zero Kakeya
  content.
- `kakeya_aeMeasurable_selection_of_jvn` (proven) — feeds `kakeya_borel_selection` the Kakeya graph;
  all Kakeya geometry/measurability discharged here (`measurable_coveredLength_prod` joint
  measurability ⟹ Borel graph; `isKakeya_exists_aeCover` non-empty sections from `IsKakeya`).
- `Wiring.kakeya_hausdorffContentBound_of_aeMeasurableSelection` (lemma B, proven) — bridges
  `AEMeasurable a` → `Measurable a` by replacing `a` with its measurable representative
  (`AEMeasurable.mk` / `.measurable_mk` / `.ae_eq_mk`); a.e.-coverage transfers on the a.e. set.
- `Wiring.kakeya_hausdorffContentBound_of_measurableSelection` (lemma A) — generalised this lap to take
  the **a.e./measure** coverage hypothesis (was pointwise). The spine
  (`exists_continuum_dominant_scale`, `exists_continuum_caseA_numerator`) was likewise generalised.
- `Selection.{hausdorffMeasure_pos_of_isKakeya, two_le_dimH}` → `Statement.davies_kakeya_2d`.

**Approaches considered & not taken:** (a) the *pointwise* measurable selection (Π¹₁/Kondô) — avoided
via the a.e. trick above; (b) discharging via mathlib selection — mathlib v4.29.1 has **no** measurable
selection theorem and **no** universal measurability of analytic sets (it HAS `AnalyticSet` + its
projection/closure API in `MeasureTheory/Constructions/Polish/Basic.lean`, but not the selector); (c)
feeding Aristotle — skipped: the open lemma is the full von Neumann selection (a multi-hundred-line DST
build, no bounded sub-chunk), and the corpus flags Aristotle weak on measure theory.

**The discrete route is preserved, off-path.** `Engine.kakeya_hausdorffContentBound` (Case A proven /
Case B `kakeya_subresolution_content` axiom) + `*_discrete` headline theorems are kept as real proven
structure but **not** on `davies_kakeya_2d`'s path — that axiom does NOT appear in `#print axioms`.

## ✅ State (verified via real `lake build` + `#print axioms` this lap)
- Build 🟢 green, **8300 jobs** (observed, full `lake build`).
- `#print axioms davies_kakeya_2d = [propext, Classical.choice, Quot.sound, kakeya_borel_selection]`
  (observed). All `Selection.lean` bricks axiom-clean (`[propext, Classical.choice, Quot.sound]`).
- Commits this lap (newest last): `224e60f` (clean `kakeya_measurable_selection`) → `fd66d7f`
  (Selection.lean narrows to JvN) → `721559a` (spine → a.e. form) → `60ca60b` (headline → Kakeya-agnostic
  `kakeya_borel_selection`) → `1f7dbfb` (docs). All green-gated.
- 1 dormant disclosed `sorry` at `Logic/FastGrowing/Basic.lean:121` — out of the Kakeya lane, leave it.

## 🎬 Next actions
1. **Attack `kakeya_borel_selection`** — the sole remaining axiom, now a standard von Neumann /
   Jankov–von Neumann measurable selection. This is deep DST (multi-lap). Concrete sub-steps to chip:
   (a) projection of a Borel `G ⊆ ℝ×Plane` is analytic (`MeasurableSet.analyticSet` +
   `MeasurableSet.analyticSet_image` for the continuous `fst`), `[0,1] ⊆ proj G`; (b) the hard core =
   the **Souslin-scheme / von-Neumann-derivative selector**, needing "analytic sets are universally
   measurable" (Choquet capacitability) — neither in mathlib. Check `ON-LINE-FINDINGS-*` at lap start
   (I filed `ON-LINE-REQUEST.md` UPDATE 6 asking for the cleanest formalization path / any portable
   formalization / a lighter closed-section selection).
2. If genuinely blocked on (1) pending a reference: there is no other open Kakeya-lane work, so the
   honest move is to begin transcribing whatever von Neumann-selection source the findings doc supplies,
   OR formalize the bounded prerequisite (a) above as a down payment.

## ⚠️ Gotchas
- `AEMeasurable.mk a` (= `AEMeasurable.mk a ha` via dot notation `ha.mk a`), `ha.measurable_mk`,
  `ha.ae_eq_mk` is the clean idiom to feed an a.e.-measurable function into a `Measurable`-expecting
  lemma; transfer a.e. properties with `filter_upwards [..., ha.ae_eq_mk]` then `rw [show (ha.mk a) θ =
  a θ from hθeq.symm]`.
- `setLIntegral_mono_ae` wants `∀ᵐ x ∂μ, x∈s → f x ≤ g x` (UNRESTRICTED ae + implication, not
  `μ.restrict s`). That shape is why the spine's a.e. hypothesis is `∀ᵐ θ ∂volume, θ∈Icc → …`.
- `affineSegment ℝ a (a+v) = lineMap a (a+v) '' Icc 0 1`; `AffineMap.lineMap_apply_module'` gives
  `t • ((a+v)-a) + a`; finish with `rw [show (a+v)-a = v from by abel]; abel`.
- The legacy `kakeya_subresolution_content` axiom still exists in `Engine.lean` (off-headline). Don't
  be alarmed it's in the build — `#print axioms davies_kakeya_2d` does NOT list it.

## 📁 Key files
- `Kakeya2D/Selection.lean` — **the narrowing + the lone axiom + the headline** (`kakeya_borel_selection`,
  `kakeya_aeMeasurable_selection_of_jvn`, geometry/measurability bricks, `two_le_dimH`). Start here.
- `Kakeya2D/Wiring.lean` — lemma A (a.e. form) + lemma B (`AEMeasurable` bridge).
- `Kakeya2D/MeasurableRoute.lean` — the spine (generalised to a.e./measure coverage).
- `Kakeya2D/Engine.lean` — legacy discrete route (Case A proven / Case B axiom), off-path.
- `STATUS.md` (ledger) · `ON-LINE-REQUEST.md` UPDATE 6 (the sharpened, Kakeya-agnostic ask).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context, then start at Next action #1: attack `kakeya_borel_selection` (von Neumann / Jankov–von Neumann
measurable selection) — first check for an `ON-LINE-FINDINGS-*` answer to UPDATE 6, then either
transcribe the source or formalize the bounded projection-is-analytic prerequisite. Do NOT re-attempt
the discrete route or relitigate the a.e. vs pointwise choice — the a.e. graph is Borel, that's settled.**
