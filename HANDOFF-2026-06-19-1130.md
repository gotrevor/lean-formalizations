# Handoff: Davies planar Kakeya — dominant-scale axiom DISCHARGED into Case A; Case B is the wall

**Date**: 2026-06-19 11:30 UTC · **Branch**: `kakeya-davies` · **HEAD**: `30712fa`

## 🎯 What we're doing
Prove `davies_kakeya_2d : KakeyaSetConjectureDim 2` (planar Kakeya ⟹ `dimH = 2`). The whole job is the
lower bound. **This lap discharged the historic blocker** — the cross-scale / dominant-scale
orchestration — into a machine-checked proof, leaving ONE narrower axiom.

`#print axioms davies_kakeya_2d = [propext, Classical.choice, Quot.sound, kakeya_subresolution_content]`
(verified this lap, no `sorryAx`). The former monolithic `kakeya_dominant_scale_count` is **deleted**.

## 🧠 Context to carry forward (read this — it's the mental model)
The architecture: fine net of `2ᴶ` directions (`J = 1` fixed) → per-direction measurable pullbacks
(`exists_measurable_pullback_cover`) → capped dyadic scale `g n = min(dyadicIdx (ediam Uₙ).toReal, J)`
→ per-scale union covered length `L m i` → `exists_dominant_shift` gives a dominant scale `j ≤ J` and a
shift `β` whose `2⁻ʲ`-subnet (base angle `c = β·2⁻ᴶ`) retains the covered length → numerator
`1/((j+1)(j+2)) ≤ ∑ᵢ 2·2⁻ʲ·vol(Aᵢ)`. Then **case split** `j ≤ J`:
- **Case A (`j < J`)**: the scale-`j` fiber `{g n = j}` is genuine (`2⁻⁽ʲ⁺¹⁾ < ediam ≤ 2⁻ʲ`), so the
  base-angle Córdoba content brick `caseA_content` applies. Finite/infinite fiber split: infinite fiber
  ⟹ `∑'ediam^d = ⊤` (infinitely many pieces each `≥ 2⁻⁽ʲ⁺¹⁾`); finite ⟹ `caseA_content` on `s.toFinset`.
  **Fully proven, no axiom.**
- **Case B (`j = J`)**: the scale-`J` bucket is `{ediam ≤ 2⁻ᴶ}` — NOT a single scale (cap conflates all
  finer pieces), no `ediam` lower bound, so `cover_content_per_scale` fails. Cites `kakeya_subresolution_content`.

**Why Case B is the genuine wall (the Hausdorff-vs-box gap) — and the dead end I mapped this lap:**
I tried to close Case B with the multi-scale Córdoba `L²` sum over the *fixed* `2ᴶ`-net. It
**provably diverges to 0 for `d > 1`** (exactly the range needed for `dim → 2`): minimizing
`∑_{j≥J} S_j² 2^{j(1-d)}` subject to `∑_{j≥J} S_j ≥ 2ᴶ⁻¹` gives `(2ᴶ⁻¹)²/∑_{j≥J}2^{j(d-1)} = 0` because
`∑_{j≥J} 2^{j(d-1)} = ∞` for `d>1`. Intuition: the coarse net's per-scale count `M_j ≳ S_j²·2^{j-J}/poly`
is too weak at fine scales; an adversary spreads covered length over unboundedly-fine scales. The
correct count at sub-scale `j` needs the **`2ʲ`-net (right resolution)** (`M_j ≳ S_j(j)²/poly(j)`), but
then each scale uses a *different* net and there's no obvious cross-scale sum bounded below. The
shift-average resolves exactly ONE scale at its own resolution; Case B is precisely "no single dominant
scale — mass at unboundedly fine scales". **So do NOT relitigate the fixed-net L² sum for `d>1` — it's
a proven dead end.** The crude per-piece direction-counting bound also fails (it's the bush that
Córdoba's L² is needed to control). Closing Case B needs literature: the exact multi-scale combination,
or Davies' 1971 projection/duality argument (which may sidestep multi-scale Córdoba entirely and be more
formalizable). Both are requested in `ON-LINE-REQUEST.md` UPDATE 4.

## ✅ State (all verified via real `lake build` this lap — full library green, 8297 jobs)
Two commits (`7d760e2`, `30712fa`), `#print axioms`-clean except the lone `kakeya_subresolution_content`.
New in `NetThinning.lean`: `dyadicIdx` + `dyadicIdx_window` (dyadic length window), `caseA_content`
(base-angle finite-fiber content brick). `Engine.lean`: `kakeya_subresolution_content` axiom (the Case B
residual, faithfully stated) + the rewritten `kakeya_hausdorffContentBound` (the full assembly).

## 🎬 Next actions
1. **Harvest** any `ON-LINE-FINDINGS-*.md` answering `ON-LINE-REQUEST.md` UPDATE 4 (Case B / Davies
   projection / multi-scale combination). `git mv` into `archive/findings/` once consumed.
2. **Attack Case B** (`kakeya_subresolution_content`) — the lone crux. Priority order:
   (a) If a findings doc gives the rigorous multi-scale combination for `d>1` OR Davies' projection
   argument, transcribe it. (b) Else attempt **path 3 (uncapping)** from `PENDING_WORK.md` A0″ — make
   Case A handle ANY genuine dominant scale `j ≤ J` (currently the cap spuriously triggers Case B when a
   genuine coarse dominant scale coexists with sub-resolution mass); needs an `exists_dominant_shift`
   variant returning a disjunction. This is a bounded ℝ≥0∞ pigeonhole — Aristotle-feedable. (c) The
   `d<1` contradiction (path 1) is provable but doesn't reduce the headline's essential dependency.
3. Keep banging on Case B across laps; it's the whole remaining obligation.

## ⚠️ Gotchas
- `J = 1` is fixed in `kakeya_hausdorffContentBound`; any `J ≥ 1` works for correctness (Case B axiom
  absorbs the residual). Larger `J` widens Case A's reach but the adversary's fine covers still hit Case B.
- `#print axioms` check: `cp` the root `LeanFormalizations.lean` first, append a temp `_axchk` import,
  build, then **restore from the cp backup** (don't `git checkout` — loses uncommitted root edits) and
  delete the temp file. (Did this cleanly this lap.)
- Pre-existing dormant `sorry` at `Logic/FastGrowing/Basic.lean:121` (separate thread, NOT Kakeya) —
  keeps `--allow-stop` closed; ignore for Kakeya work.
- Cosmetic warnings only in the Kakeya files (`unused hv`/`hd0`, deprecated `mul_le_mul_left'`/`push_neg`).
- Aristotle: no genuinely-OPEN *bounded* lemma right now (Case B is research measure theory it fails at,
  per the corpus note). Idle is correct — don't manufacture redundant cross-confirms. A bounded target
  appears only if you pursue path-3 uncapping (the disjunction pigeonhole).

## 📁 Key files
- `…/Kakeya2D/Engine.lean` — `kakeya_subresolution_content` (Case B axiom + docstring) +
  `kakeya_hausdorffContentBound` (the full assembly) + `content_ratio_lower` + headline.
- `…/Kakeya2D/NetThinning.lean` — `dyadicIdx`/`dyadicIdx_window`, `caseA_content`, and the proven
  combinatorial bricks (`exists_dominant_shift`, `exists_shift_ge`, `one_le_tsum_volume_fiber_union`,
  `exists_measurable_pullback_cover`).
- `…/Kakeya2D/{Cover,Cordoba,CordobaL2,Directions}.lean` — base-angle Córdoba chain.
- `PENDING_WORK.md` §A0″ (this milestone + Case B attack paths); `ON-LINE-REQUEST.md` UPDATE 4 (sharp ask).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context above, then start at Next action #1 (harvest findings), then #2 (attack Case B). The
dominant-scale orchestration is already proven; Case B (the Hausdorff-vs-box gap) is the one genuine
obstacle — keep banging on it. Do NOT relitigate the fixed-net L² sum for `d>1` (proven dead end above).**
