# Handoff: weakPNT DISCHARGED — flagship 3/2−o(N) now unconditional

**Date**: 2026-06-19 11:30 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `8e650db`

## 🎯 What we're doing
Isolated clone for the no-three-in-line / HJSW frontier. The flagship
`maxNoThreeInLine_ge_three_halves_sub` (HJSW-optimal `3/2 − o(N)`) rested on ONE cited deep axiom
`weakPNT : Chebyshev.psi ~[atTop] (·)` (the Prime Number Theorem, ψ∼x). **This lap discharged it.**
The prior handoff (`...-1058.md`) had this as a multi-lap 🟠 "port or wait-and-cite" problem; the
port turned out to be **one lap**.

## 🧠 Context to carry forward
- **The big result is DONE and committed.** weakPNT is no longer an axiom — it's a machine-checked
  theorem. `maxNoThreeInLine_ge_three_halves_sub` is now **axiom-clean**
  (`#print axioms` = `[propext, Classical.choice, Quot.sound]`, observed this lap). The whole HJSW
  general-N frontier is unconditional.
- **How (this is the reusable lesson):** I ported PrimeNumberTheoremAnd's (`~/src/PrimeNumberTheoremAnd`,
  v4.29.0) Wiener–Ikehara tower onto our mathlib **v4.29.1** by COPYING SOURCE (not a lake dep — that
  route is dead, no egress for PNTAnd's GitHub deps). **Zero math edits** — only: strip `import Architect`
  + `set_option lang.lemmaCmd true`; strip `@[blueprint …]` attrs (a bracket-matcher that treats
  `/-- … -/` as opaque) and `blueprint_comment /-- … -/` blocks; redirect internal imports to local paths.
  The feared v4.29.0→v4.29.1 drift **did not materialize** — every file built verbatim. The Python
  strippers I used are inline in the commits' workflow (re-deriving them is trivial).
- **Critical subtlety that worked:** the mathlib "patch" files (`Asymptotics.lean`) MUST keep upstream
  names/namespaces EXACTLY (`Asymptotics.isLittleO_const_id_cocompact`, `Filter.Eventually.natCast`,
  `IsBigO.natCast`, `Real.tendsto_pow_log_div_pow_atTop`) or `Wiener` won't resolve them. The prior
  lap's brick-1/2 had renamed them into a local namespace — I overwrote that with the faithful version.
- **Three sorries remain, ALL off the (now-resolved) critical path** — do NOT treat them as the crux:
  1/2. `Wiener.prelim_decay_2`/`prelim_decay_3` — verbatim upstream sorries (BV / AC-deriv Fourier
     decay). **Provably DEAD CODE** here (feed only the unused `decay_alt`; clean `#print axioms WeakPNT''`
     confirms they gate nothing). I VERIFIED (not assumed) mathlib lacks both routes' building blocks:
     no Lebesgue–Stieltjes IBP for BV (sharp 2π bound), and no `∫‖ψ(t+h)−ψ(t)‖ ≤ |h|·eVariation` (weaker
     1/4 bound). Also `Real.fourier_deriv` needs everywhere-diff, so the "prelim_decay_3 ← prelim_decay_2"
     reduction is blocked too (hyp is only `AbsolutelyContinuous`). Closing these = building real analysis
     infra; off-path, low value.
  3. `nagura_prime` — now FULLY SUPERSEDED (unconditional 3/2 dominates its conditional 5/4).
- **Aristotle job `c6d615ee` is RUNNING** on `prelim_decay_2` (~3%, slow cache fetch). If it returns a
  real proof next lap: verify in our kernel + `#print axioms`, then port onto `Wiener.lean:244`.
  (Job `9db39882` = the weakPNT attempt I CANCELED once I'd discharged it locally — ignore it.)

## ✅ State (all observed this lap)
- `lake build` **GREEN, 8294 jobs** (pre-commit green-gate passed on every commit). Tree clean at `8650b`/`8e650db`.
- New axiom-clean theorems in `NumberTheory/PrimeNumberTheorem/Consequences.lean`:
  `WeakPNT''` (ψ∼x), `chebyshev_asymptotic` (θ∼x), `pi_alt'` (π(x)∼x/log x — the classic PNT),
  `nth_prime_asymp` (pₙ∼n log n) — each `#print axioms`-verified clean this lap.
- 5 commits: `9f0975f` (discharge), `1dbc152` (θ∼x), `6388ca0` (full Consequences/π∼x/logx),
  `e3d3f2e` (docs), `8e650db` (online docs).

## 🎬 Next actions
1. **Poll Aristotle `c6d615ee`** (`aristotle show c6d615ee-…`). If done with a real `prelim_decay_2`
   proof, verify + port it (then `prelim_decay_3` follows via FT-of-deriv, and `decay_alt` closes) →
   Wiener port becomes 100% sorry-free. If still running, leave it.
2. **Else, decide the prelim_decay cluster's fate** (judgement call, see PENDING_WORK "Cleanup option"):
   either build the missing infra (Stieltjes-IBP for BV — a real multi-lap analysis project) or EXCISE
   the dead `{prelim_decay_2, prelim_decay_3, decay_alt, AbsolutelyContinuous}` island to make the port
   warning-free. I left it intact (faithful + Aristotle attempting).
3. **Higher-value alternative:** extend the now-unlocked PNT — e.g. a standard-form named
   `prime_number_theorem` corollary from `pi_alt'`, or port more of PNTAnd's consequences (Mertens, etc.).
   This showcases the discharge and is low-risk.

## ⚠️ Gotchas
- The two `Wiener.lean` sorry **warnings** are EXPECTED and HARMLESS (dead code). Don't mistake them for
  a tainted build — `#print axioms` on any target is clean.
- Pre-commit hook runs `lake build` and emits many pre-existing lint warnings (push_neg deprecation,
  unused vars) — normal; look for "Build completed successfully".
- `ON-LINE-REQUEST.md` got accidentally deleted from the working tree mid-lap and I `git checkout`-restored
  it — it's fine now (no open blocking asks; one low-priority infra question in UPDATE 3).

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/` — the whole port: `Support`, `Sobolev`,
  `Fourier`, `SmoothExistence`, `Asymptotics` (patches), `Wiener` (4118), `Defs`, `Consequences`.
- `src/LeanFormalizations/Combinatorics/NoThreeInLine/PrimeGap.lean:1154` — `weakPNT := WeakPNT''` (was the axiom).
- `PENDING_WORK.md` top (`## ⭐ weakPNT DISCHARGED`) — current inventory + attack paths for the 3 sorries.
- Project memory `weakpnt-discharge-map.md` — refreshed to RESOLVED.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this clone IS the chosen thread. The hard
crux (weakPNT) is DONE; absorb the context, then start at Next action #1: poll Aristotle `c6d615ee`.**
