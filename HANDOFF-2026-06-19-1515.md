# Handoff: lap 12 — NoThreeInLine HJSW crux A.1 DONE; `3N/2` covering paper-blocked

**Date**: 2026-06-19 15:15 UTC · **Branch**: no-three-in-line · **HEAD**: `d5076da`
**Build**: 🟢 green (8297 jobs, observed this session) · all new theorems axiom-clean
(`[propext, Classical.choice, Quot.sound]`) except the one documented `native_decide` witness.

## 🎯 What we're doing
Driving the `no-three-in-line` branch. Its stated headline frontier (PLAN.md item A) is the
**Hall–Jackson–Sudbery–Wild `3N/2`** lower bound (1975, the best *proven* constant, unimproved,
never formalized). This lap attacked it hardest-first: the algebraic crux is now DONE; the
remaining `3/2` content is the covering combinatorics, which is genuinely paper-blocked.

## 🧠 Context to carry forward
- **The `3/2` is ENTIRELY in the covering, NOT the arc.** A single hyperbola arc `xy≡k mod p`
  gives `p−1` points; the doubled arc `x∈[1,2p)\{p}` gives `2(p−1)` — but both are only `~N` in a
  *square* grid (ratio 1, same as the Erdős parabola already formalized). I verified by attempting
  to reconstruct the construction: naive stacking of two `y`-strips into a `2p×2p` square would
  give ratio `2` (too good) — so cross-arc collinearity must obstruct it and cap the gain at `3/2`.
  **The cross-arc non-collinearity lemma + the exact translate choices are the genuine unknown** I
  could not reconstruct from memory. Do NOT fabricate a construction; a wrong one gives collinear
  triples and a false claim. `ON-LINE-REQUEST.md` is filed asking for the 1975 paper's exact point
  set, grid side `N` vs `p`, and the cross-arc argument.
- **Everything the covering will need is already built & green:** `card`, `grid`, and
  `noThreeCollinear` for the arcs; the `det3 ↔ collinear` iff; the decidable `idet3` criterion. The
  covering lap just assembles a `Finset` and feeds the same `le_csSup (bddAbove_grid N) ⟨…⟩`
  plumbing the parabola uses (`Statement.lean:52`).
- **Goodstein limit-α B4 is intricate, not a quick win** (I analyzed it): the naive recursion
  `H_{ω^α}(n)+1 = f_{α[n]}(n+1)` does NOT close — the `ω[n]=n+1` shift produces a `β[n]` vs `β[n+1]`
  index mismatch, and `α[n]` can itself be a limit (`(ω²)[n]=ω·(n+1)`), so peeling doesn't terminate
  at a successor cleanly. `iterate_offset` needs a *fixed* target function, which limit exponents
  don't give. Marginal anyway — the project headline (`goodsteinLength grows like f_{ε₀}`) is done.
- **Hardy.lean edits re-run ~4–5 min of `native_decide`** — keep B4 work in a NEW file importing it.

## ✅ State (this lap, 6 commits `cc3fb91`→`988e04b`, all green)
- `Hyperbola.lean` (NEW) — `hyperbola_noThreeCollinear` (single arc), `hyperbolaWide_noThreeCollinear`
  (doubled arc, `2(p−1)` pts), the reusable crux `hyperbola_xy_eq` (`x·y=k` collapses the mod-p
  determinant to `−k(a−b)(a−c)(b−c)≡0`), plus `hyperbolaWide_card`/`_grid` and residue helpers.
- `Collinearity.lean` — added the missing converse `det3_zero_imp_collinear` ⟹
  `collinear_iff_det3_zero` (exact criterion).
- `Anchors.lean` (NEW) — `collinear_diagonal`, `not_collinear_corner` (anti-vacuity), decidable
  `idet3` criterion `noThreeCollinear_of_idet`, `parabola5_noThreeCollinear` (`native_decide`).
- `Statement.lean` — `hjsw_doubled_arc_exists` + `two_mul_pred_le_maxNoThreeInLine`
  (`maxNoThreeInLine(2p) ≥ 2(p−1)`): the verified HJSW construction core wired into an actual bound
  (Θ(N) — the `3/2` is the covering). This IS the `le_csSup (bddAbove_grid)` plumbing the covering
  lap will reuse; just swap in the covering's `Finset`.
- `PLAN.md`/`README.md` updated (A.1 ✅, D ✅, A.2 ⛔ paper); `PENDING_WORK.md` has the full
  inventory + three-paths-each; `ON-LINE-REQUEST.md` filed.

## 🎬 Next actions
1. **Check for the HJSW answer first:** look for `ON-LINE-FINDINGS-*hjsw*` (root = unharvested) and
   for items deleted from `ON-LINE-REQUEST.md`. If present, read it, then assemble the `3N/2` count
   on the already-proven arcs (`hyperbola*_noThreeCollinear` + the `le_csSup (bddAbove_grid …)`
   plumbing) and add the headline `maxNoThreeInLine` bound to `Statement.lean`. `git mv` the findings
   doc into `archive/findings/` once harvested.
2. **If still no answer:** do NOT spin on the covering. Pick another open item from `PENDING_WORK.md`
   (Goodstein limit-α B4 path 2 = one concrete level in a new file; or strict domination — A3 is now
   unblocked via `fastGrowing_bachmann_reach`).

## ⚠️ Gotchas
- `ZMod p` is a field only with `[Fact p.Prime]` — `hyperbola_xy_eq` needs it for inverse cancel
  (`NeZero` alone is insufficient; `field_simp`/`mul_inv_cancel₀` silently fail).
- `rcases hx with rfl | rfl | rfl` on `x = R` ELIMINATES `R` (substitutes it away) — reference `x`
  afterward, not the old variable name (bit me in `det3_zero_imp_collinear`).
- `field_simp` then `ring`/`linear_combination` for the divided-coordinate goals; bare `field_simp`
  leaves a ring residue.

## 📁 Key files
- `src/LeanFormalizations/Combinatorics/NoThreeInLine/Hyperbola.lean` — the HJSW arcs (crux A.1).
- `…/NoThreeInLine/Collinearity.lean` — `collinear_iff_det3_zero` (the audit bridge).
- `…/NoThreeInLine/Anchors.lean` — anti-vacuity + decidable `idet3` criterion.
- `…/NoThreeInLine/PLAN.md`, `ON-LINE-REQUEST.md`, `PENDING_WORK.md` — the roadmap & open asks.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context above, then start at Next action #1: check for an `ON-LINE-FINDINGS-*hjsw*` doc; if it's
there, assemble the `3N/2` bound on the arcs already proven; if not, take another open item from
`PENDING_WORK.md`.**
