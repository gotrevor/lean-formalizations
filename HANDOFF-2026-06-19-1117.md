# Handoff: lap 8 deep-reflection — diagonal-domination crux REDUCED to one sparsity bound

**Date**: 2026-06-19 11:17 UTC · **Branch**: `no-three-in-line` · **HEAD**: `f2acef6`
`src/` **sorry-free, 0 math axioms**, `lake build` 🟢 green (8289 jobs, observed this lap). 10 commits.

## 🎯 What we're doing
Unbounded expedition (`DIRECTION.md`): the mathlib-only growth theory behind Goodstein/Kirby–Paris.
A1–A4, B1–B3, C1–C3 (incl. the Cichoń identity `goodsteinLength m = H_{seqONote m 0}(2)−2`) are all
DONE + axiom-clean. The **one remaining headline** is the **diagonal domination** `f_o(m) ≤
goodsteinLength m + 2` (every fixed `o`), machine-reduced (lap 6) to **sub-fact (ii)**: the Goodstein
descent stays `≥ ω^o` for `≥ m` steps. This was the every-9th deep-reflection lap (altitude, not
mechanics) — full synthesis lives in `PENDING_WORK.md → ## Reflection — 2026-06-19`.

## 🧠 Context to carry forward (the mental model — READ THIS)
**Direction is SOUND (keep going).** All 12 headlines re-verified `#print axioms`-clean (0 math
axioms) and the growth statements re-audited faithful. The reflection's two operative calls:
- **STOP chasing non-diagonal lower-bound refinements** (super-linear → NON-ELEMENTARY is complete +
  bankable but gives `f_n` at argument `~log₂ m`, NOT the diagonal `f_n(m)`). Don't mistake another
  non-diagonal bound for progress on the headline.
- **Next target = the `o=2` diagonal** `f_2(m) ≤ goodsteinLength m + 2` — smallest open instance.

**The crux mental model (budget↔leadExp tradeoff).** `fastGrowing_step_le_goodsteinLength` at step
`j` gives `f_o(j+2) ≤ goodsteinLength m + 2`. For the diagonal you need budget `j+2 ≥ m` (so `j ≈ m`)
AND `seqONote m j ≥ ω^o` at that `j`, i.e. `leadExp_j ≥ o`. The per-step rate bound (`leadExp drops
≤1/step`) only guarantees `leadExp ≥ 2` for `~log₂ m` steps — the gap is the budget `log₂ m → m`. The
TRUTH: leadExp drops are RARE (only at pure-power boundaries), so it stays high for `≫ m` steps.

**What lap 8 built (the per-step leading-exponent characterization is now COMPLETE):**
- `log_bump_pred_of_not_pow` — at a NON-pure-power step (`b^{log_b n} < n`), leadExp is exactly
  preserved (`log_{b+1}(bump b n − 1) = bump b (log_b n)`; the `−1` is absorbed by lower terms).
- `log_bump_pred_of_pow` — at a pure power (`n = b^{log_b n}`, `log_b n ≥ 1`), leadExp drops by
  EXACTLY one (the rare "borrow").
- `leadExp_ge_of_not_pow` — unconditional non-decrease off pure powers (no `≥ base` cap).
- `bump_eq_of_lt` (`bump b n = n` for `n < b`) + `leadExp_small_nonincreasing` — full trajectory:
  leadExp GROWS while `≥ base`, then is NON-INCREASING once `< base`, falling only at pure powers.
- **`ppCount` + `leadExp_ge_sub_ppCount`** — the sharpened telescope `log₂ m ≤ leadExp_k + ppCount m k`
  (deficit bounded by the *number of pure-power steps*, not the step index).

**⟹ THE DIAGONAL CRUX IS NOW REDUCED TO ONE SPARSITY BOUND.** Since `ppCount` is monotone, the
implication is CORRECT: **`ppCount m m ≤ log₂ m − 2` ⟹ `leadExp_k ≥ 2` for all `k ≤ m`** ⟹
`seqONote m (m−2) ≥ ω²` ⟹ `f_2(m) ≤ goodsteinLength m + 2` (general `o`: `ppCount m m ≤ log₂ m − o`).
The sparsity bound is *plausibly true* (pure-power hits `G_i = (i+2)^e` are extremely sparse among the
astronomically-large early terms — likely `O(1)` hits) but proving it rigorously **IS** the deep
steps-between-drops content.

**⚠ Dead ends already mapped (don't retry):** (a) the telescope gives a LOWER bound on `ppCount`, NOT
the upper bound needed — the sparsity bound is a genuinely separate fact. (b) "leadExp stays ≥ base for
m steps" is FALSE — `leadExp ≥ base i = i+2` fails once `i > log₂ m − 2`; the depth lives in the SMALL
regime (leadExp < base). (c) `goodsteinSeq m j ≥ (j+2)²` is NOT a clean invariant — it breaks at pure
powers (where `G` dips just below the square); the per-step lemmas now pin those break points exactly.

## ✅ State (this lap, all axiom-clean, build-verified 8289 jobs)
6 new theorems + 1 def in `Logic/Goodstein/Domination.lean`, each `#print axioms`-clean and
native_decide-anchored: `log_bump_pred_of_not_pow`, `log_bump_pred_of_pow`, `leadExp_ge_of_not_pow`,
`bump_eq_of_lt`, `leadExp_small_nonincreasing`, `ppCount`, `leadExp_ge_sub_ppCount`. Plus the synthesis:
STATUS.md fully refreshed; `## Reflection — 2026-06-19` in PENDING_WORK; 3 stale docstrings fixed
(`hstep_oadd_one_zero`/`hstep_toONote` claimed "disclosed sorry" though both PROVED lap 5); memory
`goodstein-growth-via-per-step-rate-bounds.md` updated.

## 🎬 Next actions
1. **Prove the sparsity bound `ppCount m m ≤ Nat.log 2 m − 2`** (or its general-`o` form) — the genuine
   steps-between-drops content. This closes the `o=2` diagonal `f_2(m) ≤ goodsteinLength m + 2` via the
   chain above. Two routes (in PENDING_WORK): (a) bound the pure-power hit count directly; (b) bypass
   `ppCount` — prove `G_k ≥ (k+2)²` for `k ≤ m` by a value/quadratic-plateau induction, using
   `leadExp_small_nonincreasing` to control the small regime where the difficulty lives.
2. If (1) stalls, decompose to the narrowest open core and **feed Aristotle** a bounded self-contained
   carve (a slot is free — all jobs IDLE this lap; nothing crisp+tractable was feedable since the
   per-step lemmas closed locally and the sparsity bound is the hard monolith).
3. Wire the closed `o=2` result, then generalize to all `o` (the reduction is uniform in `o`).

## ⚠️ Gotchas
- `bump_zero : bump b 0 = 0` is `@[simp]` (Engine.lean). `bump b n` for `n < b` = `n` (`bump_eq_of_lt`).
- `base k` = `k+2`; `simp only [base]` proves `base (k+1) = base k + 1` (don't append omega → "no goals").
- `goodsteinSeq m (k+1) = bump (base k) (goodsteinSeq m k) − 1` holds by `rfl`.
- `Nat.le_log_iff_pow_le` (flipped iff), `Nat.log_eq_of_pow_le_of_lt_pow` for computing/bounding logs.
- ℕ→ONote coercion `↑n = ONote.ofNat n`; prefer explicit `ONote.ofNat n`. `mul_le_mul_left'` deprecated → `gcongr`.

## 📁 Key files
- `src/LeanFormalizations/Logic/Goodstein/Domination.lean` — ALL the growth-bound work (~900 lines);
  the new per-step lemmas are ~lines 345–700, the reduction targets (`fastGrowing_step_le_goodsteinLength`,
  `goodstein_dominates_of_index_le`) ~lines 700–800.
- `PENDING_WORK.md` (`## Reflection — 2026-06-19` + the lap-7/8 frontier), `STATUS.md` (lap-8 entry +
  axiom ledger), `HANDOFF.md` (thin pointer, refreshed) — all current.
- `Logic/Goodstein/{Growth,Engine}.lean` — Cichoń identity, telescope, `toOrdinal`, `bump`, `seqOrd`.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor, don't wait for
instructions, and don't offer other KB threads — this IS the chosen thread (Goodstein growth theory).
Absorb the mental model (esp. the sparsity-bound reduction and the small-regime depth), then start at
Next action #1: prove `ppCount m m ≤ log₂ m − 2` (the genuine steps-between-drops content) to close the
`o=2` diagonal. Do NOT retreat to more non-diagonal refinements — that's the flagged fixation trap.**
