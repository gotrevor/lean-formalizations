# Handoff: Constructible numbers (Wantzel) — all 3 classical impossibilities + nonagon, axiom-clean

**Date**: 2026-06-15 22:57 UTC · **Branch**: main · **HEAD**: `67fd9ae`

## 🎯 What we're doing
The prior bounded runs (Curtis, power-tower) were COMPLETE with a stop sentinel. This
session was relaunched autonomously ("keep driving"), so I took the standing next
frontier named in the last handoff + `PENDING_WORK.md`: **P1 — compass-and-straightedge
impossibility (constructible numbers)**. Built the whole Layer-1 algebraic core of
Wantzel's theorem from scratch (mathlib has no constructible-number theory) and applied
it to the classical problems. New module: `src/LeanFormalizations/Geometry/Constructible/`.

## 🧠 Context to carry forward
- **Definition chosen** (the audit/faithfulness surface): `IsSqrtTower : IntermediateField ℚ ℝ → Prop`
  (inductive: `⊥`, or `step` adjoining real `a` with `a*a ∈ K`, producing `(K⟮a⟯).restrictScalars ℚ`);
  `IsConstructible x := ∃ K, IsSqrtTower K ∧ x ∈ K`. The `restrictScalars ℚ` form was
  deliberate — it makes the degree-power-of-2 proof fall out by **pure defeq** via
  `Module.finrank_mul_finrank ℚ K K⟮a⟯` (no rewrite). Don't "simplify" to a `⊔`-based
  step; that breaks the clean tower law.
- **Engine**: `IsSqrtTower.finrank_eq_pow_two` (tower law + each quadratic step
  `[K(a):K] ≤ 2` because `a` is a root of `X²-a²`) ⟹ `IsConstructible.finrank_adjoin_eq_pow_two`
  ⟹ `not_isConstructible_of_finrank_adjoin_eq_three` (the degree-3 obstruction all three
  hard cases reuse).
- **Degree-3 inputs**: `∛2` via Kummer (`X_pow_sub_C_irreducible_of_prime`, from "2 not a
  cube in ℚ" ← irrationality of ∛2). `2cos20°` / `2cos40°` via triple-angle + a monic
  integer cubic irreducible by the **integral-root-theorem recipe** (see Gotchas / the
  flushed memory) — NOT Eisenstein.
- **Squaring the circle** is shipped *conditional* on `Transcendental ℚ π` as an explicit
  hypothesis (mathlib has only the analytic part of Lindemann, not π-transcendence). This
  keeps it unconditionally axiom-clean. Honest dependency, documented.
- **Abandoned dead end**: tried to generalize the cubic-irreducibility to a single helper
  `Irreducible (X³-3X-C c)` parametrized by `c : ℤ`. The `C c` cast normal form oscillates
  in simp (`C c` ↔ `Int.cast` ↔ `aeval x (C c)`) and `linarith` can't close. Gave up and
  duplicated per concrete constant (Trisection uses `-C 1`, Nonagon uses `+C 1`, where
  `map_one` fires). Don't re-attempt the generalization.
- **Aristotle**: submitted the trisection cubic, then proved it locally faster and
  cancelled the (still-QUEUED) job. Currently nothing genuinely open is in flight →
  Aristotle correctly idle. The Layer-2 geometric recursion below is not yet a bounded
  self-contained leaf, so don't manufacture a job for it.

## ✅ State (all observed this session)
- `lake build` GREEN — **8265 jobs**, repeatedly, including the pre-commit green-gate.
- **All 7 headlines `#print axioms`-clean** = `[propext, Classical.choice, Quot.sound]`
  (verified: `cbrt2_not_constructible`, `cos20_not_constructible`, `cos40_not_constructible`,
  `squaring_the_circle_impossible`, `of_quadratic`, `isAlgebraic`, `finrank_eq_pow_two`).
  No `sorryAx`, no custom axiom, no `native_decide`.
- `src/` sorry-free. 9 commits this lap (`8ce7df6` … `67fd9ae`).
- Also proved this lap (beyond the headline 3): the **regular nonagon** not constructible
  (4th result), constructibles form a **subfield closed under √**
  (`IsConstructible.{add,sub,mul,neg,inv,sqrt}`, `isConstructible_ratCast`), the
  **constructible⟹algebraic** bridge, and **`IsConstructible.of_quadratic`** (quadratic
  formula preserves constructibility — the algebra of a line∩circle step).

## 🎬 Next actions
1. **P1 Layer 2 — geometric faithfulness** (the genuine open frontier; see
   `PENDING_WORK.md` "NEXT on this thread", path 1). The two algebraic cruxes are ALREADY
   DONE (`of_quadratic` for line∩circle/circle∩circle, field closure for line∩line). What
   remains is *pure analytic-geometry bookkeeping*: define `ConstructiblePoint : ℝ×ℝ → Prop`
   inductively from `{(0,0),(1,0)}` closing under line/circle intersections, then prove
   `ConstructiblePoint p → IsConstructible p.1 ∧ IsConstructible p.2` by showing each
   intersection's coords satisfy a quadratic/linear system over already-built coords and
   feeding `of_quadratic`/field ops. No more field theory needed. Start here.
2. Cheap win if you want a warm-up: **regular heptagon** (`2cos(2π/7)` root of monic
   `X³+X²-2X-1`, same recipe as Nonagon.lean) — fiddlier only in deriving the minpoly from
   Chebyshev/`cos(2π/7)` relations. `PENDING_WORK.md` has the pointer.
3. To make squaring-the-circle unconditional you need `Transcendental ℚ π` (full Lindemann)
   — a multi-year wall; advance only by formalizing the next Lindemann prerequisite or
   filing an `ON-LINE-REQUEST`. Not a one-lap target.

## ⚠️ Gotchas
- `K⟮a⟯` macro = `adjoin K (insert a ∅)`; `rw [restrictScalars_adjoin]` won't fire — use
  it as a term-mode `have … := restrictScalars_adjoin ℚ K {a}` (F is the first explicit
  arg). Element-wise `algebraMap_mem` also works. (Flushed to memory buffer.)
- Pre-existing harmless warnings in `Curtis/Lemma2.lean` (unused vars / simp args) — leave
  them, documented in `PENDING_WORK.md`.
- `le_or_lt` is gone in this mathlib (v4.29.1) → use `abs_cases` / `lt_or_ge`.

## 📁 Key files
- `src/.../Geometry/Constructible/SqrtTower.lean` — the engine: `IsSqrtTower`,
  `IsConstructible`, `finrank_eq_pow_two`, field closure, `of_quadratic`. The bulk.
- `src/.../Geometry/Constructible/{CubeRoot,Trisection,Nonagon}.lean` — the degree-3 inputs.
- `src/.../Geometry/Constructible/Statement.lean` — audit surface (all headlines).
- `src/.../Geometry/Constructible/README.md` — "what to audit" + status (Layer 1 done, 2 open).
- `PENDING_WORK.md` — refreshed: P1 L1 ✅, Layer-2 attack paths, heptagon, Lindemann.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or
wait for instructions, and don't offer other projects from the KB — this doc IS the chosen
thread. Absorb the context, then start P1 Layer 2 (Next action #1): define
`ConstructiblePoint` and prove its coordinates are `IsConstructible`, leaning on the
already-proved `of_quadratic` + field closure. Keep `lake build` green and `#print axioms`
clean; commit every green step.**
