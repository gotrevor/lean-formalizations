# Handoff: deep-reflection lap — direction confirmed, `weakPNT` de-risked

**Date**: 2026-06-19 10:40 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `3839adf`

## What this lap was
The every-9th **deep-reflection lap** (strong model, altitude not mechanics). No proof changes; the
deliverable was the synthesis. Build green (8286 jobs) throughout; 5 doc commits.

## The inherited direction (READ `PENDING_WORK.md` top: `## Reflection — 2026-06-19`)
**The no-three-in-line thread is at its natural, valuable endpoint — not "unfinished."**
- Original mandated target (HJSW `3N/2` axiom-clean in `Statement.lean`) is **COMPLETE**.
- The treadmill overshot it: full general-`N` ladder `3/4 → 15/16 → 6/5 → 3/2−o(N)` (HJSW-optimal).
- The flagship `maxNoThreeInLine_ge_three_halves_sub` rests on **one** cited deep axiom `weakPNT` (PNT,
  **🟠**); the audit surface + the two **unconditional** rungs (`15/16`, `6/5`) are fully axiom-clean.
- `src/` = exactly **1 math axiom** (`weakPNT`) + **1 disclosed `sorry`** (`nagura_prime`, non-blocking).

## What I verified (don't redo blindly — but it's all current as of HEAD)
- **Whole axiom ledger re-run from real `#print axioms`** — 100% matches STATUS. All six complete
  threads + NTL audit surface + `6/5`/`15/16` = `[propext, Classical.choice, Quot.sound]`;
  `..._three_halves_sub` = `+ weakPNT`; `..._five_fourths` = `+ sorryAx`.
- **Faithfulness** re-audited AND independently cross-checked: Aristotle `72891d77` (harvested this lap)
  independently formalized the prose HJSW statement → logically-equivalent to `hjsw_lower_bound`, and
  proved `Collinear ℝ ⟺ cross-det=0`. High confidence the audit surface is faithful.

## Key de-risking finding — `weakPNT` is more tractable than it looked
- mathlib v4.29.1 **already has** the historically-hard PNT crux ζ≠0 on `Re=1`
  (`riemannZeta_ne_zero_of_one_le_re`). The only gap is the **Wiener–Ikehara tauberian bridge**.
- **Measured the port**: ALL heavy mathlib analytic prereqs of PNTAnd's `Wiener.lean` are present in our
  pin (Fourier/RiemannLebesgue, Tannery, SumIntegral, BoundedVariation, Convolution, Chebyshev,
  PrimesInAP). So the port surface is PNTAnd's OWN ~5000 lines across a **one-patch** mathlib gap
  (their lean v4.29.0 → ours v4.29.1) — bounded & mostly mechanical, NOT a from-scratch analysis rebuild.

## Next actions (priority order, from the Reflection)
1. **Preferred — wait-and-cite.** Periodically grep the pin for `WienerIkehara` / a `Chebyshev.psi`
   asymptotic; when mathlib lands it, replace `axiom weakPNT` with the citation → flagship axiom-clean
   for free. Lowest effort.
2. **Cheap dep-test — RUN THIS LAP, DEFINITIVELY DEAD on-box. Do NOT retry.** `lake update` with a local
   PNTAnd require: toolchain check PASSED, but lake died fetching PNTAnd's transitive git deps
   (`PrimeCert` → `git exited 128`, no egress); PNTAnd also pins mathlib v4.29.0 ≠ ours v4.29.1. Two
   independent blockers, neither fixable offline. Workspace restored, build green. (Details: PENDING_WORK opt 1.)
3. **Bounded port (multi-lap) — STARTED this lap; the only ACTIVE discharge route besides wait-and-cite.**
   The port SIDESTEPS the dep-hell (Wiener cone uses none of PrimeCert/leancert/checkdecls; copy source, build
   vs our mathlib). **Brick 1 DONE, axiom-clean, green (8287 jobs):**
   `NumberTheory/PrimeNumberTheorem/Asymptotics.lean` (the 5 Asymptotics prerequisites — ported with zero API
   drift). Next bricks (dependency order) + full sizing in `PENDING_WORK` option 3. If you continue: do the
   small self-contained patches next (Log/Basic, Uniformly, Chebyshev patch) before `Wiener.lean` (4118).
   **But `wait-and-cite` remains preferred** — periodically grep the pin for `WienerIkehara`; if mathlib lands
   it, abandon the port and cite. Don't sink many laps into the port while mathlib is on-trajectory.
4. **STOP doing:** squeezing tighter elementary Chebyshev constants for `nagura`/exact-`5/4` (elementary
   ceiling is exactly `6/5`, no slack); re-feeding Aristotle the cold `nagura_prime`; treating `weakPNT`
   as a lap-sized grind. An idle Aristotle is correct here — nothing genuinely open needs it.

## Pointers
- `PENDING_WORK.md` top (`## Reflection — 2026-06-19`) — the durable direction call.
- `STATUS.md` — refreshed ledger + where-it-stands.
- `weakPNT` + the whole `3/2−o(N)` build: end of `src/.../NoThreeInLine/PrimeGap.lean`.
