# Handoff: sharp Mertens trilogy + first real bricks toward C₃=−γ

**Date**: 2026-06-19 15:45 UTC · **Branch**: `ntl-hjsw` · **HEAD**: latest on `ntl-hjsw`

> Supersedes `HANDOFF-2026-06-19-1530.md` (same lap; this one adds the `MertensConstant.lean` progress).

## 🎯 What we're doing
Extending the unlocked Chebyshev/PNT layer of the no-three-in-line clone. Flagship `3/2−o(N)` is DONE &
axiom-clean. This lap: (1) upgraded the whole Mertens trilogy from `O(1)` to sharp *convergence* form;
(2) reduced the classical `e^{−γ}` headline to the single deep equation `C₃ = −γ`; (3) started building
the real bricks toward that equation in a new module `MertensConstant.lean`.

## 🧠 Context to carry forward
- **The deep equation is `mertensThirdConst = −Real.eulerMascheroniConstant`** (⇔ `M = γ + ∑'_p
  (log(1−1/p)+1/p)`). `mertens_third_classical (hγ) ⟹ ∏(1−1/p)·log N → e^{−γ}` is already proven modulo
  exactly this `hγ`. Everything else is axiom-clean.
- **Why it's deep, not elementary:** `γ = lim(∑_{k≤n}1/k − log n)` is about *all* integers; `C₃` is about
  *primes*. The bridge is the ζ Euler-product transfer. The γ does NOT fall out of `logζ(s)+log(s−1)→0`
  (that limit is 0) — γ enters only through the Abel/Tauberian transfer connecting the prime Dirichlet
  series `P(s)=∑_p p^{−s}` (s→1⁺) to the partial sums `∑_{p≤x}1/p` (= `mertens_second_tendsto`'s `M`).
- **What I built this lap toward it (`MertensConstant.lean`, all axiom-clean):** real prime zeta
  `primeZeta` + summability; the real specialisation of mathlib's complex Euler product; **term realness**
  (`neg_clog_eq_ofReal`: `−Complex.log(1−p^{−s})` is the coercion of the real `−log(1−p^{−s})`, because
  `1−p^{−s}∈(0,1)`); the real log-sum summability (bounded by `2p^{−s}` using the repo's
  `log_one_sub_add_self_abs_le`); and **`riemannZeta_eq_ofReal_exp`**: `ζ(s) = ↑(exp(∑'_p −log(1−p^{−s})))`
  — i.e. ζ is manifestly real-positive on `(1,∞)`, the **real Euler product**.
- **I deliberately stopped before the Abel/Tauberian step (iv below).** It's the genuinely hard part and
  needs fresh context — a wrong brick fails the correctness bar. The bricks above are the safe, correct,
  reusable scaffolding.

## ✅ State (all observed this lap)
- `lake build` **GREEN, 8297 jobs**; every commit passed the pre-commit green-gate.
- `Mertens.lean` (sharp trilogy, axiom-clean): `mertens_second_tendsto` (→M), `mertens_third_tendsto`
  (→C₃), `mertens_third_tendsto_exp` (∏·logN→e^{C₃}), `mertens_third_isEquivalent` (∏ ~ e^{C₃}/logN),
  `mertens_third_classical` (e^{−γ} modulo `hγ`). γ imported from `Harmonic.EulerMascheroni`.
- `MertensConstant.lean` (new, axiom-clean): `primeZeta`, `summable_primeZeta_term`, `primeZeta_nonneg`,
  `riemannZeta_eulerProduct_ofReal`, `prime_rpow_lt_one`, `one_sub_prime_rpow_pos`, `neg_clog_eq_ofReal`,
  `summable_real_eulerLog`, `clog_tsum_eq_ofReal`, `riemannZeta_eq_ofReal_exp`. Added to the root
  `LeanFormalizations.lean` import list.
- ~13 commits this lap. Other open `sorry`s unchanged & off every headline: `nagura_prime`,
  `Wiener.prelim_decay_2/3` (dead code).

## 🎬 Next actions
1. **Continue the `C₃=−γ` bricks in `MertensConstant.lean`** (route + exact next bricks in
   `PENDING_WORK.md` §"the ONE deep equation"). In order:
   (i) define `ζ_ℝ s := (riemannZeta (s:ℂ)).re`, prove `0 < ζ_ℝ s` and `Real.log (ζ_ℝ s) =
   ∑'_p −Real.log(1−p^{−s})` (take `Real.log` of `riemannZeta_eq_ofReal_exp`).
   (ii) expand `−log(1−x) = ∑_{k≥1} x^k/k` (look for mathlib `Real.hasSum_log_one_sub` / the `log` Taylor
   series; else prove via `hasSum_geometric` integrated) and split `log ζ_ℝ(s) = primeZeta s + G(s)`.
   (iii) `s→1⁺` limit via `tendsto_riemannZeta_sub_one_div`.
   (iv) the Abel/Tauberian transfer (hard) — match against `mertens_second_tendsto`.
2. If the ℂ tower stalls, lower-hanging alternative is explicit two-sided Chebyshev `ψ/θ` bounds (PENDING).
   Do NOT retreat to `nagura_prime`/`prelim_decay` (lower value, off-headline).
3. Aristotle `0fa80268` (`summable_primeCorr`) is SUPERSEDED (proved locally) — note & ignore when it
   returns. Only feed Aristotle a genuinely-OPEN *bounded* lemma; the deep ζ tower isn't cleanly bounded,
   so an idle Aristotle is correct.

## ⚠️ Gotchas
- `Summable.of_nonneg_of_le {f g} (hg : 0≤g) (hgf : g≤f) (hf : Summable f) : Summable g` — `g` is the
  SMALL (goal) function, `f` the dominating one; set `(f := …)`, not `(g := …)`.
- cpow↔rpow: `Complex.ofReal_cpow (0≤x) : ↑(x^y) = (↑x)^(↑y)`; `Complex.ofReal_log (0≤x) : ↑(log x) =
  Complex.log ↑x`. Coerce a real Euler factor in via `rw [Complex.ofReal_cpow …]; push_cast; ring_nf`.
- `isEquivalent_iff_tendsto_one` leaves pointwise function division `(f/g) N` — `simp only [Pi.div_apply]`
  before any `rw` on the quotient. (flushed to staging memory.)
- `open MeasureTheory intervalIntegral` makes `integral_const_mul`/`integral_mono_on` ambiguous — qualify.

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/Mertens.lean` — sharp trilogy.
- `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/MertensConstant.lean` — the C₃=−γ bricks (resume here).
- `PENDING_WORK.md` — §"the ONE deep equation C₃=−γ" has the bricks done + next bricks (i)–(iv).
- `STATUS.md` — ledger. `memory/weakpnt-discharge-map.md` — running map.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this clone IS the chosen thread. Absorb the
context, then resume in `MertensConstant.lean` at Next action #1(i): define `ζ_ℝ`, prove it positive, and
derive `Real.log (ζ_ℝ s) = ∑'_p −log(1−p^{−s})` from `riemannZeta_eq_ofReal_exp`.**
