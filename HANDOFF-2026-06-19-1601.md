# Handoff: Limit B spine built — `e^{−γ}` reduced to ONE Tauberian (final-value) limit

**Date**: 2026-06-19 16:01 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `b369cf1`

> Supersedes `HANDOFF-2026-06-19-1511.md`. Same thread (classical `e^{−γ}` Mertens 3rd). Huge lap:
> brick B1 + the entire analytic spine of Limit B landed (10 axiom-clean lemmas), reducing Limit B to a
> single deep step — the Abelian final-value theorem.

## 🎯 What we're doing
Discharging `mertens_third_classical_of_tauberian`'s hypothesis **Limit B**:
`primeZeta s + log(s−1) → M − γ` as `s→1⁺` (`M = meisselMertensM`, `γ = Real.eulerMascheroniConstant`).
Proving Limit B ⟹ `mertensThirdConst = −γ` ⟹ `∏_{p≤N}(1−1/p)·log N → e^{−γ}` (all already wired).

## ✅ State (all observed this lap; `lake build` GREEN 8297 jobs after every commit)
All in `MertensConstant.lean`, all `#print axioms = [propext, Classical.choice, Quot.sound]`:
- **B1 DONE** `primeZeta_eq_abel_integral`: `P(s) = (s−1)∫_1^∞ (∑_{p≤⌊t⌋}1/p)·t^{−s} dt`. All 6 Abel
  hypotheses proven; the last, `hg_int = integrableAtFilter_rpow_neg_mul_log`, **proven locally**
  (superseding Aristotle `2919e0d2`).
- **exp form of B1** `primeZeta_eq_abel_integral_exp`: `P(s) = (s−1)∫_0^∞ A(eˣ)·e^{−(s−1)x} dx`
  (`A=primeRecipSum⌊·⌋`), via the measurable Jacobian change of variables (`integral_image_eq_integral_abs_deriv_smul`,
  exp injective, `exp''Ioi 0 = Ioi 1`). **This is the working form for the assembly.**
- **γ-injection** `integral_log_mul_exp_neg_Ioi_eq_neg_gamma`: `∫_0^∞ log u·e^{−u} du = −γ` (complex
  `hasDerivAt_GammaIntegral` + `Real.hasDerivAt_Gamma_one` + ofReal bridge). The spot where `−γ` enters.
- **log-part FULLY evaluated** `sub_one_mul_integral_log_exp`: `(s−1)∫_0^∞ log x·e^{−(s−1)x} = −γ − log(s−1)`
  (change of var `u=(s−1)x` + the γ-injection + `integrableOn_log_mul_exp_neg`). **The full `−γ` contribution.**
- **M-part** `sub_one_mul_integral_exp_neg`: `(s−1)∫_0^∞ e^{−(s−1)x} = 1` (so `M`-term `= M` exactly).
  Also t-form `tendsto_sub_one_mul_integral_rpow` + `sub_one_mul_integral_rpow_neg`.
- **Tauberian prerequisite** `tendsto_primeRecipSum_floor_exp`: `primeRecipSum⌊eˣ⌋ − log log⌊eˣ⌋ → M`.

## 🎬 Next actions — finish Limit B (the SOLE remaining gap to `e^{−γ}`)
With `A(eˣ) = log x + M + r(x)`, the exp form splits as
`P(s) = (s−1)∫ log x·e^{−(s−1)x} + (s−1)∫ M·e^{−(s−1)x} + (s−1)∫ r(x)·e^{−(s−1)x}`
`= (−γ−log(s−1)) + M + error(s)`, so `P(s)+log(s−1) → M−γ` **iff `error(s) → 0`**. Three sub-steps:

1. **`r(x) → 0`** (`x→∞`), `r(x) = primeRecipSum⌊eˣ⌋ − log x − M`. Have
   `tendsto_primeRecipSum_floor_exp` (→ M after subtracting `log log⌊eˣ⌋`). REMAINING: `log log⌊eˣ⌋ − log x → 0`.
   Route: (i) `log⌊eˣ⌋ − x → 0` by squeeze, `log(1−e^{−x}) ≤ log⌊eˣ⌋ − x ≤ 0` (from `eˣ−1<⌊eˣ⌋≤eˣ`:
   `Nat.floor_le`, `Nat.lt_floor_add_one`; `log(1−e^{−x})→0`); (ii) then `log log⌊eˣ⌋ − log x =
   log(log⌊eˣ⌋ / x) → log 1 = 0` since `log⌊eˣ⌋/x = 1 + (log⌊eˣ⌋−x)/x → 1`.
2. **integrable split** — additivity of the 3 pieces needs each integrable on `Ioi 0` for `s>1`.
   `log x·e^{−(s−1)x}` and `M·e^{−(s−1)x}`: like `integrableOn_log_mul_exp_neg` (rescaled). **`r(x)·e^{−(s−1)x}`:
   NOTE `r` is unbounded near `x=0`** (`A(eˣ)=0` for `x<log 2`, so `r=−log x−M→+∞`), but `r·e^{−(s−1)x}` is
   integrable (log singularity) — bound `|r(x)| ≤ |primeRecipSum⌊eˣ⌋| + |log x| + |M|` and dominate.
3. **THE CRUX — Abelian/final-value theorem** (deep, multi-lap, not in mathlib):
   `lim_{δ→0⁺} δ·∫_0^∞ r(x)·e^{−δx} dx = lim_{x→∞} r(x) = 0`. Approach: sub `u=(s−1)x` (use
   `integral_comp_mul_left_Ioi` as in `sub_one_mul_integral_log_exp_eq`) → `δ∫ r e^{−δx} = ∫ r(u/δ)·e^{−u} du`;
   then `r(u/δ) → 0` pointwise as `δ→0⁺` (since `u/δ→∞`) and is dominated by an integrable envelope
   (`r` is bounded on `[x₀,∞)` and the near-0 blow-up is killed by the `u`-tail) → dominated convergence
   (`MeasureTheory.tendsto_integral_filter_of_dominated_convergence`) gives `→ ∫ 0 = 0`. **Attack THIS first
   next lap (hardest-first); 1–2 are bounded scaffolding.**
4. **Assemble** `tendsto_primeZeta_add_logSub_limitB` (= Limit B) and feed `mertens_third_classical_of_tauberian`.

## ⚠️ Gotchas (this lap)
- `A=primeRecipSum⌊·⌋` is a STEP function ⟹ the continuous-`g` substitution `integral_comp_mul_deriv_Ioi`
  does NOT apply; use the measurable `integral_image_eq_integral_abs_deriv_smul` (Jacobian form).
- `integral_const_mul`, `setIntegral_congr_fun`, `integral_sub`, `integrableAtFilter_rpow_atTop_iff`,
  `IsBigO.integrableAtFilter`, `integral_comp_mul_left_Ioi` are the workhorse mathlib lemmas here.
- `intervalIntegrable_log'` is in namespace `intervalIntegral`; `isLittleO_log_rpow_atTop` is ROOT (not `Real.`).
- Aristotle weak on measure theory (`reference/2026-06-05-aristotle-weak-on-measure-theory.md`) — the remaining
  B2 pieces are all measure theory, so an **idle Aristotle is correct**; do not feed it these.

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/MertensConstant.lean` — all this lap's work; the
  Limit-B spine is at the BOTTOM (resume there). Crux-isolation `mertensThirdConst_eq_neg_gamma_of_tauberian`
  mid-file (~line 370).
- `STATUS.md` — refreshed ledger (all headlines trust-base clean; `e^{−γ}` is 🟡 frontier, conditional on
  the *provable* Limit B hypothesis — no 🔴).
- `PENDING_WORK.md` — the C₃=−γ section has the full B0/B1/B2 decomposition with this lap's DONE markers.

---
**→ Next session: resume in `MertensConstant.lean` at Next action #3 — the Abelian final-value theorem
`lim_{δ→0⁺} δ∫_0^∞ r e^{−δx} = 0` (the deep crux). Steps #1–#2 (`r→0`, integrable split) are bounded
scaffolding you can fill as needed. Then assemble Limit B and discharge `e^{−γ}`. Don't summarize this doc
back to Trevor or switch repos — this clone IS the chosen thread.**
