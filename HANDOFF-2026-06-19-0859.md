# Handoff: refined-Chebyshev stack built — general-`N` constant, analytic-half assembly remains

**Date**: 2026-06-19 08:59 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `10c002b`

## 🎯 What we're doing
Lifting the no-three-in-line **general-`N` lower constant** above Bertrand's `3/4`. HJSW's real theorem
is `3N/2 − o(N)` for all `N`; the formalized general-`N` bound is only `3/4`
(`maxNoThreeInLine_ge_three_quarters`). The lever is prime-gap strength: a prime in `(n,c·n]` gives
constant `3/(2c)`. The lone open `sorry` in `src/` is `nagura_prime` (prime in `(n,6n/5]`), wired to the
`5/4` payoff `maxNoThreeInLine_ge_five_fourths`. NO headline depends on it — all headlines axiom-clean.

## 🧠 Context to carry forward (this reorients the whole attack — read it)
- **THE strategic finding (proven 3 ways this lap, don't re-derive):** the *crude* elementary Chebyshev
  bounds **cannot beat `c=2` for ANY `c<2`** — not just `6/5`. The central-binomial split forces a prime
  in `(n,c·n]` only when `L·c > 8U/3 − 2log2` (`U,L`=θ upper/lower consts); crude `U=log4,L=log4/2` ⇒
  `c>3.33`, even true PNT `U=L=1` ⇒ only `c>1.28`. **The ONLY route past `3/4` is a REFINED Chebyshev
  bound `ψ(x)≳0.91x`, which mathlib lacks.** The earlier "tuned Nagura `6/5` inequality" ask was one
  special case of this same wall — superseded.
- **The refined bound IS buildable, and I built its entire prerequisite stack this lap** (8 commits, all
  axiom-clean, kernel-verified, in `PrimeGap.lean`). What's left is ONE focused chunk: the Stirling-floor
  *error-bounding* (everything is `O(log n)`).
- The refined route may land a *different* explicit `c<2` (whatever `A≈0.92` yields, ≈`1.70`, giving
  constant `≈0.88`) rather than exactly `6/5`. That's fine and still beats `3/4`. Retarget
  `maxNoThreeInLine_ge_five_fourths` to the achieved `c` when the analytic half lands. `nagura_prime`'s
  exact `6/5` needs Nagura's *tuned* numerics on top — keep it as the stretch goal, not the blocker.
- **Aristotle `1644a603` (`aris-nagura`)** has been grinding from-scratch `nagura_prime` for >1h — very
  unlikely to crack cold (it needs exactly this infra). Consider redirecting it to the narrowed analytic
  assembly next lap (see Next actions). Don't thrash it pointlessly, but it's low-value as-is.

## ✅ State (all observed this session)
- `lake build` GREEN: **8286 jobs**, full repo, confirmed after every commit. Tree clean at `10c002b`.
- 8 commits (`5d51e9f`→`10c002b`). New axiom-clean lemmas in `PrimeGap.lean`, each `#print axioms` =
  `[propext, Classical.choice, Quot.sound]`:
  1. `sum_vonMangoldt_mul_floor_div` — Chebyshev keystone `∑_{d≤n}Λ(d)⌊n/d⌋ = log(n!)`.
  2. `floor_comb_bounds` — `⌊n⌋−⌊n/2⌋−⌊n/3⌋−⌊n/5⌋+⌊n/30⌋ ∈ {0,1}` (period-30, `decide`).
  3. `logFactorial_div_eq_sum`, `logFactorial_comb_eq`, `logFactorial_comb_le_psi` — the `2,3,5,30`
     `T`-combination `= ∑Λ(d)g(n/d) ≤ ψ(n)` (combinatorial half).
  4. `log_factorial_le` — explicit Stirling **upper** bound on `log(m!)` (mathlib has only lower).
  5. `log_three_gt` (`>1.09`), `log_five_gt` (`>1.6`), `chebyshev_const_gt` (`A>0.91`).
  6. `logFactorial_leading_identity` — `T`-combination main terms `= A·x` exactly (the cancellation).
- Only `nagura_prime` carries `sorry` (line ~447). Headlines untouched, all axiom-clean.

## 🆕 ADDENDUM (later same session, HEAD `…`): per-term bounds DONE — final assembly is mechanical
Built 5 more axiom-clean lemmas (commits after `10c002b`), giving **every per-term bound** for `f(n)`:
- `mul_log_le_mul_log` (`m·log m ≤ x·log x`, `1≤m≤x`) + `mul_log_sub_le` (secant: `b log b − a log a ≤
  (b−a)(log b+1)`) — the floored↔continuous bridges.
- `log_factorial_div_le` (`log(⌊n/k⌋!) ≤ (n/k)log(n/k) − ⌊n/k⌋ + log(2n)/2 + 1 − log2/2`) — the `k∈{2,3,5}`
  **upper** terms.
- `log_factorial_div_ge` (`(n/k)log(n/k) − n/k − log(n/k) − 1 ≤ log(⌊n/k⌋!)`) — the `+⌊n/30⌋` **lower** term.
- (for the `log(n!)` term use `Stirling.le_log_factorial_stirling` directly, k=1, no floor.)
So the **only** thing left is the final `linarith` assembly: feed the 5 per-term bounds +
`logFactorial_leading_identity` (collapses the `(n/k)log(n/k)` combo to `A·n`) + the floor-slop bound
(`−n + ⌊n/2⌋+⌊n/3⌋+⌊n/5⌋ − n/30 ≥ −3`, since the linear-in-`n` part is `−n(1−½−⅓−⅕+1/30)=0`) →
`f(n) ≥ A·n + (O(log n) error)`. Then bound the error terms (`log(2n) ≤ log n + log2`, `log(n/30) ≤ log n`,
etc.) to get `f(n) ≥ A·n − C·log n − D`, and chain with `logFactorial_comb_le_psi` for `ψ(n) ≥ A·n − C log n`.
This is now pure bookkeeping — no new math. **Start here next lap.**

## 🟢 ADDENDUM 2 (later same session): REFINED ψ BOUND PROVEN — `psi_refined_lower` is axiom-clean
The analytic half is DONE and chained. New axiom-clean lemmas (commits through the `psi_refined_lower`
commit):
- `logFactorial_comb_lower` (`n≥30`): `A·n + (O(log n) error) ≤ f(n)` — the full analytic-half assembly
  (single `linarith` over the 5 per-term bounds + leading identity + floor slop; `norm_num at … ⊢`
  first to normalize `↑2` vs `2` cast atoms — that was the one trick needed).
- **`psi_refined_lower` (`n≥30`):** `A·n + Real.log n/2 + log(2π)/2 − 3log(2n)/2 + 3log2/2 −
  log(n/30) − 7 ≤ ψ(n)`, `A=(7/15)log2+(3/10)log3+(1/6)log5 > 0.91`. **THE refined Chebyshev lower bound
  mathlib lacks** — strictly beats the elementary `log4/2≈0.69`. `le_trans logFactorial_comb_lower
  logFactorial_comb_le_psi`. THIS WAS THE WHOLE POINT — the crux infra is now built and proven.

## 🎬 Next actions (downstream from `psi_refined_lower` — the hard part is DONE)
1. **Dual UPPER bound `ψ(n) ≲ (6/5)A·n`.** Same `T`-machinery: `ψ(n) − ψ(n/6) ≤ f(n)` (lower half of
   the `g∈{0,1}` split — `g(n/d)=1` forces `n/6 < d ≤ n`, i.e. `ψ(n)−ψ(n/6)`) and `f(n) ≤` Stirling-UPPER
   of the combination (`log_factorial_le` on `+` terms, `le_log_factorial_stirling` on `−` terms — mirror
   of `logFactorial_comb_lower`). Then iterate `ψ(n) ≤ A·n + ψ(n/6) ≤ … = (6/5)A·n + O(log n)`.
2. **Two-sided `θ` (ratio < 2).** `|ψ−θ| ≤ 2√x log x` (`abs_psi_sub_theta_le_sqrt_mul_log`, already used
   in `theta_lower`) ⇒ `θ` lower `≳ A·n ≈ 0.91n`, upper `≲ 1.106n`.
3. **Central-binomial split** with `L=A≈0.91`, `U=(6/5)A≈1.106` ⇒ prime in `(n,c·n]`, `c≈1.70<2`; then
   `3/(2c)>3/4` via `maxNoThreeInLine_ge_of_two_mul_prime_le`. Retarget `maxNoThreeInLine_ge_five_fourths`
   to the achieved `c`, discharging the `nagura_prime` `sorry` dependency for a real `>3/4` constant.
2. **Then the dual UPPER iterate** `ψ(n) ≤ A·n/(1−1/6) + … ≈ 1.106 n` (same `T`-machinery: `ψ(n)−ψ(n/6) ≤
   f(n) ≤` Stirling-upper of the combination; iterate). Gives a two-sided `θ` with ratio `<2`.
3. **Feed the central-binomial split** with `L=A≈0.91`, `U=(6/5)A≈1.106` ⇒ prime in `(n,c·n]`, `c≈1.70`;
   then `3/(2c)>3/4` via `maxNoThreeInLine_ge_of_two_mul_prime_le`. Retarget/rename
   `maxNoThreeInLine_ge_five_fourths` to the achieved constant.
4. **Aristotle:** `aristotle list` (one-shot). If `1644a603` is IDLE, harvest (download, kernel-verify,
   `#print axioms`, port). Otherwise consider `aristotle stop 1644a603` and submit the action-1 assembly
   instead (self-contained, far more tractable than from-scratch Nagura) — inline the 8 lemmas' *results*
   as `axiom`s + goal `ψ(n) ≥ A·n − C log n`, concrete `N₀`/`C` to help it.

## ⚠️ Gotchas
- `import Mathlib.Analysis.SpecialFunctions.Stirling` AND `…Complex.ExponentialBounds` are **not**
  transitively pulled by `NumberTheory.Chebyshev` — you'll hit "Unknown identifier" without them even
  though `#check` works under bare `import Mathlib`. Both are already added to `PrimeGap.lean`.
- `(n ! : ℕ)` type-ascription trips the parser inside casts — write `(Nat.factorial n : ℝ)`.
- `open ArithmeticFunction` makes `id` ambiguous → use `open scoped ArithmeticFunction` for the `Λ`
  notation (already done per-lemma).

## 📁 Key files
- `src/.../NoThreeInLine/PrimeGap.lean` — the entire frontier; new refined-Chebyshev stack sits between
  `theta_lower` and `nagura_prime`.
- `PENDING_WORK.md` (top section) — full strategic finding + precise remaining recipe (mirrors this).
- `STATUS.md` — axiom ledger (new refined-stack row) + chronological log.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context above, then start at Next action #1 (build the analytic half `ψ(n) ≥ A·n − C log n`, combining
`logFactorial_comb_le_psi` + the two Stirling bounds + `logFactorial_leading_identity`).**
