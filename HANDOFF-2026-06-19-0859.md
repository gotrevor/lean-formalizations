# Handoff: C3 identity + bridge + cofinality + length-bound DONE; headline = super-exp term bound

**Date**: 2026-06-19 08:59 UTC · **Branch**: `no-three-in-line` · **HEAD**: `4d62f72`
`src/` **sorry-free, 0 math axioms**, `lake build` 🟢 green (8289 jobs, all observed this lap).

## 🎯 What we're doing
Active expedition (`DIRECTION.md`): build the mathlib-only growth theory behind Goodstein
independence — "`goodsteinLength` grows like `f_{ε₀}`". This lap closed the long-standing
borrowing crux and built most of the domination corollary. The remaining target is the headline
"**`goodsteinLength` eventually dominates every `fastGrowing o`**".

## ✅ State (this lap — 11 commits, all axiom-clean, all build-verified)
1. **C3 borrowing crux PROVED** — discharged `hstep_oadd_one_zero` (the borrowing predecessor of
   `ω^E`, heart of Cichoń). `goodsteinLength_eq_hardy : goodsteinLength m = H_{seqONote m 0}(2)−2`
   is now machine-checked. In `Logic/Goodstein/Growth.lean`: the `Good`/`Canon` frontier invariant,
   `canon_repr`/`canon_round_trip`, `Canon_pred`, `Good_fundSeq`, `hstep_pred_pow`.
2. **Hardy↔fastGrowing bridge** `fastGrowing_le_hardy_pow : f_α ≤ H_{ω^α}` (matching args), in new
   `Logic/Goodstein/Domination.lean`: `hardy_split`, `hardy_oadd_iter` (iteration law),
   `hardy_finite`, `iterate_le_iterate`, `succ_iterate`.
3. **Cofinality** `toOrdinal_two_cofinal` (Goodstein ordinals cofinal in ε₀).
4. **Linear length bound** `le_goodsteinLength : m ≤ goodsteinLength m` (via `le_bump` +
   `goodsteinSeq_ge_sub`) — a citable growth lower bound.
Two Aristotle jobs (`77c99f0e`, `26c3a445`) proved locally + cancelled.

## 🧠 Context to carry forward (the mental model for the headline)
`goodsteinLength m = H_{toONote 2 m}(2) − 2` — a Hardy value with **large index, fixed arg 2**.
- **Telescope enabler (proved):** `goodsteinLength m + 2 = H_{seqONote m j}(j+2)` for ALL
  `j ≤ goodsteinLength m`. So you can evaluate at a HIGH-budget step `j+2` where comparisons unlock.
- **The norm obstruction (key, do not retry the naive way):** `hardy_le_of_lt` needs `norm α ≤ x`,
  and Hardy index-monotonicity GENUINELY FAILS at fixed small arg — `H_ω(2)=5 < H_5(2)=7` though
  `ω>5`. So you cannot dominate `H_{toONote 2 m}(2)` by comparing a big-coeff notation at arg 2.
- **Assembly target:** pick `j` with budget `j+2 ≥ max(m, norm(oadd o 1 0))`, then
  `H_{seqONote m j}(j+2) ≥ H_{ω^o}(j+2) ≥ f_o(j+2) ≥ f_o(m)` via `hardy_le_of_lt` (now norm-valid)
  + bridge + `fastGrowing_monotone`. Needs `oadd o 1 0 ≤ seqONote m j` (index lower bound).
- **Why (i) the linear bound is INSUFFICIENT (checked this lap):** at `j=m-2` (budget `m`),
  `goodsteinSeq m (m-2) ≥ m-(m-2)=2` only → `seqONote m (m-2)` reads as `finite 2` (repr 2), far
  below `ω^o`. Sweet-spot tension: small `j` ⇒ huge index, small budget; large `j` ⇒ big budget,
  tiny index. So **sub-fact (ii) needs a super-exponential Goodstein-term lower bound** at a
  budget-`≥m` step — essentially the growth content itself. That is the genuine remaining crux.

## 🎬 Next actions
1. **Attack sub-fact (ii): a strong Goodstein-term / ordinal-stays-high lower bound.** Concretely,
   show that `seqOrd m j ≥ ω^(repr o)` (i.e. `oadd o 1 0 ≤ seqONote m j`) at some `j` with
   `j+2 ≥ m` and `j ≤ goodsteinLength m`. Likely route: track the LEADING CNF term of the Goodstein
   term across bump steps (the bump grows the leading place value `b^L → (b+1)^(bump L)`); prove the
   leading exponent of `seqOrd m j` stays `≥ repr o` for `j` up to ~`m`. This is the deep Cichoń
   lower bound — multi-lap; decompose, leave a checkpoint `sorry` if needed.
2. Then assemble the headline (the chain in "Context" above is fully worked out modulo (ii)).
3. Alt route: relate `goodsteinLength` to `fastGrowingε₀` and finish via A4
   (`fastGrowing_lt_fastGrowingε₀`, already proved). Same depth.
4. Aristotle is idle (correct — no crisp self-contained open lemma right now). If you carve (ii)
   into a self-contained statement, feed it.

## ⚠️ Gotchas
- `hardy_le_of_lt` requires `norm α ≤ x` — NOT free index monotonicity (fails at small arg).
- `rw`-ing function equations (`hardy_succ`/`hardy_limit`/`fastGrowing_*`) leaves beta-redexes:
  use `simp only [...]` (beta-reduces) then `rw`, or `show` to restate.
- ℕ+ coeff defeq (`k.succPNat ≡ ⟨k+1,_⟩`, `b+1-1 ≡ b`): `rw` auto-`rfl` may not fire — `show`/`rfl`.
- `repr.eq_2` is not a usable identifier; use plain `simp` to unfold `(oadd e n r).repr`.
  `repr 0`/`repr_zero` may not match `ONote.zero.repr` from `induction` — use `have : (ONote.zero:
  ONote).repr = 0 := rfl`.
- `Ordinal.induction` deprecated → `WellFoundedLT.induction`. `e.repr + 1` (Ordinal):
  `rw [← Order.succ_eq_add_one]` then `Order.lt_succ`/`Order.succ_le_of_lt`.
- Aristotle CLI: `aristotle submit "<prompt text>" --project-dir <dir>` — the FILE goes in
  `--project-dir`; the positional arg is the prompt TEXT, not a path.

## 📁 Key files
- `src/LeanFormalizations/Logic/Goodstein/Domination.lean` — bridge, iteration law, cofinality,
  `le_bump`/`le_goodsteinLength` (this lap's new file).
- `src/LeanFormalizations/Logic/Goodstein/Growth.lean` — C3 (sorry-free): `Good`/`Canon` +
  `hstep_pred_pow` + `goodsteinLength_eq_hardy` + the telescope (`hardy_seqONote_telescope`).
- `src/LeanFormalizations/Logic/FastGrowing/{Hardy,Domination,Basic}.lean` — `hardy`,
  `hardy_le_of_lt` (NOTE the `norm` budget), A4 `fastGrowing_lt_fastGrowingε₀`, `fastGrowing_monotone`.
- `PENDING_WORK.md` (NEXT CRUX, sub-fact (ii)) · `STATUS.md` · `DIRECTION.md`.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or offer
other KB threads — this IS the chosen thread (Goodstein growth theory). The C3 identity, the
Hardy↔fastGrowing bridge, cofinality, and the linear length bound are DONE and axiom-clean. Absorb
the "Context" mental model (esp. the norm obstruction + the sweet-spot tension), then start at Next
action #1: the super-exponential Goodstein-term lower bound (sub-fact ii).**
