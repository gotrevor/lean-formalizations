# Handoff: Power-tower LOWER half — COMPLETE, crux fully discharged, axiom-clean

**Date**: 2026-06-14 21:37 UTC · **Branch**: main · **HEAD**: `2181f40`

## 🎯 What we're doing
Bounded run (operator directive in `DIRECTION.md`): prove the infinite power tower's
convergence on the **lower** half of the Euler interval, `e^(-e) ≤ x < 1` (the
oscillating regime), and stitch it with the already-done upper half into the
headline `tower_converges_of_mem` over the full `[e^(-e), e^(1/e)]`. **This run is
DONE** — stop sentinel written. This doc records the finished state.

## 🧠 Context to carry forward
- **The DIRECTION's proof plan for the crux is WRONG.** It said: prove "no 2-cycle
  for x≥e^-e" by subtracting two tangent-line inequalities `x^t ≥ y+(log y)(t−y)` at
  the fixed point `y`. That step is mathematically invalid (you can't subtract
  inequalities), and numerically the tangent-at-`y` bound only constrains `log y` to
  an interval *straddling* `−1`, never below it. I verified this and abandoned it.
- **The correct mechanism** (what I built): `g = f∘f` (`f t = x^t`) has
  `g'(t) = (log x)²·x^(x^t)·x^t = c²·e^{c(e^{ct}+t)}` (`c=log x<0`), bounded by
  `|c|/e ≤ 1` for `x ≥ e^-e`. The bound is `exp_mul_add_ge`: `e^{ct}+t ≥
  (1+log(-c))/(-c)` straight from `Real.add_one_le_exp` — the *same* "max of t·e^{-t}"
  trick as the upper half; the `t`-terms cancel because `c+(-c)=0`.
  - `x > e^-e`: `|g'|<1` uniformly ⟹ Banach contraction ⟹ unique fixed point ⟹ β=γ.
  - `x = e^-e` (K=1, not a contraction): `g'<1` off the single point `1/e`; the
    antitone `h=g−id` is `0` on all of `(β,γ)`, forcing `g'=1` there ⟹ contradiction.
- **Convergence structure (no fixed-point `y` needed):** even subseq `a(2n)` ↓ γ,
  odd `a(2n+1)` ↑ β (both monotone-bounded), limits satisfy `x^β=γ`, `x^γ=β`;
  crux gives β=γ=L; whole sequence → L via `tendsto_of_even_odd`; `x^L=L` falls out.
- **Decision: STRETCH `iff` OMITTED** (note in `PENDING_WORK.md`, NO sorry). It needs
  non-convergence for `0<x<e^-e`, i.e. proving a *genuine attracting* 2-cycle exists
  (fixed point repelling + tower from a₀=1 avoids its stable manifold) — separate
  multi-lap real-analysis work, beyond the directive's ~2-lap stretch budget.

## ✅ State (all observed, not assumed)
- `lake build` GREEN — 8260 jobs, this session, repeatedly.
- `#print axioms tower_converges_of_mem` = `[propext, Classical.choice, Quot.sound]`
  — **fully axiom-clean** (also verified for `two_cycle_collapse`,
  `tower_converges_lower`, `endpoint_fixed_point_lower`). NO custom axiom, no sorryAx,
  no native_decide.
- src/ sorry-free.
- 4 commits this lap, latest `2181f40`. Stop sentinel written
  (`$LEAN_STOP_SENTINEL` = `~/src/.treadmill/lean-formalizations.stop`).
- Aristotle job `59cb8ee1…` (submitted on the crux early) is moot — crux proved
  locally; cancel returned 404 (terminal). Ignore it.

## 🎬 Next actions
1. **This bounded run is complete.** A fresh session should wait for a NEW operator
   directive. If `DIRECTION.md` is unchanged (still the lower-half scope) and the stop
   sentinel is present, there is nothing to do — do not re-open the lower half.
2. If/when Trevor wants more on this target: the natural next scope is the **sharp
   `iff`** — prove divergence for `0<x<e^-e` (see `PENDING_WORK.md` for the precise
   missing piece). Otherwise the standing next frontier is **P1 constructible-numbers
   / doubling-the-cube** (`PENDING_WORK.md`).

## ⚠️ Gotchas
- In `∀ t, … x^(x^t) …` Lean defaults `t : ℕ` (npow vs rpow ambiguity) — annotate `∀ t : ℝ`.
- `div_le_iff` → `div_le_iff₀`; `le_or_lt` gone → `lt_or_ge`; `push_neg` → `push Not at h` (v4.29.1).
- `antitone_of_deriv_nonpos` (GLOBAL, no interior plumbing) is much cleaner than the
  `…On` variant for this; pair with `Filter.EventuallyEq.deriv_eq` for "const on an interval ⟹ deriv 0".

## 📁 Key files
- `src/LeanFormalizations/RealAnalysis/PowerTower/EngineLower.lean` — NEW, the whole lower-half engine + crux.
- `src/.../PowerTower/Defs.lean` — `eNegE`, anchor `endpoint_fixed_point_lower`.
- `src/.../PowerTower/Statement.lean` — headline `tower_converges_of_mem` (audit surface).
- `DIRECTION.md` — the run's directive (note: its crux plan is the invalid one above).
- `PENDING_WORK.md` — stretch-omission note + parked P1/P2/P3.

---
**→ Next session: this is your starting point. Don't summarize this doc back to
Trevor or wait for instructions, and don't offer other projects from the KB — this
doc IS the chosen thread. The bounded run is COMPLETE (stop sentinel set); if the
directive is unchanged, confirm nothing is open and hold. Otherwise pick up at
"Next actions" per Trevor's new directive.**
