# Handoff: DirichletBeta Phase 4 — 7 of 8 leaves closed, N2 two-thirds built

**Date**: 2026-09-04 14:12 · **Branch**: `catalan` · **HEAD**: `e886287` (build green, seen)

## 🎯 What we're doing
Close the last `sorry` in `src/LeanFormalizations/NumberTheory/DirichletBeta/`:
`exists_int_combination` (N2) in `LinearForm.lean`. `Statement.lean` is proof-complete; once N2 is
proved, `exists_even_beta_irrational` closes (then run `#print axioms` on it and `box done`).
Read `DIRECTION.md` "Phase 4 — ACTIVE" for the frozen names/constants.

## 🧠 Context to carry forward
- **N3 (crux) and N4 were closed integral-free** (`CompleteMonotone.lean`, `Integral.lean`,
  `Bound.lean`): `Rval s n u = C_n c_u M_u`, `M = B^s` strictly completely monotone, Pascal recursion
  on the binomial sums `N_j`. `Bound.lean` reuses `rForm_eq` + `Nseq_le_Mseq`. Don't reopen.
- **N2 design (decided, partly built):**
  1. `PartialFractions.lean`: `Rep n σ f` = away from poles `f = Σ_{k≤n} Σ_{i<σ} a i k/(t+k)^{i+1}`
     with `d_n^{σ-1-i} a i k ∈ ℤ` (**exponent index is `i+1`, `i ∈ range σ`**). `Rep.mul_simple`
     raises the level by one when multiplying by `c + Σ e_j/(t+j)` with integer `c, e_j`.
  2. `Blocks.lean`: `rep_Rfun (hs : 3 ≤ s) n : Rep n s (Rfun s n)` where
     `Rfun s n t = 2^{6n}(n!)^{s-3}(2t+n)∏_{j<3n}(t-n+(j+1)-½)/∏_{j≤n}(t+j)^s`. Blocks
     `block n b` for `b ∈ {-n,0,n}` and `block0 n = n!/∏(t+j)`; `s = 3` handled by pairing
     `(2t+n)` with the left block (`Simple.linear_mul`, integer constant term allowed mid-product).
  3. **Remaining (LinearForm.lean), the plan I would execute:**
     - Symmetry `Rfun s n (-t-n) = Rfun s n t` for odd `s`, even `n` (reindex `∏_{j<3n}` by
       `prod_range_reflect`; `(2t+n)` flips sign, denominator flips sign since `s(n+1)` odd).
       `NonPole n (-t-n) ↔ NonPole n t`.
     - Symmetrize: `ã i k := (a i k + (-1)^(i+1) a i (n-k))/2`. Then `pfSum ã t = Rfun t` on
       non-poles (via `sum_range_reflect`), `ã i (n-k) = (-1)^(i+1) ã i k`, and
       `2 · d_n^{s-1-i} ã ∈ ℤ` (the lost factor 2 is absorbed by the `2^{i+1}` below). This avoids
       any uniqueness-of-partial-fractions theorem.
     - Bridge: `(Rval s n u : ℚ) = Rfun s n (n + u + ½)`; vanishing `Rfun s n (w + ½ - m) = 0` for
       `w < m + n` (`n = m + m`; the numerator factor with `j = m+n-1-w` is zero).
     - **Partial sums, not tsum** (the `i+1 = 1` series `β(1)` is only conditionally convergent):
       `PS W := Σ_{w<W} (-1)^{1+m+w} Rfun(w+½-m)`; `PS (m+n+U) = Σ_{u<U} (-1)^{n+u+1} Rval` so
       `PS → rForm s n` (`HasSum.tendsto_sum_nat` + `tendsto_add_atTop_iff_nat`). Termwise
       `Σ_{w<W} (-1)^{1+m+w}/(w+k-m+½)^{i+1} = (-1)^{1+k} 2^{i+1} [E_{i+1}(W ± (k-m)) - F_{i,k}]` with
       `E_q(W) := Σ_{ℓ<W} (-1)^ℓ/(2ℓ+1)^q` and `F_{i,k} = E_{i+1}(k-m)` (k ≥ m) or
       `-(-1)^i E_{i+1}(m-k)` (k < m) — all denominators `2ℓ+1 ≤ n-1` divide `d_n`.
       Limits: `E_q → L_q` exists for `q = 1` (`Antitone.tendsto_alternating_series_of_tendsto_zero`),
       `= dirichletBeta q` for `q ≥ 2`. Coefficient of `L_{i+1}` is `Ã i = 2^{i+1} Σ_k (-1)^{1+k} ã i k`,
       zero for even `i` by the symmetry (n even). Then `tendsto_nhds_unique` gives
       `rForm = Σ_i Ã i L_{i+1} + Ã_0`; integers `A i' := d_n^s Ã (2i'-1)`, `A 0 := d_n^s Ã_0`.
       Reindex odd `i ∈ range s` ↔ `i' ∈ Icc 1 ((s-1)/2)` (s odd; parity split by induction on `h`, `s = 2h+1`).
- Zudilin 2019 §2 (`papers/zudilin-2019-arithmetic-catalan-relatives.txt` ~line 250) has a typo:
  the sum starts at `ν = 1 - m`, not `-m - 1` (that's what makes all denominators `≤ n-1`).
- The validation script's `S(i,k)` uses `-m-1`; integrality there came out by cancellation. Use `1-m`.

## ✅ State
- Closed & axiom-clean (all seen via `#print axioms`): `dn_le_exp`, `summable_betaTerm`,
  `beta_two_eq_catalanConst`, `Rval_pos`, `summable_Rval`, `rForm_neg`, `abs_rForm_le_exp`.
- Green infra for N2: `PartialFractions.lean` (55d7e90), `Blocks.lean` (e886287) — `rep_Rfun` proved.
- Only `sorry` in scope: `LinearForm.lean:43` (`exists_int_combination`). Working tree clean except
  untracked `papers/__pycache__/`.

## 🎬 Next actions
1. In `LinearForm.lean` (import `Blocks`), prove `Rfun_symm` and the symmetrized `Rep`-like
   statement (`∃ ã, symmetric ∧ half-integral ∧ Rfun = pfSum ã` on non-poles).
2. Bridge `Rval_eq_Rfun`, vanishing lemma, `PS → rForm`.
3. Termwise partial-sum identity + limits; assemble `exists_int_combination`.
4. `lake build`, `#print axioms exists_even_beta_irrational`, commit, `box done --green "..."`.

## ⚠️ Gotchas
- `Rep` level bookkeeping: promoting `σ → σ+1` needs one more `d_n` (`(σ+1)-1-i = σ-i`), see `Rep.mono`.
- `omega` "failures" on `σ+1-1-i = σ-1-i` were real: that equation is false.
- `Finset.sum_range_succ'`/`prod_range_add` rewrite the first matching sum; use `conv_rhs` when
  both sides have sums.
- `push_neg` deprecated → `push Not at h`. `le_or_lt` missing in this mathlib (use membership facts).

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/DirichletBeta/{PartialFractions,Blocks}.lean` — N2 engine
- `src/LeanFormalizations/NumberTheory/DirichletBeta/{CompleteMonotone,Integral,Bound}.lean` — N3/N4
- `src/LeanFormalizations/NumberTheory/DirichletBeta/LinearForm.lean` — the last sorry
- `DIRECTION.md` "Phase 4 — ACTIVE" — frozen contract

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB - this doc IS the chosen thread. Absorb the
context above, then pick up at "Next actions" #1 (symmetry + symmetrization in `LinearForm.lean`).**
