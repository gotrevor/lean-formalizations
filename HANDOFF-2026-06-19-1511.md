# Handoff: C₃=−γ crux ISOLATED to one Tauberian limit; B1 Abel rep 5/6 assembled

**Date**: 2026-06-19 15:11 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `72c72f7`

> Supersedes `HANDOFF-2026-06-19-1545.md`. Same thread (the classical `e^{−γ}` Mertens constant),
> big progress: the deep equation is now reduced to ONE precise analytic limit, and the Abel route
> to that limit is 5/6 assembled.

## 🎯 What we're doing
Discharging the last gap to the classical Mertens 3rd `∏_{p≤N}(1−1/p)·logN → e^{−γ}`. That headline was
already proven in-repo *modulo* the opaque `mertensThirdConst = −γ` (`mertens_third_classical`). This lap
reduced THAT to a single clean analytic limit and built most of the Abel-summation machinery to attack it.

## 🧠 Context to carry forward
- **The whole architecture rests on ONE insight:** `mertensThirdConst = −γ` is forced by computing the
  limit `L(s) = primeZeta s + log(s−1)` (`s→1⁺`) **two independent ways**:
  - **Limit A** (ζ Euler-product side) → `−mertensCorr 1`. **PROVEN, axiom-clean.**
  - **Limit B** (Abel/Tauberian from `∑_{p≤x}1/p`) → `M − γ`. **The remaining crux.**
  Uniqueness of limits ⟹ `−mertensCorr 1 = M − γ` ⟹ `mertensThirdConst = −γ`. This is already
  formalized as `mertensThirdConst_eq_neg_gamma_of_tauberian (hB) : mertensThirdConst = −γ` and
  `mertens_third_classical_of_tauberian (hB) : ∏(1−1/p)·logN → e^{−γ}`. **So the entire remaining job is
  to prove Limit B and feed it in.** Do NOT re-derive Limit A or the assembly — they're done.
- **Why B1 then B2:** Limit B unfolds via Abel summation of `primeZeta s = ∑_p p^{−s}`:
  - **B1** (`primeZeta s = (s−1)∫_1^∞ (∑_{p≤t}1/p)·t^{−s} dt`) — mechanical, via mathlib
    `tendsto_sum_mul_atTop_nhds_one_sub_integral₀` (`Mathlib/NumberTheory/AbelSummation.lean`). **5 of its
    6 hypotheses are PROVEN this lap.** 6th delegated to Aristotle (running).
  - **B2** (take `s→1⁺` of that integral, using `∑_{p≤t}1/p = log log t + M + o(1)` and `−γ = Γ'(1)`) —
    the genuinely hard analytic wall (dominated convergence + the `u=(s−1)log t` substitution into
    `∫_0^∞ log u·e^{−u} du = Γ'(1) = −γ`). Multi-lap.
- **Aristotle worked well this lap:** `boundary_decay` (job `f0c52c60`) returned, was kernel-verified on
  our v4.29.1 + axiom-clean, ported with trivial cleanup. A new job `2919e0d2` (`integrable_rpow_neg_mul_log`
  = the 6th B1 hypothesis `hg_int`) is **RUNNING** (submitted ~15:05 UTC). Prompts archived in `tools/mk/`.
- B0's partial-sum tendsto is over `Finset.range N`; the Abel theorem uses `∑ k ∈ Finset.Icc 0 n`. These
  differ by `Icc 0 n = range (n+1)` — reconcile with `tendsto_add_atTop_nat 1` (as `primeCorr_tendsto` in
  `Mertens.lean` already does) or restate B0 over Icc. Minor but don't forget it in the B1 assembly.

## ✅ State (all observed this lap)
- `lake build` **GREEN, 8297 jobs** (full build run after every commit; pre-commit green-gate passed).
- ~9 commits, all axiom-clean (`#print axioms` = `[propext, Classical.choice, Quot.sound]` on every new
  headline). HEAD `72c72f7`.
- **`MertensConstant.lean` new this lap:** `mertensCorr` + `log_realZeta_split` (ii-b);
  `tendsto_realZeta_sub_one_div`, `tendsto_sub_one_mul_realZeta`, `tendsto_logRealZeta_add_logSub` (A1),
  `continuousOn_mertensCorr` (A2), `tendsto_primeZeta_add_logSub` (Limit A); `tsum_primeCorrCoeff_eq`
  (bridge); **`mertensThirdConst_eq_neg_gamma_of_tauberian` + `mertens_third_classical_of_tauberian`** (the
  milestone); `primeZetaCoeff`/`primeZetaCoeff_tendsto` (B0); `primeRecipSum_le_one_add_log` (harmonic);
  `boundary_decay` (Aristotle port) + `abel_boundary_tendsto` (B1 `h_lim`); `primeRecipCoeff`,
  `primeRecipCoeff_zero` (`hc`), `sum_Icc_primeRecipCoeff`, `rpow_one_sub_mul_primeRecipCoeff`,
  `hasDerivAt_rpow_one_sub`/`differentiableAt_rpow_one_sub` (`hf_diff`)/`deriv_rpow_one_sub`,
  `locallyIntegrableOn_deriv_rpow_one_sub` (`hf_int`), `abel_hg_dom` (`hg_dom`).
- **B1 hypothesis ledger** for `tendsto_sum_mul_atTop_nhds_one_sub_integral₀`: `hc`✅ `hf_diff`✅ `hf_int`✅
  `h_lim`✅ `hg_dom`✅ — only **`hg_int`** (Aristotle `2919e0d2`) outstanding.

## 🎬 Next actions
1. **`aristotle list` → if `2919e0d2` is IDLE, download + kernel-verify + `#print axioms`** (see
   `reference/aristotle-download-and-verify.md`). Port `integrable_rpow_neg_mul_log` into
   `MertensConstant.lean`. If still RUNNING, poll between other steps; if it failed/timed out, prove
   `hg_int` locally (split `t^{−s}(1+log t) = t^{−s} + t^{−s}log t`; `integrableOn_Ioi_rpow_of_lt` for the
   first; for the second dominate `t^{−s}log t = O(t^{−s'})`, `1<s'<s`, via `log t = o(t^ε)`).
2. **Assemble brick B1**: apply `tendsto_sum_mul_atTop_nhds_one_sub_integral₀` with `c = primeRecipCoeff`,
   `f = fun u => u^(1−s)`, the 6 hypotheses above. Its LHS `∑ k ∈ Icc 0 n, f k * c k = ∑ primeZetaCoeff s k`
   (`rpow_one_sub_mul_primeRecipCoeff`) → `primeZeta s` (`primeZetaCoeff_tendsto`, reconcile Icc/range).
   Uniqueness ⟹ `primeZeta s = (s−1)∫_1^∞ (∑_{p≤⌊t⌋}1/p)·t^{−s} dt`.
3. **Then start B2** (the wall): the `s→1⁺` limit of that integral. Recipe in `PENDING_WORK.md`
   §"the ONE deep equation" → B2. γ enters via `Real.hasDerivAt_Gamma_one` (`Harmonic/GammaDeriv.lean`).
4. Keep one Aristotle job in flight on a genuinely-open *bounded* sub-lemma (B2 has bounded sub-pieces:
   e.g. `(s−1)∫_2^∞ M·t^{−s} dt → M`, or the substitution integral). Do NOT re-feed `boundary_decay`/`hg_int`.

## ⚠️ Gotchas
- Aristotle `submit` long prompt CRASHES (`ENAMETOOLONG`, per-path-component >255B). Use `--project-dir`
  with a `Problem.lean` (`import Mathlib`, target `:= sorry`, guidance in header) + short inline instruction.
- `tendsto_nhds_unique` needs `[NeBot l]`; for `𝓝[>] (1:ℝ)` it's an instance (no manual proof needed).
- `Summable.tsum_add` (NOT bare `tsum_add`) for `∑'(f+g)=∑'f+∑'g`. `Injective.tsum_eq` (with
  `support ⊆ range`) for reindexing ℕ-sums to `Nat.Primes`-sums; `Nat.Primes` is defeq `{p // p.Prime}`.
- `ContinuousOn.congr (f := known) h h'` — `f` is the KNOWN function, result is `ContinuousOn (unknown)`;
  `h' : EqOn unknown f`. Getting the named-arg direction wrong gives a type mismatch.
- Don't trust an Aristotle proof until it builds in OUR kernel (v4.28 vs v4.29.1) + `#print axioms` clean.

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/MertensConstant.lean` — all this lap's work; B1
  hypotheses + the crux-isolation theorem live at the bottom. **Resume here.**
- `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/Mertens.lean` — `meisselMertensM` (M, def via
  improper integral), `mertensThirdConst`, `mertens_second_tendsto`, `mertens_third_classical`.
- `PENDING_WORK.md` §"the ONE deep equation C₃=−γ" — full B0/B1/B2 decomposition + assembly recipe.
- `tools/mk/aristotle-*.txt` / `*-Problem.lean` — archived Aristotle prompts.
- `.lake/packages/mathlib/Mathlib/NumberTheory/AbelSummation.lean` — `tendsto_sum_mul_atTop_nhds_one_sub_integral₀`.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this clone IS the chosen thread. Start at Next
action #1: `aristotle list`, harvest `2919e0d2` (`hg_int`) if ready, then assemble brick B1.**
