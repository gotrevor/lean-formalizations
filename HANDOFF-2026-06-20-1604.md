# Handoff: treadmill STOP — completion re-verified from ground truth, no relaunch

**Date**: 2026-06-20 16:04 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `73c1f4b` (+ this doc) · `LEAN_LAP_ALLOW_STOP=1`

## 🎯 What we're doing
Nothing left to *do* — the repo's headline theorems are complete and kernel-certified. This lap arrived
carrying the **stale** de-vendor / "v4.29.1→v4.31.0 upgrade" kickoff objective, which actually landed two
laps ago (`a0f17d1`). Per finish-and-stop mode, I did not redo done work or open the forbidden off-headline
side quest; I re-verified the completion state from **ground truth by my own hand** and the treadmill issued
a real STOP (it will NOT relaunch after this lap).

## 🧠 Context to carry forward
- **This is genuine finish-and-stop, not a pause.** The completion bar is met on all three axes (sorry-free
  `src/`, every headline `#print axioms`-clean, frontier saturated). The previous ~5 laps were re-verification
  self-stops; this lap the treadmill governor confirmed STOP-and-don't-relaunch. Do **not** treat a future
  relaunch as a signal to reopen work — only an explicit Trevor directive lifting finish-and-stop does that.
- **The kickoff de-vendor objective is DONE** (`a0f17d1`): mathlib `v4.31.0`, PNTAnd is a real lake dep on
  `kim-em/PrimeNumberTheoremAnd` (rev `40e5246b…`), the hand-ported vendored tower (8 files) deleted. Ignore
  any future kickoff that still asks for it — it's stale.
- **Only one repo-connected open item exists** and it is OFF-headline: the sharp `2π|u|` BV-Fourier decay
  constant (`prelim_decay_2/3`, currently only the non-sharp `4|u|` route-(b) is proven & axiom-clean in
  `src/`). It needs a signed-BV / Lebesgue–Stieltjes integration-by-parts development **absent from mathlib**;
  it gates **no headline**. Building that infrastructure to sharpen a bonus is exactly the side quest
  finish-and-stop forbids — left for `PENDING_WORK.md` item 1 (route (c): Jordan decomposition + signed
  Stieltjes measure + IBP) only if finish-and-stop is ever lifted.
- **The dep's `Wiener.lean` carries 2 `prelim_decay` sorries** — dead code off the WeakPNT path. `weakPNT` is
  `#print axioms`-clean *with them present*; the clean footprint is authoritative. Not a defect to chase.
- **`ON-LINE-REQUEST.md` is correctly still open**: its two items are *write-to-external-repo* doc-propagation
  tasks (fold two v4.31 gotchas into `~/src/lean-universe/` + `~/src/mathlib-bump-cookbook/`) that a sandboxed
  session can't perform. They block nothing here; a networked host session fulfills them.

## ✅ State (all observed in real output this lap)
- **`lake build` GREEN — 8621 jobs** (cached replay; only deprecation/unused-simp warnings, no errors).
- **Toolchain** `leanprover/lean4:v4.31.0`; `lakefile.toml` has `mathlib @ v4.31.0` and `PrimeNumberTheoremAnd`
  (kim-em, rev `40e5246b0cf0070875842488ccf60c461e60bc62`) as real `[[require]]`s.
- **`src/` sorry-free**: tight grep (`sorry`/`admit` outside comments, in tactic position) returns NONE; every
  hit is docstring prose. Zero `axiom` declarations.
- **All 14 `.bump-axioms` headlines `#print axioms` = `[propext, Classical.choice, Quot.sound]`** — ran
  `lake env lean /tmp/axcheck.lean` (generated from `.bump-axioms`) this lap; zero `sorryAx`, zero math axioms.
  The 14: Curtis `no_polynomial_relation`; PowerTower `tower_converges_iff_full`; Constructible
  `isConstructible_iff_constructiblePoint` / `cbrt2_not_constructible` / `squaring_the_circle_impossible_uncond`;
  Transcendence `transcendental_pi_axiomClean` / `e_transcendental`; Goodstein `goodstein_terminates`;
  NoThreeInLine `hjsw_lower_bound` / `maxNoThreeInLine_ge_three_halves_sub` / `weakPNT`; Mertens
  `mertens_third_classical_eGamma`; BVFourierDecay `prelim_decay_2_route_b` / `prelim_decay_3_route_b`.
- **Working tree clean** before this doc (the build changed nothing — pure replay).

## 🎬 Next actions
1. **Stay stopped.** Do not reopen the stale de-vendor objective, do not manufacture side quests, do not run
   yet another full re-audit (the footprint is stable across ~6 laps now). This thread is closed.
2. Only if Trevor **explicitly lifts finish-and-stop**: the one real multi-lap target is the sharp `2π|u|`
   BV-Fourier constant via `PENDING_WORK.md` item 1 / `STATUS.md` Long-term (route (c)).

## ⚠️ Gotchas
- **v4.31 first-from-source full builds hit macOS-side FD exhaustion** (3-file olean split × parallel heavy
  leaves on the orbstack bind mount; symptom `Too many open files` on a rotating leaf set, jobs ~8590–8620).
  Spurious — each file builds fine alone. No `-j` flag in this lake; fix is to build modules sequentially once
  to cache, then full `lake build` is a clean replay. Already cached — only bites after a `lake clean`.
- A bare `HANDOFF.md` here is a **thin master pointer**, refreshed deliberately, not per-lap; dated
  `HANDOFF-*.md` are the per-lap batons. This doc is a dated baton — `HANDOFF.md` already reflects the
  complete state and needs no edit.

## 📁 Key files
- `STATUS.md` — living overview + axiom ledger.
- `PENDING_WORK.md` — item 1 = the lone (off-headline) future target if finish-and-stop is lifted.
- `.bump-axioms` — the 14-headline faithfulness gate list; `lake env lean` an `#print axioms` file from it.
- `ON-LINE-REQUEST.md` — open external-repo doc tasks (block nothing here).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. The repo is
COMPLETE and the treadmill has STOPPED by design: absorb the context above, then STAY STOPPED unless Trevor
explicitly lifts finish-and-stop. There is no proof work to pick up.**
