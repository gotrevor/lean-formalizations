# Handoff: Catalan salvage — Phase 3 GREEN, run stopped

**Date**: 2026-09-04 12:29 UTC · **Branch**: `catalan` · **HEAD**: `bf5fd77`

## 🎯 What we're doing
Formalizing the salvage of Sun's arXiv:2609.04176v1 (Catalan's constant) in
`src/LeanFormalizations/NumberTheory/Catalan/`.  Phase 1 (Thm 2.1 + 2-adic no-go) was already
green.  This lap's objective was Phase 3: make `Frame.lean` — the generic frame any
Apéry/Nesterenko-style attack on `G` must fill — sorry-free and axiom-clean.  **Done.**  The
treadmill stop was requested; this doc checkpoints the finished state.

## 🧠 Context to carry forward
- `Frame.lean` has three blocks: **W** (wiring: integers → 0 forces irrationality), **E**
  (integrality: `F_B`-free integerizer of the fake-tail residual matrix, culminating in the
  floor `|det| ≥ 1/(q·L²)^S`), **D** (real place: Beta identity, its square, finite difference
  of `p/(X+c)²`, closed form `resid_tail_eq` of the true-tail entries), plus the sink edge
  `catalan_irrational_of_smallForms : SmallForms → Irrational catalanConst`.
- **`SmallForms` is a `def : Prop` and must stay one.**  Host ledger probe says it is false for
  Sun's weights (`log|N_B| ≈ +3.4·S·B`).  Nothing in the repo claims `G` irrational.
- **Statement fix made this lap (loud):** E1 `resid_fakeTail_den` and E2
  `det_resid_fakeTail_den` gained `(hSB : S ≤ B)`.  Frozen E1 was false: exact rational probe
  at `B=0, S=7, a/q=1` leaves denominator 15 after scaling by `q·L²` (the pole
  `1/(2(i+j+1)+1)` is absorbed by `Π_i` only when `j+1 ≤ B`).  E3/sink already had `S < B`.
  All other 12 frozen statements were proved exactly as planted.
- Reusable technique (details in `HANDOFF-2026-09-04-catalan-phase3-green.md`): `IsInt`
  closure toolkit for integrality; `resid_map` + `RingHom.map_det` for the ℚ→ℝ cast;
  `exists_taylor_two` (two `divByMonic`s) for the pole decomposition;
  `Summable.tsum_finsetSum` for the finite-sum/tsum swap.

## ✅ State
- `lake build` green (8661 jobs), observed at each commit; pre-commit hook rebuilt too.
- `#print axioms` = `[propext, Classical.choice, Quot.sound]` on `irrational_of_forms`,
  `abs_det_ge_of_rational`, `catalan_irrational_of_smallForms`, `alt_choose_sum_div_sq`,
  `resid_tail_eq` (observed).
- Entire `Catalan/` directory sorry-free.  Commits: `3266127`, `4577241`, `7d84bfd`, `bf5fd77`.
- `box done --green` was signalled (sentinel written); host verifies and halts.
- Uncommitted, NOT mine, left untouched: `papers/catalan-motive-probe.py` (modified),
  `papers/catalan-three-proof-paths.md` (untracked), `papers/__pycache__/`.

## 🎬 Next actions
1. Host: merge `catalan` → `main` (never pushed from the box).
2. If a v2 construction appears: replace `resid`/`oddLcm` in `Frame.lean`, re-run the ledger
   probe `papers/sun-2026-catalan-twoadic-check.py ledger` FIRST; `resid_tail_eq` gives the
   exact closed form its real-place estimate starts from.  No asymptotics in Lean.
3. Optionally fold the E1/E2 hypothesis change into `DIRECTION.md`'s frozen-name list (altitude
   lap owns that file).

## ⚠️ Gotchas
- `push_cast` on a whole ℚ→ℝ inequality over-normalises to `NNRat.divNat`; rewrite with explicit
  `← Rat.cast_*` then `Rat.cast_lt`.
- `set` abbreviations make `rw [sum_range_succ]` hit both sides; state the successor equations
  as standalone `have`s.
- `omit [CharZero F] in` must precede the docstring, not sit between docstring and `lemma`.

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/Catalan/Frame.lean` — Phase 3, complete.
- `HANDOFF-2026-09-04-catalan-phase3-green.md` — per-leaf technique notes.
- `DIRECTION.md` — objective, frozen names, lane discipline.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait
for instructions, and don't offer other projects from the KB — this doc IS the chosen thread.
The run is complete; pick up at "Next actions" #1 only if asked to continue the Catalan thread.**
