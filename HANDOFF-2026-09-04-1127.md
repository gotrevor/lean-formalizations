# Handoff: Catalan salvage — Phase 1 GREEN, run stopped by host

**Date**: 2026-09-04 11:27 UTC · **Branch**: `catalan` · **HEAD**: `9d2615d`

## 🎯 What we're doing
`DIRECTION.md` (the Catalan salvage of Sun's arXiv:2609.04176v1): build the machine-checked
conjecture graph around what survives of the paper — Theorem 2.1 (`residual_rank`) and the
2-adic no-go (`sun_ledger_impossible`).  **Phase 1 is complete.**  The host stopped the treadmill
on the scoped target (`sorry-free:src/LeanFormalizations/NumberTheory/Catalan` — 0 sorries).
Nothing in this thread claims or implies that Catalan's constant is irrational; it remains open.

## 🧠 Context to carry forward
- Every frozen statement in the planted scaffold was correct; none needed fixing.
- The crux `resid_mulVec_eq_zero` was closed by a design change: instead of the paper's `K`
  (degree `≤ 4B`, `4B+1` zeros counted with multiplicity via `gcd(D(X),D(X+1))`), the formal
  proof uses `Kpoly := K/(lin 1 · Gpoly)` — degree `≤ 2B+S+1`, `2B+S+2` simple integer zeros —
  and `eq_zero_of_natDegree_lt_card_of_eval_eq_zero`.  `key_identity` recovers the
  rational-function identity that `no_rational_solution` consumes.
- `no_rational_solution` needs no algebraic closure: after the coprime reduction,
  `D(-3/2)·D(-1/2)=0` forces an infinite arithmetic progression of roots.
- Phase 2 (the moonshot) is **probe-first** and per `DIRECTION.md` owned by review laps: state a
  `def … : Prop` with provenance, run the numeric probe, and record refutations; no Lean
  discharges for Phase-2 nodes.

## ✅ State
- All four modules `Tails/Residual/TwoAdic/Statement.lean` sorry-free; `lake build` green
  (8660 jobs, observed).  `#print axioms` on `residual_rank`, `exists_row_set_det_ne_zero`,
  `fakeTail_eq_tail_of_catalan_eq`, `sun_ledger_impossible`, `two_pow_padicValNat_bigF_dvd_NB`,
  `padicValNat_two_bigF_ge`, `tail_add_tail_succ`, `tail_eq_catalan_sub_partialSum` =
  `[propext, Classical.choice, Quot.sound]` (observed this session).
- Docs updated: `HANDOFF-2026-09-04-catalan-phase1-green.md` (full lap record + gotchas),
  `HANDOFF.md` pointer, `STATUS.md` banner, Catalan `README.md`, `PENDING_WORK.md`.
- `box done` acknowledged by the host; no relaunch.  Not pushed (host pushes).

## 🎬 Next actions
1. Host: push branch `catalan`; decide whether to merge to `main` (only Catalan files + docs changed).
2. Phase 2 (review lap, optional): extend `papers/sun-2026-catalan-twoadic-check.py` to report
   the real-place growth in `B` of the `F_B`-free `N'_B` (node N1) and to model an even
   normaliser (node N2); a node whose integer grows with `B` is refuted — record it as a
   killed thread, build no road.

## ⚠️ Gotchas
See the gotcha list in `HANDOFF-2026-09-04-catalan-phase1-green.md` (no order on abstract
`Field F`; explicit `s f` args of `natDegree_prod_le`/`natDegree_sum_le_of_forall_le`;
`Δ_[h]`/`/.` notations need `open`; `Matrix.submatrix_smul` shape).

## 📁 Key files
- `DIRECTION.md` — the objective and frozen names.
- `src/LeanFormalizations/NumberTheory/Catalan/Statement.lean` — audit surface (headlines).
- `src/LeanFormalizations/NumberTheory/Catalan/Residual.lean` — Theorem 2.1 engine + crux design.
- `papers/sun-2026-catalan-twoadic-check.py` — the Phase-2 probe to extend.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait
for instructions, and don't offer other projects from the KB — this doc IS the chosen thread.
Phase 1 is done; pick up at "Next actions" #2 only if Trevor asks for Phase 2.**
