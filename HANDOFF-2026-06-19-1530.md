# Handoff: sharp Mertens trilogy landed — C₃=−γ is the sole remaining gap

**Date**: 2026-06-19 15:30 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `f197b91`

## 🎯 What we're doing
Isolated clone for the no-three-in-line / HJSW frontier. Flagship `3/2 − o(N)` is DONE & axiom-clean
(`weakPNT` discharged earlier). This thread is extending the unlocked Chebyshev/PNT layer. Last lap
landed the classical Mertens trilogy in `O(1)` form. **This lap upgraded all three to sharp
*convergence* form** and reduced the classical `e^{−γ}` headline to a single named deep equation.

## 🧠 Context to carry forward
- **The Mertens work lives entirely in `src/.../PrimeNumberTheorem/Mertens.lean`** (now ~1200 lines).
  All headlines `#print axioms`-clean (`[propext, Classical.choice, Quot.sound]`), all mathlib-absent.
- **What "sharp" meant here, concretely:** the old `mertens_second`/`_third_up_to_const` were `=O[atTop] 1`.
  The upgrade is genuine *limits*. For the 2nd, the trick was turning the bounded remainder integral into a
  **convergent improper integral** over `(2,∞)`: bound `∫_2^N ‖remainder‖ ≤ C/log2` (via
  `intervalIntegral.integral_mono_on` + `integral_inv_mul_sq_log`), feed
  `MeasureTheory.integrableOn_Ioi_of_intervalIntegral_norm_bounded` to get `IntegrableOn … (Ioi 2)`, then
  `intervalIntegral_tendsto_integral_Ioi 2 _ tendsto_natCast_atTop_atTop`. For the 3rd, the correction
  series `∑'_p(log(1−1/p)+1/p)` is absolutely convergent (`Summable.of_norm_bounded` vs `∑1/n²`), so the
  finite `primeCorr N` converges to the tsum (`HasSum.tendsto_sum_nat` composed with `+1`).
- **Why I did NOT start the `C₃=−γ` proof this lap:** it is a genuine multi-lap ℂ-analysis Tauberian
  tower, not a 1-lap unit. I verified the ingredients exist in mathlib but the assembly (real prime-zeta
  `P(s)`, `logζ = P + bounded`, boundary→partial-sum Abel transfer) is many interlocking ℂ lemmas. The γ
  does **not** fall out of `logζ(s)+log(s−1)→0` (that limit is 0); γ enters via the Abel transfer. A
  half-built, possibly-wrong brick would violate the correctness bar, so I instead **maximally narrowed**
  the gap: stated `mertens_third_classical` (headline modulo the one equation), imported γ, and documented
  the exact route. That's a legitimate "advance the wall" lap per the charter.
- **Aristotle `0fa80268` (`summable_primeCorr`) is now SUPERSEDED** — I proved the equivalent
  `summable_primeCorrCoeff` locally. When it returns, just note it; do not port (and Aristotle defaults to
  mathlib v4.28.0 ≠ our v4.29.1, so it'd need in-kernel re-verification anyway).

## ✅ State (all observed this lap)
- `lake build` **GREEN, 8296 jobs** (pre-commit green-gate fired on every commit; Mertens module 3563 jobs).
- **7 commits** this lap (`ecebe9d` … `f197b91`). New axiom-clean theorems in `Mertens.lean`:
  - `mertens_second_tendsto` : `∑_{p≤N}1/p − log log N → M` (`meisselMertensM`).
  - `mertens_third_tendsto` : `log∏(1−1/p) + log log N → C₃` (`mertensThirdConst := (∑'ₚ) − M`).
  - `mertens_third_tendsto_exp` : `∏(1−1/p)·log N → e^{C₃}`.
  - `mertens_third_isEquivalent` : `∏(1−1/p) ~[atTop] e^{C₃}/log N` (textbook form).
  - `mertens_third_classical (hγ : mertensThirdConst = −Real.eulerMascheroniConstant)` ⟹
    `∏(1−1/p)·log N → e^{−γ}` — no axiom added; the gap is an explicit hypothesis.
  - support: `mertensRemBound`, `mertensRemainder`, `integrableOn_mertensRemainder_{Icc,Ioc,Ioi}`,
    `integral_norm_mertensRemainder_le`, `mertensRemainder_integral_tendsto`, `primeCorrCoeff`,
    `summable_primeCorrCoeff`, `primeCorr_eq_sum_range`, `primeCorr_tendsto`, `primeProd_pos`.
- Other open `sorry`s unchanged & off every headline: `nagura_prime`, `Wiener.prelim_decay_2/3` (dead code).

## 🎬 Next actions
1. **Attack `C₃ = −γ`** (the sole gap to classical Mertens 3rd). It's multi-lap — *advance*, don't expect
   to close. First bounded brick: define the real prime-zeta `P(s) = ∑'_p (p:ℝ)^{−s}` (s>1) and prove the
   `s→1⁺` behaviour of `logζ(s) + log(s−1)` from `riemannZeta_eulerProduct_exp_log` (in
   `EulerProduct/DirichletLSeries.lean`) + `tendsto_riemannZeta_sub_one_div` (`ZetaAsymp.lean`, gives
   `ζ(s)−1/(s−1)→γ`). NB ζ is over ℂ; for real s>1, `(s:ℂ)` makes `ζ` real-positive — extracting that
   cleanly is itself a sub-step. Full route is in `PENDING_WORK.md` (§"the ONE deep equation C₃=−γ").
2. If the ℂ tower stalls, the lower-hanging PNT-layer alternative is explicit two-sided Chebyshev `ψ/θ`
   bounds (see PENDING). Do NOT retreat to `nagura_prime`/`prelim_decay` — both lower value, off-headline.
3. Keep Aristotle fed only if a genuinely-OPEN *bounded* lemma exists. Right now the only open work is the
   deep ζ tower (not cleanly bounded), so an idle Aristotle is correct — do not manufacture cross-confirms.

## ⚠️ Gotchas
- `isEquivalent_iff_tendsto_one` leaves the quotient as **pointwise function division** `(f/g) N` (a
  `Pi.div` redex), not `f N / g N` — `rw [div_div_eq_mul_div]` fails "pattern not found" until you
  `simp only [Pi.div_apply]`. Plain `simp only []` does NOT reduce it. (Flushed to staging memory.)
- `integrableOn_primeSumDiv_floor_div N` is stated for `Icc 2 N`; for the `(2,N]` hypothesis of the
  improper-integral lemma use `.mono_set Set.Ioc_subset_Icc_self`.
- `open MeasureTheory intervalIntegral` makes `integral_const_mul`/`integral_mono_on` ambiguous — qualify
  as `intervalIntegral.…`.
- Aristotle proofs need in-kernel `#print axioms` verify (its v4.28.0 ≠ our v4.29.1).

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/Mertens.lean` — the whole Mertens development.
- `PENDING_WORK.md` — §"the ONE deep equation C₃=−γ" has the precise route + first bounded sub-step.
- `STATUS.md` — ledger (top changelog entry = this lap). `memory/weakpnt-discharge-map.md` — running map.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this clone IS the chosen thread. Absorb the
context above, then start at Next action #1: begin the `C₃=−γ` attack by formalizing the real prime-zeta
`P(s)` and the `s→1⁺` log-ζ brick (route in PENDING_WORK).**
