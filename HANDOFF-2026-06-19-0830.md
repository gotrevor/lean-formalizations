# Handoff: general-`N` constant frontier — Chebyshev θ-lower infra LANDED

**Date**: 2026-06-19 ~08:30 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `4d6629d`

> **Final-state note (end of lap).** After building the Chebyshev infra I examined the Nagura wall
> precisely and recorded a DEFINITIVE finding: the elementary Chebyshev bounds are **provably
> insufficient** for ratio `6/5` (crude split ⇒ `C(2n,n) ≤ (2n)^√(2n)·4^(31n/15)`, `31/15>1`, no
> contradiction). Examined mathlib's `Bertrand.real_main_inequality` — the `6/5` analogue is NOT a
> trivial reparametrization; it needs Nagura's genuine sharper estimate. So `nagura_prime` is blocked on
> external input: **filed `ON-LINE-REQUEST.md`** (Nagura 1952 tuned inequality) + Aristotle `1644a603`
> attempting it. All other repo obligations are closed (headlines proven & axiom-clean). 11 commits this
> lap (`745ba53`→`4d6629d`); whole repo green (8286 jobs).

## State
All NTL **headlines** proven & axiom-clean (kernel-verified this lap): `hjsw_lower`/`hjsw_lower_bound`
(3N/2 at N=2p), `2N` upper, Erdős Θ(N), general-`N` `3N/4`. The other five threads complete &
axiom-clean. `lake build` GREEN (8286 jobs). `src/` carries **one** disclosed `sorry`: `nagura_prime`
(the active frontier, NOT a headline).

## What this lap did (review lap → general-`N` constant frontier)
1. **Kernel-reverified** all 7 NTL headlines axiom-clean.
2. **Refactored** the prime-gap interface `maxNoThreeInLine_ge_of_two_mul_prime_le` (+ `_mono`) — the
   plug-in point separating combinatorics from the prime-gap input. Axiom-clean.
3. **Opened `PrimeGap.lean`**: `nagura_prime` (prime in `(n,6n/5]`, Nagura 1952) = disclosed-`sorry`
   crux; wired the `5/4` payoff `maxNoThreeInLine_ge_five_fourths` (`3⌊5N/12⌋ ≤ max N`, `N≥60`).
4. **Built the Chebyshev lower-bound infrastructure mathlib lacks — from scratch, all axiom-clean:**
   - `centralBinom_dvd_lcm_Icc` (`C(2n,n) ∣ lcm(1..2n)`) → `four_pow_lt_mul_lcm` (`4ⁿ < n·lcm(1..2n)`).
   - `factorization_finset_lcm` (`v_p` of a Finset.lcm = sup) + `primePow_dvd_lcm_Icc_iff`
     (`pᵏ ∣ lcm(1..N) ⟺ pᵏ ≤ N`).
   - `log_lcm_Icc_eq_psi` (`log(lcm(1..N)) = ψ N`, the von Mangoldt ↔ lcm bridge, via `vonMangoldt_sum`).
   - `psi_lower` (`n·log4 − log n < ψ(2n)`) + `theta_lower` (`… − 2√(2n)·log(2n) < θ(2n)`). **The θ
     LOWER bound mathlib was missing.** All 5 spinoffs are PR-worthy mathlib gaps.
5. Submitted `nagura_prime` to **Aristotle `1644a603`** (RUNNING at lap end).

## Exact next steps
1. **Harvest Aristotle `1644a603`** (`aris-nagura`) when IDLE: `aristotle download 1644a603 --destination
   /tmp/aris-nagura-out`; kernel-verify + `#print axioms`; port onto `nagura_prime`.
2. **The Nagura endgame = the refined central-binomial argument.** ⚠️ `theta_lower`'s constant is crude
   (`θ(x) ≳ 0.69x`); with `θ(x) ≤ 1.386x` it only gives `c>2` (Bertrand), NOT `6/5`. Nagura needs the
   factorization split of `C(2n,n)` — sharpen mathlib's `Nat.centralBinom_le_of_no_bertrand_prime`
   (`Mathlib/NumberTheory/Bertrand.lean`): under "no prime in `(n,6n/5]`", primes `>n` dividing
   `C(2n,n)` lie in `(6n/5,2n]`, so `C(2n,n) ≤ (2n)^√(2n)·4^(2n/3)·∏_{6n/5<p≤2n}p`; bound the product
   (`primorial_le_four_pow` / `theta_lower`) and contradict `4ⁿ ≤ n·C(2n,n)` for large `n`; small
   `n∈[25,N₀)` by explicit prime list / `decide`. The delicate part is the cross-range constant
   accounting — genuine multi-lap content. See `PENDING_WORK.md` top.
3. **Carry-over:** Aristotle `72891d77` (NL→Lean faithfulness of the headline) finished IDLE but its
   `show`/`download` 500 server-side this lap — retry. Not load-bearing.

## Build / rules
`lake build` (whole repo) or `…NoThreeInLine.PrimeGap`. Commit every green; never push (host pushes).
Tree clean at `f2d4c9f`. ALLOW_STOP=1 but project is NOT complete (`nagura_prime` open) — do not stop.
