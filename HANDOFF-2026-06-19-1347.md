# Handoff: classical MERTENS TRILOGY landed — next is the sharp constants

**Date**: 2026-06-19 13:47 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `5f0111b`

> Companion to the detailed baton **`HANDOFF-2026-06-19-1342.md`** (read it for the full gotcha list +
> per-theorem lemma map). This is the budget-checkpoint snapshot; the two commits since `f9b15ab` are doc-only.

## 🎯 What we're doing
Isolated clone for the no-three-in-line / HJSW frontier. The flagship `3/2 − o(N)` is DONE & axiom-clean
(`weakPNT` discharged a prior lap). This lap drove the unlocked Chebyshev/PNT layer to the **classical
Mertens trilogy** in `src/.../NumberTheory/PrimeNumberTheorem/Mertens.lean`. Next: the **sharp Mertens
constants** (Meissel–Mertens `M`, then `e^{−γ}`/`γ`).

## 🧠 Context to carry forward
- **`weakPNT` is a THEOREM, not an axiom** — if any doc says otherwise it's stale (I corrected STATUS/HANDOFF
  this lap). Every headline in `src/` is `#print axioms`-clean; `src/` carries **0 math axioms**.
- **Why I stopped before the constant-`M` upgrade:** it's a coherent ~100-line improper-integral unit that
  re-uses `mertens_second`'s internals; better as a fresh-context unit than crammed at commit 23 of a
  marathon. The exact route is recorded (PENDING action #2) — not a dead end, just deferred.
- **Aristotle defaults to mathlib v4.28.0 ≠ our v4.29.1.** Its returned proofs need in-kernel `#print axioms`
  verification before trust/port. Its `prelim_decay_2` proof (`c6d615ee`) FAILED on our pin (sorryAx,
  smul/mul drift) — correctly NOT ported (dead code anyway).

## ✅ State (all observed this lap)
- `lake build` **GREEN, 8296 jobs**; Mertens module green (3563 jobs); pre-commit green-gate on every commit.
- Mertens trilogy, all axiom-clean (`[propext, Classical.choice, Quot.sound]`), mathlib-absent:
  `mertens_first_prime`, `mertens_second`, `mertens_third_up_to_const` (`∏(1−1/p) ≍ 1/log N`).
- 23 commits this lap (`8dd6b6b` … `5f0111b`). Open `sorry`s (all non-blocking, off every headline):
  `nagura_prime`, `prelim_decay_2/3`.
- **Aristotle `0fa80268` RUNNING** on `summable_primeCorr` (correction-series convergence).

## 🎬 Next actions
1. **Poll Aristotle `0fa80268`** (`aristotle show 0fa80268-…`, the one-shot). If it returns
   `summable_primeCorr`: VERIFY on our pin (`#print axioms`, v4.28.0 drift risk) before porting. Else prove
   locally (comparison `|log(1−1/p)+1/p| ≤ 1/p² ≤ ∑1/n²` over `Nat.Primes`; reuse `log_one_sub_add_self_abs_le`,
   `summable_one_div_sq`). Whenever it idles AND an open lemma exists, submit the next bounded one.
2. **Upgrade Mertens' 2nd to convergence** `∑1/p − log log x → M` (Meissel–Mertens). Full route in
   `PENDING_WORK.md` action #2: `integrableOn_Ioi_of_intervalIntegral_norm_bounded` (I=`Cr/log2`) →
   `IntegrableOn f (Ioi 2)` → `intervalIntegral_tendsto_integral_Ioi` → assemble with
   `primeSumDiv_div_log_tendsto_one`. `f` = remainder integrand; reuse `mertens_second`'s `h_ae`/`hI1`/`hI2`.
3. Then the deep `M = γ` (`e^{−γ}`) identification (ζ Euler product near `s=1`) — generational, multi-lap.

## ⚠️ Gotchas
- `open MeasureTheory intervalIntegral` → `integral_const_mul`/`one_div_le_one_div_of_le` ambiguous; qualify.
- `congr_fun`-family + `Finset.sum_filter_of_ne` give un-β-reduced `(fun x=>…) t` → `simp only []` before `rw`.
- `sum_mul_eq_sub_integral_mul₁` (NOT `₀`): `1/log` undiff. at `t=1`. `integrableOn_mul_sum_Icc` for step-fn.
- Aristotle proofs: verify on our pin (v4.28.0 drift). Full list in `HANDOFF-2026-06-19-1342.md`.

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/Mertens.lean` — the Mertens trilogy.
- `PENDING_WORK.md` — sharp-constants plan (action #2 has the exact improper-integral lemma route).
- `STATUS.md` — ledger (every headline 0 math axioms). `HANDOFF-2026-06-19-1342.md` — full detail.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this clone IS the chosen thread. Absorb the
context, then start at Next action #1: poll Aristotle `0fa80268`, then drive the Mertens-2nd convergence
upgrade (action #2, route fully mapped in PENDING_WORK).**
