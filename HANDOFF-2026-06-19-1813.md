# Handoff: A3 crux is the only open sorry — Bachmann proven & wired, ready to attack

**Date**: 2026-06-19 18:13 UTC · **Branch**: `kakeya-davies` · **HEAD**: `34f9f1b`

## 🎯 What we're doing
Headline `davies_kakeya_2d` (planar Kakeya, Davies 1971) is **COMPLETE + axiom-clean** — that is the
deliverable and it is done (`STATUS.md` has Trevor's FINISH-AND-STOP banner). But the keep-going
governor directs continued sorry-closing, so this run is now driving the **only remaining open sorry in
the repo**: `fastGrowing_fundSeq_step` (the A3 index-monotonicity crux of the fast-growing hierarchy
growth theory, `wip/Logic/FastGrowing/Basic.lean:155`).

## 🧠 Context to carry forward
- **Banner vs governor**: `STATUS.md`'s banner says self-stop (headline done). The governor overrode
  the self-stop repeatedly and wants sorries closed. The reconciliation that worked all lap: **comply
  with the governor BUT never touch the headline files** (`src/.../Kakeya2D/Statement.lean` etc.).
  Headline stayed axiom-clean throughout. Do the same.
- **A3 crux — the real wall, precisely mapped this lap.** `fastGrowing_fundSeq_step` (limit `o`, fund
  seq `f`): `fastGrowing (f n) (n+1) ≤ fastGrowing (f (n+1)) (n+1)`. Validated reduction (compiled in a
  scratch with axiomatized prereqs): case-split on `fundamentalSequence (f (n+1))` →
  - `inl none` (f(n+1)=0): **dispatched** (vacuous: `f n < f(n+1)=0` contradicts `0 ≤ f n`).
  - `inl (some a)` / `inr g`: both collapse to ONE shape `fastGrowing X (n+1) ≤ fastGrowing Y (n+1)`
    with `X ≤ Y` (limit case: `X=f n`, `Y=g(n+1)`, with `f n ≤ g(n+1)` from Bachmann).
  - **KEY**: this residual is NOT provable from `X ≤ Y` alone — plain fixed-arg index monotonicity is
    FALSE (`f₅(2) ≫ f_ω(2)=8`). It needs the **coupled arg+index induction** (Buchholz /
    Cichoń–Wainer / Fernández-Duque–Weiermann arXiv:2203.07758): the Bachmann *relation* `≤_n` and
    monotonicity along it, by well-founded recursion on structural depth. This is research-grade,
    multi-lap. **Do NOT introduce `fastGrowing_coupled_mono : X ≤ Y → fastGrowing X k ≤ fastGrowing Y k`
    as a sorry — that statement is FALSE.** Keep the honest monolithic `fastGrowing_fundSeq_step` sorry
    until the true coupled invariant is formalized. The residual must carry the full coupling hypotheses
    (e.g. `g` is the fund seq of `f(n+1)`, `f n ≤ g(n+1)`, depth/`n`-bound), not just `X ≤ Y`.
- **Bachmann prerequisite is DONE.** `fundSeq_bachmann` (Bachmann inequality `f n ≤ g(n+1)`) is fully
  proven, axiom-clean, **needs no `NF` hypothesis**, and is now imported into `Basic.lean`. So the limit
  case of A3 can call it directly.
- **Aristotle**: no jobs in flight (faithfulness job `4addbb42` harvested; bachmann job `6a98cf00`
  canceled because I proved it locally first). If you want parallelism, the A3 coupled-mono is a
  candidate to feed, but it's hard to state as a self-contained Problem — better to develop locally.

## ✅ State (all verified this lap via real build output)
- `lake build` 🟢 **green, 8301 jobs** (headline, defaultTargets). `davies_kakeya_2d` axiom-clean
  (`#print axioms = [propext, Classical.choice, Quot.sound]`). `src/` headline files UNTOUCHED.
- `lake build DaviesWip` 🟢 **green, 8252 jobs** — one disclosed sorry (the A3 crux). DaviesWip is an
  off-headline lean_lib (`srcDir = "../wip"`, NOT in defaultTargets) globbing the 4 active wip files.
- **Two wip files driven sorry-free this lap**: `Capacitability.lean`+`VonNeumannSelection.lean` (jvn
  route, `7f1ac85`); `Bachmann.lean` (`339c939`, simplified `19fe59c`).
- New src/ audit file `FaithfulnessCheck.lean` (`5a31aab`): machine-checked that an independent
  Aristotle formalization of the prose ≡ our `KakeyaSetConjectureDim 2`. Axiom-clean.
- Working tree clean except `STATUS.md` (Trevor's banner edit — leave unstaged, do not fold into commits).

## 🎬 Next actions
1. Attack `fastGrowing_fundSeq_step` (`wip/Logic/FastGrowing/Basic.lean:155`). Build the validated
   reduction (vacuous case dispatched), then develop the **coupled-monotonicity** core — define the
   Bachmann relation / the right inductive invariant, prove by WF recursion using `fundSeq_bachmann`
   (already imported, reference it as `Bachmann.fundSeq_bachmann` from inside `namespace
   LeanFormalizations.Logic.FastGrowing`, NOT the fully-qualified path — see Gotchas).
2. Check with `lake build DaviesWip` (the A3 file is in this lib). Commit every green step; expect this
   to span multiple laps — narrowing the invariant is itself progress.
3. Do NOT touch the headline `src/` files; they are the complete deliverable.

## ⚠️ Gotchas
- **Namespace ref**: inside `namespace LeanFormalizations.Logic.FastGrowing`, refer to the Bachmann
  lemma as `Bachmann.fundSeq_bachmann` (relative). Writing the *fully*-qualified
  `LeanFormalizations.Logic.FastGrowing.Bachmann.fundSeq_bachmann` gets re-prefixed by the open
  namespace → "Function expected" error. (Burned a few min on this at lap end.)
- `wip/` is outside defaultTargets: build with `lake build DaviesWip`, or single files with
  `lake env lean <path>`. `lean_lib.srcDir` COMPOSES with the package `srcDir` — that's why DaviesWip
  uses `../wip` (package srcDir is `src`).
- `aristotle list` (fast one-shot) for polling; never `aristotle show` (never-returning live TUI).
- `fundamentalSequence (oadd e m 0)` `b=0` case: `(n+1).succPNat.natPred = n+1` and `(1:ℕ+).natPred = 0`
  are `rfl` — feed them to `simp only` to reduce the inner match (this unlocked the Bachmann b=0 bash).

## 📁 Key files
- `wip/Logic/FastGrowing/Basic.lean` — the A3 crux (sorry at :155); growth theory; imports Bachmann.
- `wip/Logic/FastGrowing/Bachmann.lean` — `fundSeq_bachmann` PROVEN, axiom-clean, NF-free.
- `lakefile.toml` — `DaviesWip` lean_lib (4 wip globs).
- `PENDING_WORK.md` — full inventory + three-paths-each; A3 reduction recorded in item 3.
- `HANDOFF-2026-06-19-late.md` — fuller narrative of this lap's three wins.
- `src/.../Kakeya2D/Statement.lean` + `FaithfulnessCheck.lean` — the headline + its faithfulness guard. DO NOT TOUCH the headline.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context above, then pick up at "Next actions" #1: develop the coupled-monotonicity core of
`fastGrowing_fundSeq_step`, using the already-proven `Bachmann.fundSeq_bachmann`. Keep the headline
untouched and axiom-clean.**
