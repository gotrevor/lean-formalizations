# Handoff: planar Kakeya headline complete + a FALSE axiom found, refuted, removed

**Date**: 2026-06-19 21:30 UTC · **Branch**: `kakeya-davies` · **HEAD**: `483c97f`

(Lap-end checkpoint. The detailed baton for this lap is `HANDOFF-2026-06-19-2105.md` — read it for the
full anatomy of the unsoundness, the commit chain, and the axiom sweep. This file is the tight resume.)

## 🎯 What we're doing
Expedition: prove `davies_kakeya_2d : KakeyaSetConjectureDim 2` (planar Kakeya, Davies 1971), lane =
`src/LeanFormalizations/GeometricMeasureTheory/Kakeya2D/` only. **The headline is COMPLETE,
axiom-clean, faithful, and hardened.** This lap was a fresh-mind review that turned into a soundness fix.

## 🧠 Context to carry forward
- The headline was already proven (prior lap) via the **elementary open-cover selection** route
  (`Selection.kakeya_hausdorffContentBound_elementary`): fatten the cover to an open superset, a compact
  unit segment in an open set has a tube ⟹ a fixed dense base point gives full coverage. No descriptive
  set theory, no measurable-selection axiom. This is the SOUND route; do not re-attack measurable
  selection / von Neumann — it's solved.
- **The lap's finding:** the off-headline legacy axiom `kakeya_subresolution_content` (discrete-route
  "Case-B residual"), trusted as "true but deep" by every prior lap, is **FALSE**. I derived `False`
  from it (kernel-checked) — kept as the sound theorem `Engine.kakeya_subresolution_content_is_unsound`.
  Defects: (1) dropped the `hcR` constant↔exponent hypothesis (conclusion had to hold for arbitrary
  `cR>0` vs fixed finite `∑'ediam^d`); (2) even repaired, false for `d>1` (arbitrary base points ⟹
  unshared sub-resolution pieces, `∑ediam^d→0`).
- **Why this matters for how you work:** a self-authored axiom is unchecked conjecture; refute it before
  trusting/re-citing it. The discrete route genuinely cannot close Case B — that's WHY the elementary
  route exists. Do NOT reintroduce any abstract Case-B axiom, and do NOT follow `CASE_B_ANALYSIS.md`'s
  "(W) wire + (S) selection" plan to *prove* it (it's false; the doc now has a warning header).
- The legacy discrete `_discrete` chain is REDUNDANT with the headline (proves the same result a
  superseded way) and now carries a disclosed off-headline `sorry` at its Case-B branch. Closing that
  `sorry` soundly needs the deep multi-scale Córdoba sum — **low value, the headline doesn't need it.**
  Removing the redundant chain would be a Trevor call ("never delete structure"); the Engine import DAG
  (`Selection → Wiring → Engine`) blocks Engine from delegating to the elementary route.

## ✅ State (verified this lap via real `lake build` + `#print axioms`)
- Build 🟢 green, 8300 jobs. Working tree clean at `483c97f`.
- `#print axioms davies_kakeya_2d = [propext, Classical.choice, Quot.sound]`. Same for the new anchor
  `davies_kakeya_2d_disc_anchor : dimH (closedBall 0 1) = 2`, `two_le_dimH`,
  `hausdorffMeasure_pos_of_isKakeya`, `kakeya_hausdorffContentBound_elementary`, and the refutation
  `kakeya_subresolution_content_is_unsound`. ALL headline-path theorems clean.
- Repo-wide: **0 axiom declarations**; exactly **2 code `sorry`s**, both off every headline:
  `Engine.lean:450` (legacy Case-B) and `FastGrowing/Basic.lean:124` (dormant, out of lane).
- Defs (`IsKakeya`, `KakeyaSetConjectureDim`) match `~/src/formal-conjectures` `Wikipedia/Kakeya.lean`
  verbatim; the headline is a drop-in axiom-clean proof of that repo's `kakeya_2d := sorry` (line 70).

## 🎬 Next actions
1. The headline target is achieved and hardened. Pick ONE, in priority order:
   - **(low value, optional)** Decide the redundant legacy `_discrete` chain's fate: leave the honest
     `sorry` (current, recommended), or remove the redundant chain (Trevor call). Do NOT grind the deep
     multi-scale Córdoba sum just to close a redundant off-headline `sorry`.
   - **(hygiene)** Finish stale-docstring cleanup if desired (`Cover.lean` references are accurate; lower priority).
   - **(out of scope, would need new lane)** `kakeya_3d` (Wang–Zahl 2025) — multi-year, new infra.
2. If nothing above is judged worthwhile, this lane is at genuine terminus; a review/reflection lap may
   confirm and the allow-stop gate still fails only on the 2 off-headline/out-of-lane `sorry`s.

## ⚠️ Gotchas
- Engine is BELOW Selection in the import DAG — it cannot use the elementary route. That's why the
  Case-B branch is a `sorry` rather than a delegation.
- `kakeya_subresolution_content_is_unsound` proof: `Metric.ediam_le`, `edist_dist`, `ENNReal.ofReal_*`,
  `tsum_eq_single`; config `J=0`, one piece = one unit segment, `cR = vol(disc).toReal + 1`.

## 📁 Key files
- `Kakeya2D/Statement.lean` — audit surface: `davies_kakeya_2d`, the disc anchor, `isKakeya_closedBall`.
- `Kakeya2D/Selection.lean` — the SOUND headline route.
- `Kakeya2D/Engine.lean` — refutation `kakeya_subresolution_content_is_unsound`; legacy Case-A assembly + Case-B `sorry`.
- `STATUS.md` (ledger refreshed) · `PENDING_WORK.md` (finding + lesson at top) · `HANDOFF-2026-06-19-2105.md` (detailed baton).

---
**→ Next session: this is your starting point. The planar-Kakeya headline is DONE and axiom-clean — do
NOT re-prove it, do NOT re-attack measurable selection, do NOT reintroduce any Case-B axiom (it's
false). Don't offer other KB projects; this doc IS the thread. Read the context above, then act on
"Next actions" — most likely confirming the lane is at terminus and that remaining items are low-value
off-headline cleanup. Stay in the `Kakeya2D/` lane.**
