# Handoff: no-three-in-line general-`N` constant frontier CLOSED (Bertrand 3/4 → HJSW 3/2−o(N))

**Date**: 2026-06-19 10:18 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `7cb9513`

## 🎯 What we're doing
Lifting the HJSW no-three-in-line **general-`N` lower constant**. At lap start it was `3/4` (Bertrand).
This lap pushed it all the way to HJSW's optimal **`3/2 − o(N)`**. The frontier is now **closed**; the
single remaining piece of debt is discharging one cited PNT axiom.

## 🧠 Context to carry forward (the mental model — read this)
The whole thread is: *a prime in `(n, c·n]` ⟹ no-three constant `3/(2c)`* (`shearSel` construction,
interface `maxNoThreeInLine_ge_of_two_mul_prime_le`). So everything reduces to **how small a `c > 1` we
can guarantee a prime gap for**.

- The route to a prime gap is the **two-sided Chebyshev `θ` argument**: if no prime in `(n, ⌊cn⌋]` then
  `θ(⌊cn⌋) = θ(n)` exactly (prime-filtered `Ioc` sums coincide — this `hθeq` Finset proof is reused
  verbatim in all three gap lemmas). Combine a `θ` *lower* bound at `cn` with a `θ` *upper* at `n`;
  `c > U/A` (ratio of the two constants) gives a contradiction ⟹ a prime exists.
- **Elementary ceiling (proven, internalized):** the Chebyshev `T`-method gives `A≈0.921`, `(6/5)A≈1.106`
  — these *are* the classical Chebyshev constants, ratio exactly `6/5`. So elementary `c > 6/5`, constant
  caps **strictly below `5/4`**. I built the full elementary stack and reached `15/16` then `6/5`
  (unconditional, axiom-clean). Pushing further with "more primes" is a **mirage** — it only re-approaches
  `6/5`. (See memory `[[lean-chebyshev-tmethod-ceiling]]`.)
- **The breakthrough:** PNT `ψ(x)=x+o(x)` makes the ratio `→1`, so `c` can be *any* `>1`, constant `→3/2`.
  Key discovery: `~/src/PrimeNumberTheoremAnd` proves `WeakPNT'' : ψ ~[atTop] (·)` and its `ψ` **is
  mathlib's `Chebyshev.psi`** (proof via `psi_eq_sum_Icc`). I cited it as a **disclosed `axiom weakPNT`**
  (a proven theorem behind a wall mathlib lacks) and built the full `3/2−o(N)` on top.
- **Why an axiom, not an import:** PrimeNumberTheoremAnd pins `lean4:v4.29.0` vs our `v4.29.1` (+ possibly
  a different mathlib rev). Adding it as a lake dep is risky (could break the build), so I deferred it —
  the build stays self-contained with just the one disclosed axiom.
- `nagura_prime` (the old lone `sorry`, exact `6/5` at `n≥25`) is now **non-blocking and superseded** —
  the `3/2` result subsumes its purpose. Don't sink time into it.
- Lean tactic pain I hit (now in memory `[[lean-linarith-set-clearvalue-only]]`): `linarith` timeouts →
  `clear_value` on `set`-bindings, `linarith only [...]`, fraction not scientific literals, stage the
  final contradiction into tiny `linarith only` calls with `ring`-equation bridges for nonlinear monomials.

## ✅ State (all observed this session)
- `lake build` **GREEN, 8286 jobs**, confirmed after every commit; tree clean at `7cb9513`.
- All new lemmas in `PrimeGap.lean`, axiom-clean except where noted (`#print axioms` verified):
  - Elementary, **fully axiom-clean** `[propext, Classical.choice, Quot.sound]`:
    `psi_refined_lower`, `theta_refined_lower`, `logFactorial_comb_upper`, `logFactorial_comb_ge_psi_sub`,
    `psi_refined_upper_step`, **`psi_refined_upper`** (the telescoping iterate `ψ ≤ (6/5)A·n+O(log²n)`),
    `sqrt_log_small`, `log_le_sqrt_small`, `chebyshev_const_lt`, `floor_comb_ge_one`;
    headlines **`maxNoThreeInLine_ge_fifteen_sixteenths`** (`15/16`, `N≥2⁴¹`) and
    **`maxNoThreeInLine_ge_six_fifths`** (`6/5`, `N≥5·2⁴⁰`).
  - PNT-based (depend on `weakPNT` + standard three): `psi_pnt_bounds`, `sqrtlog_isLittleO`,
    `exists_prime_gap_pnt` (prime in `(n,cn]` ∀`c>1`), **`maxNoThreeInLine_ge_three_halves_sub`**
    (`∀ε>0, eventually (3/2−ε)N ≤ maxNoThreeInLine N`).
- The only `sorry` left in `src/` is `nagura_prime` (non-blocking).

## 🎬 Next actions
1. **Discharge `weakPNT`** to make `3/2−o(N)` fully axiom-clean. Try (in a throwaway branch first): add
   `~/src/PrimeNumberTheoremAnd` as a lake dependency, `import` its `Consequences`, and replace
   `axiom weakPNT` with `theorem weakPNT : Chebyshev.psi ~[atTop] (fun x ↦ x) := WeakPNT''` (or use it
   directly). Expect mathlib-rev/toolchain friction — if `lake` refuses, that's the real blocker; note it
   in `ON-LINE-REQUEST.md` (build-integration, doable on-box, not an online question).
2. If (1) stalls, the result is already a strong landing — leave `weakPNT` disclosed and consider the
   thread complete pending an upstream mathlib PNT.
3. Optional polish: a cosmetic `<;>`-vs-`;` linter warning in `PrimeGap.lean` (`field_simp <;> ring`).

## ⚠️ Gotchas
- Don't try to beat `5/4` with a finer Chebyshev prime combination — proven dead end (the `T`-method
  ratio is `6/5`). Only PNT gets past it.
- `exists_prime_gap_pnt` / the `3/2` theorem are stated with **`∀ᶠ … in atTop`** (existential threshold) —
  intentional; explicit thresholds would be astronomical and pointless here.
- `isLittleO_log_rpow_atTop` lives at root namespace (not `Asymptotics.`), under
  `import Mathlib.Analysis.SpecialFunctions.Pow.Asymptotics` (added this lap).

## 📁 Key files
- `src/LeanFormalizations/Combinatorics/NoThreeInLine/PrimeGap.lean` — the entire frontier; the PNT
  section is at the very end, after `maxNoThreeInLine_ge_five_fourths`.
- `PENDING_WORK.md` (top) — the closure record + the `weakPNT`-discharge plan.
- `STATUS.md` (top entry) — chronological state.
- `ON-LINE-REQUEST.md` — all asks now resolved/superseded (PNT found locally).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context above, then pick up at Next action #1: attempt to discharge `weakPNT` by depending on
`~/src/PrimeNumberTheoremAnd` (throwaway branch first), and if the toolchain/mathlib revs fight you,
record the precise blocker and leave it disclosed.**
