# Handoff: Limit B PROVEN — classical `e^{−γ}` Mertens 3rd now UNCONDITIONAL & axiom-clean

**Date**: 2026-06-19 16:39 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `e7d332f`

> Supersedes `HANDOFF-2026-06-19-1601.md`. That handoff had reduced the classical `e^{−γ}` Mertens 3rd
> to a single deep step — the Abelian/Tauberian final-value theorem. **This lap cracked it.** The whole
> Mertens thread is now COMPLETE.

## 🎯 What we're doing
The classical sharp Mertens 3rd theorem `∏_{p≤N}(1−1/p)·log N → e^{−γ}` was gated on one deep limit,
**Limit B**: `primeZeta s + log(s−1) → M − γ` as `s→1⁺` (`M = meisselMertensM`, `γ = eulerMascheroniConstant`).
Limit B's sole remaining gap was the **Abelian final-value theorem** (a Tauberian step, not in mathlib).
This lap proved that crux and assembled Limit B end-to-end, discharging `e^{−γ}` unconditionally.

## ✅ State — all observed green this lap (`lake build` = 8297 jobs, every commit), all `#print axioms` = `[propext, Classical.choice, Quot.sound]`
All new work in `MertensConstant.lean`. New top-level theorems (NO hypotheses, fully unconditional):
- **`mertens_third_classical_eGamma`** : `∏_{p≤N}(1−1/p)·log N → e^{−γ}` — the headline.
- **`mertensThirdConst_eq_neg_gamma`** : `C₃ = −γ`.
- **`tendsto_primeZeta_add_logSub_limitB`** : Limit B itself.
- **`tendsto_sub_one_mul_integral_abelian`** : the deep crux — for `f→0` at atTop (+ integrability),
  `δ∫_0^∞ f·e^{−δx} → 0` as `δ→0⁺`. Proved by the classical **ε–X split** (NOT dominated convergence).

Supporting lemmas (all axiom-clean): `integrableOn_{id,one_add_id,primeRecipSum,log}_mul_exp_neg_mul`
(the 3 `eˣ`-form pieces integrable on `(0,∞)`); `tendsto_log_floor_exp_sub`,
`tendsto_log_log_floor_exp_sub_log`, `tendsto_floorExpRemainder` (the remainder `r(x)→0`).

Commits this lap (oldest→newest): `e58bc10` (Abelian crux) · integrability pieces · `dd8f27d` (`r→0`) ·
`59ba188` (Limit B assembled + `e^{−γ}` unconditional) · `e7d332f` (STATUS).

## 🧠 Context to carry forward
- **Proof architecture of the crux** (`tendsto_sub_one_mul_integral_abelian`): I deliberately chose the
  ε–X argument over dominated convergence because `r(x)` is **unbounded near `x=0`** (`r=−log x−M→+∞`
  for `x<log 2`, since `primeRecipSum⌊eˣ⌋=0` there), which makes a uniform DCT envelope (with a `log δ`
  blow-up) painful. The ε–X split sidesteps that entirely: split `∫_0^∞=∫_(0,X]+∫_(X,∞)`, tail
  `≤(ε/2)/δ` (so `δ·tail≤ε/2`), head `≤∫_(0,X]|f|=K` (since `e^{−δx}≤1`, so `δ·head<ε/2`). This is also
  why the crux's hypotheses are `(hf0: f→0)`, `(hint: f·e^{−δx} integrable on Ioi 0 ∀δ>0)`,
  `(hloc: |f| integrable on Ioc 0 X ∀X)` — exactly what the split needs, nothing more.
- **Assembly mental model**: `primeRecipSum⌊eˣ⌋ = log x + M + r(x)` splits the Abel-integral into three
  integrable pieces giving `primeZeta s = (−γ−log(s−1)) + M + (s−1)∫r·e`; the `−γ` enters via the
  γ-injection `∫_0^∞ log u·e^{−u}=−γ` (already in-file from a prior lap). The error `(s−1)∫r·e→0` is the
  crux composed with the shift `s↦s−1` (`𝓝[>]1 → 𝓝[>]0`).
- **The two remaining repo sorries are genuine infrastructure walls, both OFF every complete thread** — I
  did honest feasibility probes this lap:
  - `nagura_prime` (PrimeGap.lean:1120): needs **effective** PNT. The in-repo PNT is the *limit* form
    (`pi_alt'`, `chebyshev_asymptotic`), which gives no explicit threshold `N₀`, so the `n∈[25,N₀)`
    `decide` gap can't be bridged. Also **superseded** by the unconditional `3/2−o(N)` (its `5/4` is below).
  - `prelim_decay_2/3` (Wiener.lean:244/252): BV-Fourier decay `‖𝓕ψ u‖≤Var/(2π|u|)`. mathlib has NO
    Lebesgue–Stieltjes IBP, and no `eVariationOn = ∫|deriv|` for AC functions, so even the AC sub-route
    is blocked (`fourierIntegral_fderiv` needs everywhere-`Differentiable` + integrable `fderiv`, which
    BV ψ doesn't satisfy). These feed only the **unused** `decay_alt`; the flagship is axiom-clean without them.

## 🎬 Next actions
The Mertens/NTL critical paths are COMPLETE. Pick a **fresh high-value direction** (do NOT grind the two
blocked walls above — they need infrastructure mathlib lacks):
1. **Extend the now-unlocked PNT/Mertens layer with another mathlib-absent classical theorem.** Candidates
   in easy reach of the in-repo machinery: the explicit `θ(x)~x`/`ψ(x)~x` consequences not yet stated;
   Mertens-type `∑_{p≤x} (log p)/p² ` convergence; or a clean `Statement.lean`-style audit surface for the
   new `e^{−γ}` headline (faithfulness anchor). Decide and proceed.
2. If you want to attack a wall anyway, the *highest-leverage prerequisite* is `eVariationOn (AC f) = ∫|deriv f|`
   (the variation-equals-integral-of-derivative FTC) — that single mathlib-absent lemma would unblock the AC
   case of `prelim_decay_2/3`. Building it is a legitimate multi-lap prerequisite.

## ⚠️ Gotchas (this lap, reusable)
- **`integral_add` rw fails on beta-redex integrands.** When the goal integrand is `fun x => A+B+C`
  (beta-reduced) but `integral_add (hf.add hg) hk` produces a Pi-add pattern `((f+g) a + h a)`, `rw` won't
  match. Fix: state each additivity step as an **explicitly-typed `have`** (Lean coerces the `integral_add`
  term to your stated beta-reduced type via defeq), then `rw [h1,h2,h3]`. See `hsplit` in the assembly.
- `abs_add_le` is the triangle-inequality name (NOT `abs_add`, which is unknown).
- `Measurable.of_discrete` (fun_prop-tagged) handles the step function `primeRecipSum ∘ ⌊·⌋ ∘ exp`.
- `integrableOn_const` needs `(hs := measure_Ioc_lt_top.ne)` supplied — the `finiteness` autoparam/aesop
  times out on `Ioc`.
- `intervalIntegrable_log'` lives in namespace `intervalIntegral`. `lt_div_iff₀` (not `lt_div_iff`).
  `integrableOn_rpow_mul_exp_neg_mul_rpow` (p=s=1) is the clean source of `x·e^{−δx}` integrability;
  `integrableOn_Ioi_comp_mul_left_iff` does the `x↦δx` scaling for the log piece.
- Aristotle correctly left IDLE this lap — all the work was measure theory (its weak spot), and the one
  deep crux was a hand proof, not a feedable open lemma.

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/MertensConstant.lean` — ALL this lap's work is
  at the BOTTOM (after `tendsto_primeRecipSum_floor_exp`): the Abelian crux, the integrability pieces,
  `r→0`, then `tendsto_primeZeta_add_logSub_limitB` and the unconditional `e^{−γ}` headlines.
- `STATUS.md` — header + axiom ledger refreshed; the Mertens trilogy is now fully ✅ (no 🟡). NOTE: a few
  prose lines mid-file (≈20, 27–36, 236–239, 310) still describe Limit B as "open frontier" — stale, a
  next-lap review pass can clean them; the header and ledger are correct.
- `PENDING_WORK.md` — the C₃=−γ/Limit B section is now historical; its decomposition is all DONE.

---
**→ Next session: this is your starting point. The deep Mertens crux is DONE and verified — don't re-open
it, don't summarize this doc back to Trevor, and don't offer other KB projects. The NTL/Mertens threads
are complete; per Next action #1, pick ONE fresh mathlib-absent classical theorem in the unlocked
PNT/Mertens layer and start formalizing it. Decide the target yourself and proceed.**
