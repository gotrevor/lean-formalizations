# Handoff: Davies planar Kakeya — DEEP-REFLECTION lap; core glue of the wiring (W) PROVEN

**Date**: 2026-06-19 ~17:00 UTC · **Branch**: `kakeya-davies` · **HEAD**: `f57d64c`

## 🎯 What we're doing
Prove `davies_kakeya_2d : KakeyaSetConjectureDim 2` (planar Kakeya ⟹ `dimH = 2`, Davies 1971). Whole
job = the lower bound, machine-checked **down to ONE cited math axiom** `kakeya_subresolution_content`.
Verified this lap: `#print axioms davies_kakeya_2d = [propext, Classical.choice, Quot.sound,
kakeya_subresolution_content]`. Build green (8298 jobs).

## 🧘 This was a deep-reflection lap — read the synthesis first
- **STATUS.md fully refreshed** (header, where-it-stands, dated bullet, axiom ledger re-run from the
  REAL `#print axioms`, doctrine applied). STATUS had drifted to the stale axiom name
  `kakeya_dominant_scale_count` — corrected to `kakeya_subresolution_content`.
- **`PENDING_WORK.md` → `## 🧘 Reflection — 2026-06-19`** (top of file): the direction call, KEEP/STOP
  lists, and the single highest-value next target with reasoning. Read this.

**Direction call (CONFIRMED + recalibrated):** keep the Córdoba route (don't pivot to Davies' duality
and throw away 95%-built machinery). **Honesty finding:** the live axiom is instantiated at `J=1` in
`Engine.kakeya_hausdorffContentBound`, so Case A (`j=0`) is near-vacuous and the axiom carries
essentially the *whole* lower bound for any realistic fine cover — the "strictly weaker residual"
framing was over-optimistic, and the bespoke statement's truth is hard to audit. The discrete net route
provably *cannot* escape Case B (any cover by sub-resolution pieces triggers it for every fixed `J`).
**So the honest narrowing = the measurable-selection route**, replacing the murky residual with the
clean, citable **Jankov–von Neumann measurable selection**.

## ✅ Concrete proof progress this lap — ALL mathematical pieces of (W) now PROVEN
Three new axiom-clean lemmas in `MeasurableRoute.lean` (all `#print axioms = [propext,
Classical.choice, Quot.sound]`); full library green (8298 jobs) at each commit:
- `exists_continuum_caseA_numerator` (commit `f57d64c`) — the core glue (below).
- `volume_coveredFiber_subsingleton_zero` + `volume_coveredFiber_biUnion_subsingleton_zero`
  (commit `76db3db`) — zero-`ediam` negligibility, discharging the one real subtlety of the `j=0`
  fiber (a subsingleton piece meets the segment in ≤ one `t`, so a countable family of them carries
  zero covered length ⟹ droppable from the fiber fed to `caseA_content`).

**So (W) is now pure assembly — every brick it needs is proven.** What remains is mechanical:
mirror `Engine.kakeya_hausdorffContentBound`'s closed-piece reduction + finite/infinite-fiber split,
but at the uncapped scale, dropping the Case-B branch.

`exists_continuum_caseA_numerator` (the core glue) composes the two proven spine lemmas into *exactly*
the `hnum` numerator `caseA_content` consumes:
```
exists_continuum_caseA_numerator {a : ℝ → Plane} (ha : Measurable a)
    {C : ℕ → Set Plane} (hC : ∀ n, MeasurableSet (C n)) (g : ℕ → ℕ)
    (hcov : ∀ θ ∈ Icc 0 1, Icc 0 1 ⊆ {t | a θ + t • dir θ ∈ ⋃ n, C n}) :
  ∃ (j : ℕ) (α : ℝ),
    ofReal (1/((j+1)*(j+2)))
      ≤ ∑ i ∈ range (2^j), ofReal (2*(1/2)^j)
          * volume {t | t ∈ Icc 0 1 ∧ a (α+i*(1/2)^j) + t • dir (α+i*(1/2)^j) ∈ ⋃ n ∈ g⁻¹'{j}, C n}
```
The continuum→discrete bridge is now CLOSED. Cap-free ⟹ genuine scale-`j` window ⟹ no Case B.

## 🎬 Next action: finish (W) = `kakeya_hausdorffContentBound_of_measurableSelection`
**⚠️ ARCHITECTURE — put the wiring in a NEW file `Kakeya2D/Wiring.lean` importing BOTH `Engine`
(for `content_ratio_lower`, `Plane`, `HausdorffContentBound`) and `MeasurableRoute` (the spine +
glue + negligibility), and wire it into `LeanFormalizations.lean`.** Do NOT add `import ...Engine`
to `MeasurableRoute.lean`: the eventual headline-switch needs Engine-level code to CALL the wiring, so
`MeasurableRoute → Engine` would force a cycle when that happens. `Wiring → {Engine, MeasurableRoute}`
(both → NetThinning) is acyclic and future-proof. State there:
```
theorem kakeya_hausdorffContentBound_of_measurableSelection
    {S : Set Plane} {d : ℝ} (hd0 : 0 < d) (hd2 : d < 2)
    (hsel : ∀ (C : ℕ → Set Plane), (∀ n, MeasurableSet (C n)) → S ⊆ ⋃ n, C n →
        ∃ a : ℝ → Plane, Measurable a ∧
          ∀ θ ∈ Set.Icc (0:ℝ) 1, Set.Icc (0:ℝ) 1 ⊆ {t | a θ + t • dir θ ∈ ⋃ n, C n}) :
    HausdorffContentBound S d
```
**Proof skeleton (copy `Engine.kakeya_hausdorffContentBound`, drop the cap + Case-B branch):**
1. `obtain ⟨cR, hcRpos, hcR⟩ := content_ratio_lower hd0 hd2`; `refine ⟨1, zero_lt_one, D⁻¹*ofReal cR, …⟩`.
2. `intro t hcov hdiam`; closed pieces `U n = closure (t n)` (verbatim Engine 231–240: `hUmeas`,
   `hUcov : S ⊆ ⋃ U n`, `hUdiam`, rewrite `∑' ediam(t n)^d = ∑' ediam(U n)^d`).
3. `obtain ⟨a, ha, hcov_arc⟩ := hsel U hUmeas hUcov`.
4. `g n = if 0 < (ediam (U n)).toReal then dyadicIdx (ediam (U n)).toReal else 0` (UNCAPPED).
5. `obtain ⟨j, α, hnum⟩ := exists_continuum_caseA_numerator ha hUmeas g hcov_arc`.
6. Feed `caseA_content hd0.le hcRpos hcR j α (fun i => a (α+i*(1/2)^j)) A …` where
   `A i = {t | t ∈ Icc 0 1 ∧ a(α+i*(1/2)^j) + t • dir(α+i*(1/2)^j) ∈ ⋃ n ∈ g⁻¹'{j}, C n}` (= hnum's
   sets). `hscov` is automatic (membership defn). `hAmeas`/`hA01` easy. `hnum` is in hand.

**⚠️ THE one real subtlety (the zero-`ediam` / `j=0` window):** `caseA_content` requires
`hediam_lo : ∀ n ∈ s, 2⁻⁽ʲ⁺¹⁾ ≤ ediam(U n)` on a FINITE fiber `s`. With uncapped `g`:
- For `j ≥ 1`: fiber `{g n = j}` = pieces with `0 < ediam` and `dyadicIdx = j` ⟹ genuine window
  `2⁻⁽ʲ⁺¹⁾ < ediam ≤ 2⁻ʲ` holds cleanly (`dyadicIdx_window`, as Engine's `hwin` 323–338). Plus the
  finite/infinite-fiber split (Engine 339–362: infinite ⟹ `∑' ediam^d = ⊤`).
- For `j = 0`: fiber `{g n = 0}` mixes good pieces (`ediam ∈ (1/2,1]`) AND **zero-`ediam` point
  pieces** (the `else 0` branch), which violate `1/2 ≤ ediam`. **Resolution (helper now PROVEN this
  lap):** use `volume_coveredFiber_biUnion_subsingleton_zero` (`dir θ ≠ 0` from `norm_dir`/`‖dir θ‖=1`)
  to show the zero-`ediam` pieces carry zero covered length. Set `Z = {n ∈ g⁻¹'{j} : ediam(U n)=0}`
  (each `U n` for `n∈Z` is a subsingleton via `EMetric.diam_eq_zero_iff`); then for each direction the
  covered set over `Z` is null, so `vol(Aᵢ) = vol(Aᵢ ∩ {covered by g n=j ∧ ediam>0})` (drop a null set
  via `measure_diff_null`/`measure_union_null`). Run `caseA_content` on the FINITE
  `s = {n : g n = j ∧ 0 < ediam(U n)}` (finite by the infinite-fiber `∑'=⊤` escape as in Engine), whose
  window holds cleanly, with `hnum` transported by the `vol(Aᵢ)` equality.

The `j=0` window is now dischargeable and (W) closes with NO new axioms — all bricks are proven. Then **switch the headline:** add a clean `axiom kakeya_measurable_selection`
(Jankov–von Neumann), instantiate `hsel`, and route `two_le_dimH` through
`kakeya_hausdorffContentBound_of_measurableSelection` — retiring `kakeya_subresolution_content`.

## (S) The deep crux (subsequent laps)
Discharge `kakeya_measurable_selection` itself: Jankov–von Neumann / KRN for the closed-valued
`B(θ)={a : segment(a,θ)⊆E}`, or a bespoke argmin-‖a‖ selection. mathlib has NO measurable-selection
infra (`SetTheory/Descriptive` = just `Tree.lean`). Reference-gated (`ON-LINE-REQUEST.md` UPDATE 5
ask 1; ask 4 = Davies' duality as a possible bypass). Keep banging across laps; "needs deep machinery"
is a hypothesis to test, not a verdict — a bespoke argmin may sidestep full KRN.

## ⚠️ Gotchas (carried)
- Do NOT relitigate the fixed-net L² sum for `d>1` (proven dead end — `CASE_B_ANALYSIS.md` §C1).
- `one_ne_top` → `ENNReal.one_ne_top` (namespaced). `push_neg` + `mul_le_mul_left'` deprecation
  warnings are cosmetic/repo-wide — don't churn.
- `set f`/`set F` will NOT fold applied/beta-reduced occurrences in a pre-existing goal; in
  `exists_continuum_caseA_numerator` I avoided `set` and wrote explicit volume terms — do the same.
- Pre-existing dormant `sorry` at `Logic/FastGrowing/Basic.lean` (NOT Kakeya — ignore).

## 📁 Key files
- `Kakeya2D/MeasurableRoute.lean` — the proven spine (`measurable_coveredLength`,
  `exists_continuum_dominant_scale`, `exists_shift_ge_integral`) + NEW `exists_continuum_caseA_numerator`.
- `Kakeya2D/CASE_B_ANALYSIS.md` — the crux analysis (why the continuum route; the honest route).
- `src/.../Engine.lean` — `kakeya_subresolution_content` (live axiom) + `kakeya_hausdorffContentBound`
  (the discrete assembly = the verbatim template for (W)'s closed-piece reduction + fiber split).
- `NetThinning.lean` — `caseA_content`, `dyadicIdx`, `dyadicIdx_window`.
- `STATUS.md` (full ledger) · `PENDING_WORK.md` §Reflection + §A0‴.

---
**→ Next session: start at "Next action" — prove the zero-`ediam` negligibility helper, then assemble
`kakeya_hausdorffContentBound_of_measurableSelection` around `exists_continuum_caseA_numerator`. All
other pieces are PROVEN. Do NOT relitigate the fixed-net L² sum. The headline stays on
`kakeya_subresolution_content` until (W)+(S) land — do not delete the discrete route.**
