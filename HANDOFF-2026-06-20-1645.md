# Handoff: NTL/HJSW — completion re-verified, treadmill STOP

**Date**: 2026-06-20 16:45 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `ef484a0`

## 🎯 What we're doing
Driving the `no-three-in-line` / HJSW formalization to its headline theorems. As of this
session the **project is COMPLETE and in finish-and-stop mode** — the operator's named focus
for the session (the mathlib v4.29→v4.31 upgrade + **de-vendor** of the PrimeNumberTheoremAnd
tower) is done. This lap was a ground-truth re-verification, not new proof work, and the
treadmill governor issued a **STOP (no relaunch)**.

## 🧠 Context to carry forward
- **Finish-and-stop is in force** (Trevor's directive, recorded in `STATUS.md` / auto-memory
  `finish-and-stop.md`). The headlines are proven and axiom-clean. The charter for this mode is
  explicit: **do NOT manufacture side quests** to stay busy. No redundant second proofs, no
  adjacent bonus problems, no new threads.
- The last ~5 commits are all "re-verify / self-stop" docs. **Do not add a 6th.** Re-grounding
  from real output is worth doing *once when you arrive* (which I did); committing another
  re-verification doc is progress-theatre, not progress. The tree is already clean.
- **The two `sorry`s in `PrimeNumberTheoremAnd/Wiener.lean:323,342` are a non-issue.** They live
  in the *external dependency*, not our `src/`. The authoritative check — real `#print axioms
  weakPNT` — comes back `[propext, Classical.choice, Quot.sound]` with **no `sorryAx`**, which
  proves no headline path transitively touches those declarations. Don't chase them: discharging
  upstream dep sorries that gate nothing here is exactly the side-quest finish-and-stop forbids.
- The de-vendor is **genuinely real**, not just claimed: `lakefile.toml` has PNTAnd as a git
  `require` (`kim-em/PrimeNumberTheoremAnd@40e5246b`), no vendored tower remains in the tree, and
  the manifest + toolchain are both on v4.31.0.

## ✅ State (all observed from real output this session)
- `lake build` → **green, 8621 jobs**. Only deprecation/unused-var lint warnings.
- Real `#print axioms` on the 5 load-bearing headlines —
  `maxNoThreeInLine_ge_three_halves_sub`, `hjsw_lower_bound`, `weakPNT`,
  `mertens_third_classical_eGamma`, `prelim_decay_2_route_b` — **all `[propext,
  Classical.choice, Quot.sound]`**.
- `src/` is **sorry-free** (the only build `sorry` warnings are the two upstream Wiener.lean ones,
  off all headline paths).
- Git tree **clean**; nothing to commit. No new proof work started this lap.

## 🎬 Next actions
1. **Do nothing / stay stopped** unless Trevor lifts finish-and-stop. The treadmill will not
   relaunch; this is a terminal checkpoint.
2. **If (and only if) finish-and-stop is lifted**: the one real multi-lap target is
   `PENDING_WORK.md` **item 1 — the sharp `2π|u|` BV-Fourier wall** (route (c)). It needs a
   mathlib-absent signed-BV integration-by-parts lemma; it gates **no** headline (the non-sharp
   `4|u|` route-(b) bonus is already in `src/`, axiom-clean). Start by reading `PENDING_WORK.md`
   item 1 and `src/LeanFormalizations/RealAnalysis/BVFourierDecay.lean`.
3. If asked to re-confirm completion, the verification recipe is in "State" above — build +
   `#print axioms` on the `.bump-axioms` list. Don't re-commit the result.

## ⚠️ Gotchas
- **Build perf (from memory `v431-devendor-build.md`):** a first-from-source full build can hit
  macOS-side FD exhaustion (v4.31's 3-file olean split × parallel heavy leaves on the bind mount).
  If it happens, build modules sequentially to cache, then a full `lake build` replays clean.
  There is **no `-j` flag** in this lake. (Didn't recur this lap — mathlib was prebuilt.)
- The `grep -rn 'sorry' src/` hits are all inside **docstrings** (text like "no `sorry`"), not
  terms. The decisive sorry check is the build's `declaration uses 'sorry'` warnings, not grep.

## 📁 Key files
- `STATUS.md` — living overview + axiom ledger; the finish-and-stop directive.
- `PENDING_WORK.md` — item 1 = the only real open (off-headline, gated) target.
- `.bump-axioms` — the 14 headline theorems whose `#print axioms` footprint is the faithfulness gate.
- `lakefile.toml` — proof the de-vendor is real (PNTAnd git require).
- `src/LeanFormalizations/RealAnalysis/BVFourierDecay.lean` — the sharp-`2π|u|` wall lives here.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. The
project is COMPLETE and self-stopped: unless Trevor has explicitly lifted finish-and-stop, the
correct action is to confirm the state holds (build + `#print axioms`, do NOT re-commit it) and
stay stopped. Only if finish-and-stop is lifted, pick up at Next action #2 (sharp `2π|u|`).**
