# Handoff: Davies planar Kakeya — the WHOLE theorem now reduces to ONE crisp axiom (no sorry)

**Date**: 2026-06-19 08:00 UTC · **Branch**: `kakeya-davies` · **HEAD**: `c982e9a`

## 🎯 What we're doing
Prove `davies_kakeya_2d : KakeyaSetConjectureDim 2` (every planar Kakeya set has `dimH = 2`).
**This lap's win:** the monolithic `sorry` in `kakeya_hausdorffContentBound` is GONE. The entire
lower bound is now machine-checked **down to a single combinatorial axiom**:
`#print axioms davies_kakeya_2d = [propext, Classical.choice, Quot.sound, kakeya_dominant_scale_count]`
— no `sorryAx`. **The remaining work is to discharge that one axiom.** Stay in `Kakeya2D/` only.

## 🧠 Context to carry forward (the mental model — read this)
The crux was always the **net-scale circularity**: the single-scale Córdoba count needs the direction
net `δ*`-separated with `N ≈ 1/δ*` at the *dominant piece scale* `δ* = 2⁻ʲ*`, but `j*` is the
pigeonhole's OUTPUT. This lap I (a) **confirmed by re-derivation** that no fixed net works — both the
naive Cauchy–Schwarz sum-over-scales AND the full-net LP minimisation provably **diverge to 0**
(adversary concentrates covered length at one huge scale on the coarse directions); the resolution
*must* deliver a `1/poly` fraction of the FULL `2^{j*}`-net covered **at the dominant scale itself**.
(b) Then I **isolated exactly that** as the axiom `kakeya_dominant_scale_count` and **machine-checked
everything downstream of it.**

Key realisation that made the assembly close: `cover_count_lower`/`cover_content_per_scale` already
handle "only a fraction of directions covered" by taking `A k = ∅` for the uncovered ones (empty set
is measurable, `⊆[0,1]`, contributes 0, `hcov` vacuous). So the axiom only needs to assert the
*aggregate* covered-length numerator `∑ₖ 2δ·vol(A k) ≥ 1/((j+1)(j+2))` — no per-direction bookkeeping.

The arithmetic that closes it (all proven): at `δ=ρ=(1/2)^j`, `N=2^j`, `η=(1/2)^(j+1)`, the Córdoba
coefficient simplifies to `4·(1/4)^j · 12π(1+j log2)` and the content ratio is `≳ b^j/poly(j)` with
`b = 2^{2-d} > 1` — exponential beats poly, so the infimum over `j` (= the content constant `c`) is
positive. The constant is `c = D⁻¹ · ofReal(cR)` where `D = vol(unit disc)`, `cR` from
`content_ratio_lower`.

**Faithfulness check I did:** the axiom is the genuine TRUE statement (Davies' theorem is true), not a
weakened/smuggled one. The numerator bound `1/((j+1)(j+2))` is exactly what the two pigeonholes
(`exists_dominant_scale`, `exists_global_dominant_scale`) would deliver via `scaleWeight`. So it's
honest debt, not a vacuous axiom. Don't "simplify" the axiom — it's calibrated to what the count needs.

## ✅ State (all verified via real `lake build` this lap)
- Full library **green, 8296 jobs**. 5 commits this lap (`d55dcfa`..`c982e9a`).
- New, all `#print axioms`-clean `[propext, Classical.choice, Quot.sound]`:
  - `Cover.cover_content_per_scale` — Córdoba piece-count ⟹ content contribution (division-free).
  - `Engine.exists_const_mul_pow_le` / `exists_pos_le_pow_div` — exp beats fixed poly (Bernoulli+√b).
  - `Engine.content_ratio_lower` — the assembled real-analysis content constant.
- `Engine.kakeya_dominant_scale_count` — the lone axiom (disclosed, documented).
- `kakeya_hausdorffContentBound` / `two_le_dimH` / `davies_kakeya_2d` — proven from that one axiom.
- Aristotle: submitted then **cancelled** `2f5d30fd` (was `content_ratio_lower`; I proved it locally).
  No job in flight. (Other-project hjsw jobs were running — left untouched.)

## 🎬 Next actions
1. **Harvest first:** check repo root for `ON-LINE-FINDINGS-*.md`; if present, read + act, then
   `git mv` into `archive/findings/`. `ON-LINE-REQUEST.md` UPDATE 2 now contains the axiom **verbatim**
   and the precise ask (the net-thinning + covered-length-retention lemma / the multi-net LP bound).
2. **Attack the axiom (`PENDING_WORK.md §A0` has 3 paths).** Recommended: **decompose-and-narrow** —
   prove the *geometric wiring* of the axiom in Lean from existing bricks (`exists_pullback_cover` per
   net direction → a dyadic scale fn `g` on cover pieces → `exists_global_dominant_scale` to a global
   `j*`), isolating the genuinely-open *thinning* (covered-length retention when passing to the
   `2⁻ʲ*`-net) as a SMALLER axiom. This shrinks the axiom further even if the thinning stays open.
3. The thinning itself is reference-gated; if findings arrive, transcribe Wolff/Mattila's
   dominant-scale lemma. Do NOT re-walk the dead ends (fixed-net Cauchy–Schwarz / naive LP — both
   diverge, confirmed).

## ⚠️ Gotchas
- `rw [div_pow]` over-matches: on a goal with both `(1/P)^2` and `(1/4)^j` it hits `(1/4)^j` first.
  Isolate structural rewrites in a focused `have`. (Flushed to memory.)
- `ℝ≥0∞` `ring` treats `D⁻¹` as an opaque atom and will reassociate it, but does NOT know `D⁻¹·D=1` —
  group via `show ... from by ring` then `ENNReal.inv_mul_cancel hD0 hDtop`. (Flushed to memory.)
- Two harmless warnings in `Engine.lean`: `unused variable hd0` (intentional — axiom is guarded by
  `0<d<2` for faithfulness) and `mul_le_mul_right'` deprecated (cosmetic; the proof step is correct).
- Cover.lean opens `Set MeasureTheory Metric` (NOT Finset/scoped Real): write `Finset.range`,
  `Real.pi` explicitly. CordobaL2 opens `Finset` (bare `range`/`π`). `cover_content_per_scale` lives in
  Cover; `content_ratio_lower` + the assembly + the axiom live in Engine.

## 📁 Key files
- `…/Kakeya2D/Engine.lean` — `kakeya_dominant_scale_count` (axiom), the assembly proof of
  `kakeya_hausdorffContentBound`, `content_ratio_lower`, the constant lemmas.
- `…/Kakeya2D/Cover.lean` — `cover_content_per_scale` + both pigeonholes + `cover_count_lower`.
- `…/Kakeya2D/CordobaL2.lean` — `cordoba_cover_count` (single-scale Córdoba L²).
- `PENDING_WORK.md §A0` (the 3 attack paths), `ON-LINE-REQUEST.md` UPDATE 2 (verbatim axiom + ask).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context above, then start at Next action #1: check for `ON-LINE-FINDINGS-*.md`, then attack the lone
axiom `kakeya_dominant_scale_count` via the decompose-and-narrow path. Everything downstream of it is
already machine-checked — you are discharging one crisp combinatorial obligation, not rebuilding.**
