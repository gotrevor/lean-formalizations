# Handoff: lap 11 — "goodsteinLength GROWS LIKE f_{ε₀}", TWO-SIDED (charter C3 DONE)

**Date**: 2026-06-19 ~14:10 UTC (updated lap-end) · **Branch**: no-three-in-line · **HEAD**: `904f5ea`

> **LAP-END UPDATE.** Beyond the lower bound below, this lap ALSO landed the UPPER bound and the C3
> audit surface, completing the two-sided "grows like `f_{ε₀}`":
> - `hardy_le_fastGrowing` (`Hardy.lean`, axiom-clean): `hardy o n ≤ fastGrowing o n` (n≥2).
> - `goodsteinLength_le_fastGrowing_ordinal` (`GrowthStatement.lean`, FULLY axiom-clean):
>   `goodsteinLength m + 2 ≤ f_{o_m}(2)` — the upper bound, via the Cichoń identity + `hardy_le_fastGrowing`.
> - C3 audit surface `GrowthStatement.lean` + anchor `fastGrowingε₀_eq_towerO`.
> Commits `08afea0`, `9f8cb56`, `904f5ea`. **Next frontier = B4 `H_{ω^α}=f_α`** (see thin `HANDOFF.md`
> + `PENDING_WORK.md`); the lower-bound writeup below remains accurate.
>
> **B4 progress this lap (`dd60893`, `5bf832f`):** mapped the wall by `native_decide` measurement —
> clean form `H_{ω^α}(n)+1 = f_α(n+1)` TRUE at finite α, FALSE at limit α (`H_{ω^ω}(1)+1=8 ≠ f_ω(2)=2048`,
> the `ω[n]=n+1` shift). Banked the ROOT BRICK `hardy_oadd_tail` (`Hardy.lean`, axiom-clean): the
> non-absorbing additive law `hardy (oadd a m b) n = hardy (oadd a m 0) (hardy b n)`. NEXT = the
> coefficient lemma `H_{ω^β·j}=(H_{ω^β})^[j]` (peel one `ω^β` via the brick + `omega_pow` fund seq) →
> finite-α restricted B4. Full derivation in `PENDING_WORK.md`.

## 🎯 What we're doing
Unbounded expedition (`DIRECTION.md`): mathlib-only growth theory behind Kirby–Paris,
"`goodsteinLength` grows like `f_{ε₀}`". Lap 10 closed the diagonal `f_o(m) ≤ goodsteinLength m + 2`
at the individual limit levels `o = ω, ω^j, ω^ω`. **This lap generalized the whole climb and reached
the destination of the LOWER bound: `f_o(m) ≤ goodsteinLength m + 2` for EVERY `o < ε₀`.**

## 🎉 What landed (all in new file `src/LeanFormalizations/Logic/Goodstein/TowerDomination.lean`)
- **`goodsteinLength_eventually_dominates_fastGrowing`** — THE FULL ε₀ HEADLINE:
  `o.NF → ∃ N, ∀ m ≥ N, fastGrowing o m ≤ goodsteinLength m + 2`. Cichoń's diagonal lower bound for
  every ordinal below ε₀, unconditional + machine-checked.
- `fastGrowing_towerO_le_goodsteinLength` — every tower level `ω↑↑k` (`towerO k`, `repr = ω↑↑k`).
- `fastGrowing_le_goodsteinLength_of_repr_le_tower` — any `o` with `repr o ≤ ω↑↑k`.
- `goodsteinLength_dominates_fastGrowing_towerO` — explicit threshold `m ≥ towerN k (2^16+k)`.

## 🧠 The two general engines (THE bootstrap payload — internalize these)
1. **General length bootstrap** `two_mul_le_goodsteinLength_iter (k m)`:
   `goodsteinLength((log₂)^[k] m) ≥ 2m` for ALL k. **KEY INSIGHT that cracked the lap:** the lap-10
   handoff feared this "needs an `f_{ω^ω}`-strength bound at the deep seed" — that was FALSE. The
   already-proved `o=ω` domination (`fastGrowing_omega_le_goodsteinLength`, valid at ANY seed `≥2^16`)
   is strong enough at every depth `k`, because `f_ω(t)=f_{t+1}(t) ≥ f_{k+2}(t)` and `f_{k+2}` already
   beats a `(k+1)`-fold tower. The carrier is the clean finite-level lemma:
   - **`towerN_le_fastGrowing (k)`**: `f_{k+2}(t) ≥ towerN (k+1) (t+1)` for `t≥2` (induction on k via
     `f_{n+1}=(f_n)^[·]`, `(f)^[t]t ≥ (f)^[2]t = f(f t)`, IH twice). `towerN k t` = k-fold `2^·` of `t`.
   - plus `succ_le_towerN_log_iter`: `m+1 ≤ towerN k ((log₂)^[k] m + 1)` (tower upper bound on the seed).
   Chain: `goodsteinLength t ≥ f_ω(t)−2 ≥ f_{k+2}(t)−2 ≥ towerN(k+1)(t+1)−2 ≥ 2^{m+1}−2 ≥ 2m`.
2. **General ordinal bridge** `omegaTower_succ_le_seqONote_repr`:
   `base i ≤ (log_{base i})^[k] (G_i)` ⟹ `ω↑↑(k+1) ≤ (seqONote m i).repr`. Pure `toOrdinal` induction
   `omegaTower_le_toOrdinal` (peel one `Nat.log` per step). `omegaTower k = ω↑↑k` (ordinal),
   `towerO k` its ONote (`repr_towerO`).
3. **Tower cofinality** `exists_repr_lt_omegaTower` (AXIOM-CLEAN): every NF ONote has `repr < ω↑↑k`
   for some k (structural induction; leading term `< ω↑↑(ke+1)` by `mul_lt_omega0_opow`, tail `< ω↑↑ka`,
   both absorbed below the next tower level via `isPrincipal_add_omega0_opow`). This is the piece that
   lifts the tower spine to ALL of ε₀.

The crux at step `i = m−2` is discharged by the self-similarity tower `iterLeadExp_dominates m k i`,
read at the fixed index `i` via the new `logSeq_iterate_apply` (`(logSeq^[k] a) i =
(Nat.log (base i))^[k] (a i)`), then `n_le_goodsteinSeq` with the bootstrap length bound.

## ✅ State (verified this session)
- Full `lake build` 🟢 **8294 jobs**. `src/` sorry-free.
- `#print axioms goodsteinLength_eventually_dominates_fastGrowing` = `[propext, Classical.choice,
  Quot.sound]` + the 12 documented `goodsteinLength_base_cases._native.native_decide.ax_*` (finite
  base-case computation, inherited via the f_ω bootstrap). No sorry. Engines
  (`towerN_le_fastGrowing`, `omegaTower_le_toOrdinal`, `exists_repr_lt_omegaTower`) are trust-base-clean.
- Commits: `4856b9a` (tower spine + engines) → `9b1e779` (full ε₀ headline + cofinality). Docs commit next.

## 🎬 Next actions (the lap-12 frontier — the matching UPPER bound)
The LOWER bound is DONE up to ε₀. "`goodsteinLength` grows *like* `f_{ε₀}`" wants the ceiling
`goodsteinLength m ≤ (f_{ε₀}-flavoured)(m)`. Hardest-first crux = a fast-growing/Hardy UPPER bound.
1. **Route (a): via the Cichoń identity** `goodsteinLength m = H_{o_m}(2) − 2` (C3, axiom-clean), where
   `o_m = seqONote m 0`. Bound `o_m.repr` ABOVE by a tower level (concrete dual of
   `exists_repr_lt_omegaTower`) + Hardy monotonicity in the ordinal index. **Build first:** Hardy
   monotone in its ordinal argument (analog of the existing `fastGrowing` monotonicity) — bounded,
   self-contained, good Aristotle candidate.
2. **Route (b): B4 `H_{ω^α}=f_α`** to convert a Hardy ceiling to a `fastGrowing` ceiling — the
   long-flagged trap under mathlib's `ω[n]=n+1`; lower priority (route (a) up to the Hardy ceiling may
   suffice for a clean "grows like" without B4).
3. Optional: a single clean ε₀ capstone packaging the lower bound as "dominates `f_{ε₀}`" via
   `ε₀ = sup_o repr o` (presentation).

## ⚠️ Gotchas
- `interval_cases` unavailable in this import context; `perl/sed` mangle Lean unicode (use `Edit`).
- `rw [hbase : base i = m]` rewrites the LOG BASES too (breaks the `(Nat.log (base i))^[k]` match) —
  use a `calc` rewriting only the LHS (see `hreg` in `fastGrowing_le_goodsteinLength_of_repr_le_tower`).
- `repr` is ambiguous (`Std.Format.repr` vs `ONote.repr`) — write `e.repr` / `(o).repr`, not `repr e`.
- `Ordinal`'s `add_one_le_iff` is the unqualified `add_one_le_iff` (NoMaxOrder); `x ≤ ω^x` is
  `right_le_opow _ one_lt_omega0`; `Nat.lt_two_pow_self` (not `Nat.lt_two_pow`).
- Editing `DominationBaseCases.lean` re-runs ~5 min of `native_decide`; keep new work in files that
  only *import* it. `TowerDomination.lean` / `GoodsteinLike.lean` are cheap.

## 📁 Key files
- `src/LeanFormalizations/Logic/Goodstein/TowerDomination.lean` — THIS LAP. All the general engines +
  the ε₀ headline. ~330 lines. Imports `DominationOmega`.
- `src/LeanFormalizations/Logic/Goodstein/DominationOmega.lean` — lap-10 per-level closures (now
  subsumed; kept as anti-vacuity witnesses + the `fastGrowing_omega_le_goodsteinLength` the bootstrap
  reuses).
- `src/LeanFormalizations/Logic/Goodstein/GoodsteinLike.lean` — the self-similarity tower abstraction
  (`iterLeadExp_dominates`).
- `src/LeanFormalizations/Logic/FastGrowing/{Basic,Hardy}.lean` — fastGrowing index/arg monotonicity,
  Hardy hierarchy. START HERE for the upper bound (Hardy monotonicity).

---
**→ Next session: the lower bound is COMPLETE to ε₀ — do NOT re-attack it. Pick up the UPPER bound
(Next action #1: Hardy monotonicity in the ordinal arg, then the Cichoń-identity ceiling). This doc +
`STATUS.md` lap-11 entry + `PENDING_WORK.md` top are your starting point.**
