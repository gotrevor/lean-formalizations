# Handoff: no-three-in-line HJSW frontier — arc lemma + certificate done, covering count blocked on paper

**Date**: 2026-06-19 05:30 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `2f5eecb`

## 🎯 What we're doing
Isolated clone `~/src/lean-formalizations-ntl`, branch `ntl-hjsw`. Sole target: the
**Hall–Jackson–Sudbery–Wild `3N/2`** lower bound for the no-three-in-line problem —
`hjsw_lower : 3*(p−1) ≤ maxNoThreeInLine (2*p)` for prime `p` (`Hyperbola.lean`). The
2N upper bound + Erdős Θ(N) parabola lower bound are already done & axiom-clean (don't redo).
Work ONLY in this clone; never touch sibling `~/src/lean-formalizations`.

## 🧠 Context to carry forward
The crux splits into (a) **arc non-collinearity** — DONE this lap — and (b) the **covering
count**, which is the genuinely hard, still-open mile.

- **(a) is proven, axiom-clean** (`hyperbola_noThreeCollinear`): three grid points on one modular
  hyperbola `xy ≡ k (mod p)` (`k ≢ 0`) are non-collinear. The engine is the Vandermonde reduction
  `k·(a−b)(b−c)(c−a) = a·b·c·D` (D = the vanishing determinant), proved by `linear_combination` from
  the three relations `aᵢ·yᵢ = k`; `ZMod p` a domain finishes. This is the hyperbola twin of
  `parabola_noThreeCollinear`.

- **(b) is genuinely blocked on the HJSW 1975 paper.** I do NOT know the actual construction, and I
  *experimentally falsified* the natural guesses (rigorously, via an exact decidable certificate —
  see below). Key results this lap:
  - "3 of 4 rectangle-corners `{r,r+p}×{s,s+p}` per residue, `s=r⁻¹`": works at `p=5` (coincidence),
    but **exhaustively NO** size-18 config at `p=7` over all keys `k` and all `4^6` drop patterns.
  - "three arcs in three `p×p` blocks" (all `k₁,k₂,k₃`; and with reflections): fails at `p=7`.
  - **Decisive:** the `24 = 4(p−1)` lifts of ONE hyperbola `xy≡k` at `p=7` have collinear-triple
    minimum hitting set `≥7` for *every* `k` ⇒ a single curve's lifts max at `≤17 < 18`.
    **⇒ HJSW must combine ≥2 distinct curves.** (Recorded in `ON-LINE-REQUEST.md` + `PLAN.md`.)
  - Note: 18 points cannot fit in one `7×7` block (max there is 2·7=14), so the construction MUST
    spread across the `14×14` grid using lifts — that's why naive single-block ideas can't work.

  The honest read: this needs the paper's exact (multi-)curve construction. I filed a precise
  `ON-LINE-REQUEST.md` (HJSW = J. Combin. Theory Ser. A 18 (1975) 336–341) asking specifically which
  curves/keys/lifts it combines and how cross-curve non-collinearity is proven.

- **The certificate is the reusable lever**: `decNoThree s ↔ NoThreeCollinear s` (exact iff, both
  directions of `det3 = 0 ⟺ Collinear ℝ`), so `native_decide` settles any concrete config. Use it to
  validate any candidate construction computationally BEFORE attempting a Lean proof.

## ✅ State (all verified this session via real `lake build` / `#print axioms`)
- `lake build` green (8285 jobs). `src/` has exactly one disclosed sorry: `hjsw_lower`.
- Axiom-clean (`[propext, Classical.choice, Quot.sound]`): `hyperbola_noThreeCollinear`,
  `hyperbola_card`, `hyperbola_grid`, `collinear_of_det3_zero`, `decNoThree_iff`, `decNoThree_imp`.
- `hjsw_lower_five` (= `3·(5−1) ≤ maxNoThreeInLine 10`, the verified p=5 instance): carries only the
  `native_decide` axiom, intentionally OFF the headline path.
- Commits this lap: `a59baae` (arc lemma) → `75759a7` (certificate+witness) → `eb1e1d3` (iff +
  Decidable) → `946b9e2`/`63002f4`/`2f5eecb` (docs + research findings). Never pushed (host pushes).

## 🎬 Next actions
1. **Check for `ON-LINE-FINDINGS-*.md` at repo root.** If the HJSW construction arrived, act on it:
   build the candidate set, validate with `native_decide (decNoThree …)` for `p=5,7,11`, then port to
   a clean general `def` and prove `hjsw_lower` (reuse `hyperbola_noThreeCollinear` for intra-curve
   triples; the new work is the cross-curve collinearity argument). `git mv` the findings doc to
   `archive/findings/` once harvested.
2. If no findings yet: the request is filed — don't re-derive. Make ANOTHER advance on the crux:
   e.g. computationally search the **two-/three-curve UNION** lift pool (`p=7`, all key pairs) with
   the certificate for an 18-point no-three subset; a hit reveals the multi-curve structure to prove.
3. Aristotle: a FastGrowing-project job was RUNNING all lap (not mine) — I did NOT contend. Once it's
   IDLE and you have a *bounded, self-contained* construction lemma (post-findings), submit that.

## ⚠️ Gotchas
- `hjsw_lower`'s sorry is allowed by the armed `--allow-stop` gate; the pre-commit `lake build` passes
  with it. Don't "fix" the gate.
- `native_decide` on `decNoThree`/`NoThreeCollinear` is fine for ANCHORS only — never let it leak into
  the general `hjsw_lower` proof (that must be a `Collinear ℝ` argument).
- Two Lean import/`set` gotchas hit this lap are flushed to memory (see report).

## 📁 Key files
- `…/NoThreeInLine/Hyperbola.lean` — `hyperbola_noThreeCollinear` (done) + `hjsw_lower` (sorry).
- `…/NoThreeInLine/Anchors.lean` — `decNoThree`/`decNoThree_iff`/`Decidable NoThreeCollinear`,
  `det3_toReal_eq_detZ`, `witness5`/`hjsw_lower_five`. The experimental + anti-vacuity surface.
- `…/NoThreeInLine/Collinearity.lean` — both bridge directions (`collinear_{imp,of}_det3_zero`).
- `…/NoThreeInLine/PLAN.md` — frontier + full experimental status.
- `ON-LINE-REQUEST.md` (repo root) — the precise paper request; the bottleneck for `hjsw_lower`.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other KB projects — this doc IS the chosen thread. Start at Next
action #1: `ls` the repo root for `ON-LINE-FINDINGS-*.md` and act on it; if none, do #2 (push the
crux further with the certificate), keeping `hjsw_lower` the focus.**
