# Handoff: lap 9 — finite-level diagonal domination CLOSED; `o=ω` frontier opened

**Date**: 2026-06-19 12:15 UTC · **Branch**: no-three-in-line · **HEAD**: `c3c2197`

## 🎯 What we're doing
Unbounded expedition (see `DIRECTION.md`): build the mathlib-only growth theory behind
Kirby–Paris — "`goodsteinLength` grows like `f_{ε₀}`". For **8 laps** the one open headline was the
*diagonal domination* `f_o(m) ≤ goodsteinLength m + 2` (Cichoń's lower bound, "sub-fact (ii)"). **This
lap closed it for every finite `o`** and framed the next tier (`o=ω`).

## 🧠 Context to carry forward (the bootstrap payload)
- **The winning idea was SELF-SIMILARITY** — the thing 8 laps missed. The leading-exponent sequence
  `L_k = log_{base k}(G_k)` is *itself* a Goodstein descent (`L_{k+1} ≥ bump(base k) L_k − 1`), so it
  **dominates the Goodstein sequence seeded at `log₂ m`** (`leadExp_ge_goodsteinSeq_log`). This turns
  "leadExp `≥ n` for `m` steps" into a length bound one scale down, which a **strong induction**
  bootstraps (the exponential length bound `goodsteinLength m ≥ 2^{m+1}+m` reproduces itself at each
  scale). The earlier `ppCount` "sparsity bound" route (lap-8 baton) is **ABANDONED/superseded** — do
  not reopen it. The `ppCount` lemmas remain in `Domination.lean` but are off the path.
- **The strong induction bottoms out at finite computational base cases** `goodsteinLength M ≥
  2^{M+1}+M` for `4≤M<16`. Naive `∀ n<N, goodsteinSeq M n ≠ 0` STACK-OVERFLOWS (goodsteinSeq recurses
  depth N; N up to 65551). Fix: a **tail-recursive forward evaluator `gpos`** (recursion on a fuel
  arg, in tail position of `&&` → compiles to a loop) + soundness `gpos_goodstein`/`glen_ge_of_gpos`.
  `native_decide` on `gpos` handles M=15 (65551 steps) in ~3.5 min. These live in
  `DominationBaseCases.lean` (isolated so editing it costs a ~5-min rebuild — avoid touching it).
- **Axiom hygiene split**: the math engine (`goodsteinLength_exp_lower`, all `*_of_log_length`
  reductions, self-similarity) is `[propext, Classical.choice, Quot.sound]`. The *unconditional*
  closures carry `Lean.ofReduceBool` (from the finite base-case `native_decide`). This is the honest,
  deliberate split — documented in the file headers.
- **`o=ω` is genuinely the next tier and is multi-lap.** For finite `o=n` we need `leadExp_j ≥ n` at
  `j≈m` (a constant). For `o=ω` we need `leadExp_j ≥ base j = m` — the leading exponent must stay in
  the **LARGE regime** for `~m` steps. Via self-similarity that needs the *one-level-down value*
  `goodsteinSeq(log₂ m) k ≥ base k` at `k≈m`, i.e. the lower sequence still in ITS large regime — one
  more recursion of the same idea (likely a doubly-iterated length bound). See `PENDING_WORK.md` →
  "NEXT FRONTIER" for three attack paths; route (a) (iterate self-similarity) reuses every lap-9 brick.
- **⚠️ perl-on-UTF8 footgun (cost me a recovery):** `perl -0pi -e` mangles Lean's unicode
  (`ℕ,≤,ω,→`) — double-encodes UTF-8. NEVER use perl/sed byte-substitution on Lean files; use the
  `Edit` tool. Recovery if it happens: `git checkout <commit> -- <file>` then re-apply via `Edit`.

## ✅ State (all verified this session)
- Full `lake build` 🟢 **8292 jobs** (last run after `c3c2197`). `src/` sorry-free.
- **`fastGrowing_ofNat_le_goodsteinLength`** (`DominationBaseCases.lean`): `16≤m → n+1≤log₂m →
  f_n(m) ≤ goodsteinLength m + 2`. The finite diagonal, machine-checked. Plus qualitative
  `goodsteinLength_dominates_fastGrowing_ofNat` (`DominationCorollary.lean`): `∀n ∃N ∀m≥N …`.
- **`fastGrowing_omega_le_goodsteinLength_of_largeRegime`** (`DominationOmega.lean`): the `o=ω`
  diagonal reduced to one open hypothesis `hreg : base(m-2) ≤ leadExp_{m-2}`. Axiom-clean.
- New engine in `Domination.lean` (all axiom-clean): `bump_mono`, `leadExp_step_ge`,
  `leadExp_ge_goodsteinSeq_log`, `two_le_goodsteinSeq`, `goodsteinSeq_small_step`,
  `goodsteinLength_le_of_small`, `n_le_goodsteinSeq`, `n_le_leadExp_of_log_length`,
  `exp_le_goodsteinLength_step`, `goodsteinLength_exp_lower`, `gpos`+soundness.
- Commits this lap: `c2e15cb` (self-similarity), `da05776` (finite-o CLOSED), `9b186a8` (corollary),
  `c3c2197` (o=ω framing). Docs updated: `STATUS.md`, `PENDING_WORK.md`.

## 🎬 Next actions
1. **Attack `o=ω` via route (a)** — discharge `hreg : base(m-2) ≤ leadExp_{m-2}` (large regime
   sustained ~m steps). Concretely: strengthen the self-similarity so the *value* of
   `goodsteinSeq(log₂ m)` stays `≥ base` at step `m-2`. This needs a length-type bound on
   `log₂ log₂ m` — likely an `ω`-level analog of `goodsteinLength_exp_lower` (doubly-iterated). Start
   by prototyping in a scratch file `import …Domination`, then port. `DominationOmega.lean` already
   has the `ω^ω` bridge wired — you only need to supply `hreg`.
2. If route (a) stalls after honest effort, try route (c): bound `goodsteinLength m` below via the
   **Cichoń identity** (`goodsteinLength m = H_{seqONote m 0}(2) − 2`, already proved) + a Hardy
   `H_{ω^ω}` lower bound. Cross-check the `Logic/FastGrowing/Hardy` API.
3. Optional polish (low priority): the finite closures require `m≥16`; the tail `4≤m<16` holds but its
   direct `f_n` certification is expensive (skip — asymptotic domination is the meaningful statement).

## ⚠️ Gotchas
- `interval_cases` is **NOT available** in the Goodstein import context ("unknown tactic"). Use an
  `omega`-proved disjunction + `rcases … <;> subst <;> …` (see `goodsteinLength_base_cases`).
- Editing `DominationBaseCases.lean` re-runs all 12 `native_decide`s (~5 min). Keep new work in
  separate files importing `Domination` (cheap). That's why `o=ω` lives in its own file.
- `native_decide` results are cached in oleans; the pre-commit green-gate is fast unless a Goodstein
  file changed.

## 📁 Key files
- `src/LeanFormalizations/Logic/Goodstein/Domination.lean` — the engine (self-similarity, exp-lower
  induction, `gpos`, general-`o` reductions). ~1290 lines.
- `…/DominationBaseCases.lean` — finite base cases + unconditional finite-`o` closures (`native_decide`).
- `…/DominationCorollary.lean` — qualitative "eventually dominates every f_n".
- `…/DominationOmega.lean` — `ω^ω` bridge + `o=ω` crux isolation (next-lap entry point).
- `PENDING_WORK.md` (top section) — lap-9 summary + `o=ω` frontier with 3 attack paths.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context above, then pick up at Next action #1: attack the `o=ω` crux `hreg` (large regime sustained
~m steps) via iterated self-similarity, prototyping in a scratch file against the cached `Domination`.**
