# Handoff: Mertens' First Theorem added to the now-unlocked PNT layer

**Date**: 2026-06-19 12:10 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `0605833`

## 🎯 What we're doing
Isolated clone for the no-three-in-line / HJSW frontier. The flagship
`maxNoThreeInLine_ge_three_halves_sub` (HJSW-optimal `3/2 − o(N)`) is **DONE and axiom-clean** — it
rests on `weakPNT`, which a prior lap discharged by porting PrimeNumberTheoremAnd's Wiener–Ikehara
tower. This lap I **extended the now-unlocked Chebyshev/PNT layer** with **Mertens' First Theorem**
(absent from mathlib), since the three remaining open `sorry`s are all at genuine multi-lap walls
(see below) with Aristotle already engaged on the most tractable.

## 🧠 Context to carry forward
- **Why I didn't grind the open sorries this lap.** All three are real walls, not tractable leaves:
  - `nagura_prime` (`PrimeGap.lean:1119`): prime in `(n, 6n/5]` for `n≥25`. I pinned its wall as
    **FINAL for elementary methods** this lap: the refined constant
    `A = (7/15)log2+(3/10)log3+(1/6)log5` is **exactly** the Chebyshev constant `≈0.921292`
    (`= (1/2)log2+(1/3)log3+(1/5)log5−(1/30)log30`, verified algebraically). So `theta_refined_lower`
    (const `A`) and `psi_refined_upper` (const `(6/5)A`) are matched at `(6/5)A` with **zero slack** —
    a prime in `(n,6n/5]` needs `θ(6n/5)−θ(n)>0`, whose leading order is `(6/5)A·n − (6/5)A·n = 0`.
    No elementary push exists. Routes: (i) explicit-error PNT (`θ(x)≥(1−ε)x` for an *explicit* `X(ε)`;
    our in-repo `θ∼x` is only qualitative ⇒ non-constructive threshold ⇒ the small-case ladder can't
    be bounded), or (ii) Nagura's 1952 finite inequality (needs the paper). **Superseded** by the
    unconditional `3/2−o(N)`; lowest value. Do NOT retry the same elementary attack.
  - `prelim_decay_2`/`_3` (`Wiener.lean:242/249`): sharp BV-Fourier decay `≤ TV/(2π|u|)`. **Dead code**
    (`#print axioms WeakPNT''` clean — gates nothing). Needs a Lebesgue–Stieltjes IBP for BV that
    mathlib lacks; the `TV/(4|u|)` shift-trick route gives the wrong constant. **Aristotle job
    `c6d615ee` is RUNNING on `prelim_decay_2`** (~20% last I looked) — leave it; if it returns a real
    proof, verify in-kernel + `#print axioms` then port onto `Wiener.lean:242`.
- **Mertens proof idea (reusable):** the repo keystone `sum_vonMangoldt_mul_floor_div`
  (`∑_{d≤N}Λ(d)⌊N/d⌋ = log(N!)`, in `PrimeGap.lean`) gives the sandwich
  `0 ≤ N·S(N) − log(N!) ≤ ψ(N)` where `S(N)=∑Λ(d)/d` (the fractional parts `{N/d}∈[0,1)` are weighted
  by `Λ≥0`). Stirling (`Stirling.le_log_factorial_stirling` + repo `log_factorial_le`) pins `log(N!)`
  and `Chebyshev.psi_le_const_mul_self` (`ψ≤(log4+4)x`) pins the error ⇒ `S(N)=log N+O(1)`.

## ✅ State (all observed this lap)
- `lake build` **GREEN, 8296 jobs** — full build passed on every commit (pre-commit green-gate too).
- **New module `NumberTheory/PrimeNumberTheorem/Mertens.lean`**, all `#print axioms`-clean
  (`[propext, Classical.choice, Quot.sound]`):
  - `abs_vonMangoldtSumDiv_sub_log_le` : `∀ N≥1, |∑_{d≤N}Λ(d)/d − log N| ≤ log 4 + 5` (explicit).
  - `mertens_first` : `(∑_{d≤N}Λ(d)/d − log N) =O[atTop] 1`.
  - `vonMangoldtSumDiv_tendsto_atTop` : the sum `→ ∞` (quantitative infinitude of primes).
  - `summable_log_div_sq` : `Summable (log n / n²)` (+ helper `log_le_two_mul_sqrt`).
- 6 commits: `c5c015a` (Mertens core), `7915a2f` (divergence + recipe), `5803ffa` (docs/harvest),
  `bc4f2d9` (summability helpers), `0605833` (docstring). Harvested both 2026-06-19 findings docs into
  `archive/findings/`. Memory (`weakpnt-discharge-map.md`, `MEMORY.md`) updated.

## 🎬 Next actions
1. **Finish the prime form `∑_{p≤x}(log p)/p = log x + O(1)`** — the recognizable Mertens, now
   substantially de-risked. Recipe (also in `Mertens.lean`'s closing docstring): bound the
   proper-prime-power tail `tail(N)=∑_{p^k≤N,k≥2}(log p)/p^k` by the convergent majorant
   `2·∑_b(log b)/b²` — **`summable_log_div_sq` already secures that majorant**. What remains is purely
   the *regrouping* (~80 lines): inject each proper prime power `d ↦ (minFac d, exponent)` into
   `Icc 2 N ×ˢ Icc 2 N` (use `isPrimePow_nat_iff` for `∃ p k`, `Nat.factorization`/`minFac` to extract),
   `Finset.sum_image` for the reindex, per-base geometric `∑_{k≥2}(log p)/p^k ≤ 2(log p)/p²` via
   `sum_le_tsum` of `∑_k p^{-k}`. Then `P(N)=vonMangoldtSumDiv N − tail(N)` and `mertens_first` give it.
   NOTE: no pointwise majorant over all `n` works — the tail converges only by prime-power sparsity
   while `∑Λ(n)/n` itself diverges; the regroup is essential. (Abel-summation via in-repo `θ∼x` is a
   comparable-effort alternative.)
2. **Poll Aristotle `c6d615ee`** (`aristotle show c6d615ee-…`, the one-shot not the live TUI). If it
   returned a real `prelim_decay_2` proof: verify + `#print axioms` + port. Else leave it; whenever it
   goes idle AND a genuinely-open lemma exists, architect the next bounded one and submit.
3. (Lower value) Mertens' second/third theorems build on the prime form; only after #1.

## ⚠️ Gotchas
- The two `Wiener.lean` `sorry` warnings + many pre-existing lint warnings (push_neg deprecation, unused
  vars) are EXPECTED/HARMLESS — look for "Build completed successfully". `#print axioms` on any real
  target is clean.
- Lean: `Summable.of_nonneg_of_le` won't unify under `apply` with a named `g` — pass the `Summable g`
  hypothesis explicitly via `refine … ?_ ?_ hg`. `summable_one_div_nat_rpow` is in the **`Real`**
  namespace. To fold `a*b⁻¹ → a/b` use `← div_eq_mul_inv`.
- mathlib pin `v4.29.1` still has **no native PNT** (re-verified this lap: no `WienerIkehara`/
  `prime_number_theorem`). Can't bump on-box (no egress). The "swap port for citation" quick-win in
  `PENDING_WORK.md` stays a host-session task; nothing to swap yet.

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/Mertens.lean` — this lap's deliverable.
- `src/LeanFormalizations/Combinatorics/NoThreeInLine/PrimeGap.lean` — keystone identity (`:226`),
  refined Chebyshev bounds, `nagura_prime` (`:1119`), flagship (`:1278`).
- `PENDING_WORK.md` — top section updated with Mertens + nagura-wall-final.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this clone IS the chosen thread. Absorb the
context, then start at Next action #1: finish the prime form of Mertens (majorant already secured by
`summable_log_div_sq` — only the prime-power regrouping remains).**
