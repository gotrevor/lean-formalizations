# Handoff: the classical MERTENS TRILOGY is in (1st & 2nd full, 3rd up to the constant)

**Date**: 2026-06-19 13:42 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `f9b15ab` (+docs)

## 🎯 This lap (review lap → 21 commits, all green + axiom-clean)
Corrected stale `weakPNT`-as-axiom docs (it was discharged a prior lap → flagship `3/2−o(N)` is
unconditional & axiom-clean), then drove the unlocked PNT layer to the **classical Mertens trilogy**, all in
`src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/Mertens.lean`, all `[propext, Classical.choice,
Quot.sound]`, all mathlib-absent:

1. **Mertens' 1st** — sharp prime form `∑_{p≤N}(log p)/p = log N + O(1)` (`mertens_first_prime`), vonMangoldt
   form, `~ log` capstones.
2. **Mertens' 2nd** — `mertens_second : (∑_{p≤N} 1/p − log log N) =O[atTop] 1`. Via Abel summation
   (`sum_mul_eq_sub_integral_mul₁`, `mertens_second_identity`) + `log log` main term (`integral_inv_log_mul`)
   + `O(1)` remainder.
3. **Mertens' 3rd up to the constant** — `mertens_third_up_to_const : (log ∏_{p≤N}(1−1/p) + log log N) =O 1`,
   i.e. `∏(1−1/p) ≍ 1/log N`. Via `log_primeProd_eq`/`_corr` + bounded `primeCorr` (`abs_primeCorr_le`) +
   the local correction bound `log_one_sub_add_self_abs_le : |log(1−x)+x| ≤ x²` (deriv monotonicity) +
   Mertens' 2nd.

## 🧠 State
- `lake build` **GREEN, 8296 jobs**; pre-commit green-gate on every commit. `src/` = **0 math axioms**.
- Open `sorry`s (both non-blocking, off every headline): `nagura_prime` (superseded), `prelim_decay_2/3`
  (dead-code island in `Wiener.lean`).
- **Aristotle:** `0fa80268` RUNNING on `summable_primeCorr` (correction-series convergence, toward the
  sharp constant). `c6d615ee` IDLE — returned a `prelim_decay_2` proof that **fails on our pin** (`sorryAx`,
  smul/mul drift; Aristotle defaults to v4.28.0 ≠ our v4.29.1). `11a2dc8c` was the correction bound — I
  CANCELLED it (proved locally first).

## 🎬 Next actions (the SHARP Mertens constants — the deep refinements)
1. **Poll `0fa80268`** (`aristotle show 0fa80268-…`). If it returns `summable_primeCorr` (`Summable
   (fun p : Nat.Primes => log(1−1/p)+1/p)`): **VERIFY on our pin** (v4.28.0 drift risk) + `#print axioms`
   before trusting. Then `primeCorr N → ∑'` (a constant `M_corr`) sharpens `mertens_third_up_to_const`'s
   `≍` toward a precise limit. (Or prove `summable_primeCorr` locally: comparison `|·| ≤ 1/p²`,
   `∑ 1/p² ≤ ∑ 1/n²` over the `Nat.Primes` subtype — reuse `log_one_sub_add_self_abs_le`, `summable_one_div_sq`.)
2. **Upgrade Mertens' 2nd to convergence** `∑_{p≤x} 1/p − log log x → M` (Meissel–Mertens): the remainder
   integral `∫_2^∞ (primeSumDiv⌊t⌋−log t)/(t log²t)` must CONVERGE (improper), not just be bounded — show the
   tail `∫_X^∞ → 0` (the integrand is `≤ C/(t log²t)`, integrable to `∞`). Real-analysis upgrade of the
   existing `mertens_second` argument.
3. **`e^{−γ}` identification** (`M = γ`): the deep part — `ζ(s)` Euler product as `s→1⁺` / Euler–Mascheroni
   `γ`. Generational, multi-lap.
4. Keep Aristotle fed when it idles AND a genuinely-open lemma exists (always verify its output on our pin).

## ⚠️ Gotchas (this lap) — see also `~/personal/claude/knowledge/.../reference/`
- `open MeasureTheory intervalIntegral` makes `integral_const_mul`, `one_div_le_one_div_of_le` **ambiguous**
  → qualify (`intervalIntegral.…`, `_root_.…`).
- `setIntegral_congr_fun`/`IntegrableOn.congr_fun`/`Finset.sum_filter_of_ne` give un-β-reduced `(fun x=>…) t`
  → `simp only []`/`show` before `rw`.
- `convert (hd : HasDerivAt …) using 1` closes the value goal by defeq (drop trailing `ring`).
- `field_simp` alone closes the remainder integrand rewrite; `gcongr` for `a/d ≤ b/d`.
- mathlib `integrableOn_mul_sum_Icc` = clean step-function integrability; `sum_mul_eq_sub_integral_mul₁`
  (NOT `₀`: `1/log` undiff. at `t=1`). `Real.summable_one_div_nat_pow` (Real namespace), `Real.log_prod`.
- **Aristotle defaults to mathlib v4.28.0** — its proofs need in-kernel `#print axioms` verification on our
  v4.29.1; expect smul/mul + lemma-rename drift.

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/Mertens.lean` — the Mertens trilogy.
- `STATUS.md` (ledger refreshed), `PENDING_WORK.md` (sharp-constants plan), `HANDOFF.md` (thin pointer).

---
**→ Next session: poll Aristotle `0fa80268`, then chase the sharp Mertens constants (actions #1–#2). The
classical trilogy is done & axiom-clean.**
