# Handoff: HJSW frontier — SHEARED-HYPERBOLA construction found; geometry formalized; selection rule open

**Date**: 2026-06-19 05:57 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `30bed5b`

## 🎯 What we're doing
Sole target: the HJSW `3N/2` lower bound `hjsw_lower : 3*(p−1) ≤ maxNoThreeInLine (2*p)`
(`Hyperbola.lean`), the only open `sorry` in `src/`. The 2N upper bound + Erdős Θ(N) are done.

## ⭐ Headline progress this lap (a real course-correction)
The prior baton concluded "single hyperbola caps at 17 ⇒ need ≥2 curves; need the paper." **That was
an artifact of only testing the |B|=p−1 plain hyperbola + uniform 3-of-4.** A fresh exhaustive scan
over permutation-graph bases (one point per column, distinct rows) found **984/5040** bases at p=7
reach 18, and among *uniformly-defined* families the **sheared hyperbola works**:

> **`base = { (x, (2x+1)⁻¹ mod p) : x ∈ Z_p }`** — the conic `y·(2x+1) ≡ 1 (mod p)`, pole `x=−2⁻¹ ↦ 0`,
> `|B| = p`. Its `4p` lifts into `[0,2p)²` contain a `3(p−1)`-point no-3-collinear set.
> **Verified REACH at p = 7, 11, 13** (also Möbius bases `(0,1,4,2),(1,1,2,4),(2,3,1,3),(2,1,4,0)`).
> The shear redistributes the slope-`±1` collisions that over-constrain the plain hyperbola; the
> extra pole point matters (`xy=1 ∪ {origin}` does NOT work — the shear is essential).

## ✅ Formalized & axiom-clean this lap (all in `Hyperbola.lean`, `#print axioms` = bare trust base)
- `collinear_imp_modp_det_zero` — **construction-agnostic**: real-collinear grid points ⇒ residues'
  2×2 determinant `= 0` in `ZMod p` (ℤ→ZMod p ring hom). The universal bridge.
- `hyperbola_collinear_zmod` — mod-`p` Vandermonde core over the field (`xy=k`).
- `hyperbola_lift_collinear_share_residue` — real-collinear hyperbola-lift triple ⇒ two share residue.
- `coord_diff_of_residue_eq` — same-residue grid coords in `[0,2p)` differ by `0` or `p`.
- `shear_hyperbola_collinear_zmod` + `shear_hyperbola_lift_share_residue` — the **same full geometric
  reduction for the actual construction base** `(2x+1)y=1` (reduces to the plain core via `u=2x+1`).
- `intCoord_diff_factor` + **`lift_triple_noncollinear`** — the **same-base-point case**: 3 distinct
  lifts of one base point are never collinear (3 corners of a `p×p` rectangle ⇒ det `±p² ≠ 0`).
- `Anchors.lean`: `witness7_shear` / `hjsw_lower_seven_shear` — first *construction-derived* 18-pt
  witness (residues on `y(2x+1)≡1 (mod 7)`), native_decide-certified, off-headline.

**Net: the GEOMETRY is COMPLETE.** Cross-residue triples impossible (`*_share_residue`); same-residue
triples impossible (`lift_triple_noncollinear`). Every collinear triple of lifts = two lifts of one
base point + a lift of a *different* base point (a slope-`±1` cross-base alignment). The ONLY
remaining obligation is the combinatorial **lift-selection rule** (kill the slope-`±1` cross-base
triples) + a short capstone assembling the two cases.

## 🧠 The open crux: the non-uniform selection rule
- Count is intrinsically `3(p−1) = 3p−3` (a **deficit of 3** below "3 per base point"); **uniform
  3-of-4 (=21 pts) does NOT exist** for any winning base. Selection is genuinely non-uniform.
- A character-only rule (keep-set by `χ(2x+1)`) does NOT work (tested p=7,11).
- Collision-graph structure: classify base pts by `y−x` (slope+1) / `y+x` (slope−1) mod p. At
  `p≡1 (mod 4)` all classes ≤2 (clean); at `p≡3 (mod 4)` there's a **size-3 class per side** —
  where the deficit concentrates. The rule must use this structure (likely split on `p mod 4`).
- This is the heart of the HJSW combinatorics (the part the 1975 paper actually works for).

## 🎬 Next actions (in order)
1. **Check `aristotle list` for `083292d5`** (`aris-hjsw`, self-contained HJSW submitted this lap).
   If DONE: `aristotle download`, VERIFY in our kernel + `#print axioms`, port the construction onto
   the real defs (use the reduction toolkit for the geometry). If it returns a wrapper-sorry, ignore.
2. **Check repo root for `ON-LINE-FINDINGS-*.md`** (the paper request is still filed/sharpened).
3. **Pin the selection rule** (`PENDING_WORK.md` Path B): search for a *structured* `3(p−1)`-lift
   selection of the sheared base — try a rule keyed on the collision-class structure + `p mod 4` +
   the size-3 classes; validate at p=7,11,13,17 with `native_decide (decNoThree …)`. Then PROVE:
   the base's mod-`p` non-collinearity is free (`shear_hyperbola_collinear_zmod`); the slope-`±1`
   triples are killed by the rule via `coord_diff_of_residue_eq`. This is the path to closing
   `hjsw_lower` axiom-clean.

## ⚠️ Gotchas
- `hjsw_lower`'s `sorry` is the only real one in `src/` (other "sorry" greps are doc/comments). The
  `--allow-stop` gate stays closed while it's open — do NOT stop / weaken the statement.
- native_decide witnesses (`witness*`, incl. `witness7_shear`) carry `ax_*` artifacts — ANCHORS only,
  never let them leak into the headline proof (must be a `Collinear ℝ` argument).
- p=17 confirmation of the sheared base is slow (exponential hitting-set search); 3 primes suffice.
- Python probes: stdout buffers when piped — keep `flush=True`; heredocs into /tmp were flaky, use
  the Write tool to author scripts.

## 📁 Key files
- `…/NoThreeInLine/Hyperbola.lean` — reduction toolkit + sheared-base geometry + `hjsw_lower` (sorry).
- `…/NoThreeInLine/Anchors.lean` — `decNoThree` certificate + witness ladder + `witness7_shear`.
- `PENDING_WORK.md` — full attack paths (Path B now primary: the selection rule). `STATUS.md` ledger.
- `ON-LINE-REQUEST.md` — sharpened paper request. Aristotle job: `083292d5`.

---
**→ Start at Next action #1 (poll Aristotle `083292d5` + check findings), then #3 (the selection
rule) — that is the whole game now. The construction family is FOUND and the geometry is DONE.**
