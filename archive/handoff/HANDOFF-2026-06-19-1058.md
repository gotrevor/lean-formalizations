# Handoff: deep-reflection lap COMPLETE — weakPNT route resolved, port started

**Date**: 2026-06-19 10:58 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `486bd42`

## 🎯 What we're doing
Isolated clone dedicated to the **no-three-in-line / HJSW frontier**. This was the every-9th
**deep-reflection lap** (altitude, not mechanics). The originally-mandated target (HJSW `3N/2`
axiom-clean in `Statement.lean`) is long COMPLETE; the treadmill overshot it to HJSW's optimal
**`3/2 − o(N)`** general-`N` constant (`maxNoThreeInLine_ge_three_halves_sub`), which rests on ONE
cited deep axiom **`weakPNT`** (the PNT, `ψ∼x`). This lap I confirmed the direction, then resolved
how `weakPNT` can/can't be discharged and **started the only viable discharge route (a source port)**.

## 🧠 Context to carry forward (read before acting)
- **The thread is at its natural endpoint.** `weakPNT` is the single open math axiom; `nagura_prime`
  is a non-blocking disclosed `sorry` (superseded). The audit surface `Statement.lean` + the two
  **unconditional** rungs past Bertrand `3/4` (`..._six_fifths`, `..._fifteen_sixteenths`) are
  axiom-clean. Don't re-grind these.
- **`weakPNT` discharge — the decision tree (settled this lap):**
  1. **Lake-dep route is DEAD on-box** (I ran it). `lake update` with a local PNTAnd require: toolchain
     check *passed*, but lake died fetching PNTAnd's transitive GitHub deps (`PrimeCert` → `git exit 128`,
     no egress); PNTAnd also pins mathlib v4.29.0 ≠ ours v4.29.1. Two independent blockers. **Don't retry.**
  2. **Wait-and-cite is PREFERRED.** mathlib v4.29.1 *already* has the hard PNT crux ζ≠0 on `Re=1`
     (`riemannZeta_ne_zero_of_one_le_re`); the only gap is the Wiener–Ikehara tauberian bridge, and mathlib
     is on-trajectory (`LSeries/PrimesInAP.lean`). Periodically `grep` the pin for `WienerIkehara` / a
     `Chebyshev.psi` asymptotic; when it lands, replace `axiom weakPNT` with the citation → flagship goes
     axiom-clean for free. **Don't sink many laps into the port while this is live.**
  3. **The port is the ACTIVE fallback, and I STARTED it.** It sidesteps the dep-hell (the Wiener cone
     uses none of PrimeCert/leancert/checkdecls — copy source, build vs our mathlib). Measured BOUNDED:
     all heavy mathlib analysis is already in our pin, so the surface is PNTAnd's own ~5000 lines across a
     one-patch gap (`Wiener.lean` 4118 is the bulk). Apache-2.0 → license-OK to port; **do NOT submit
     upstream** - this is *not* a licensing or attribution position. The standing rule is that
     AI-assisted proofs do not go upstream unless the author can defend every line unaided, which
     was not the case here.
     **[Superseded: this port was abandoned. `lakefile.toml` now takes `PrimeNumberTheoremAnd` as a
     real git dependency - nothing was copied, nothing was stripped, no attribution was removed.]**
- **`nagura_prime` is genuinely blocked, not fixation** — re-derived twice this lap: the elementary
  `T`-method caps at Chebyshev ratio exactly `6/5` (no slack), and the central-binomial split needs a
  θ-constant `A > 1.926` (impossible, since `A ≤ 1`). Needs Nagura's sharper finite inequality (the paper,
  in `ON-LINE-REQUEST.md`). Low value (would only give unconditional `5/4` vs the `3/2` we have mod PNT).
- **Faithfulness is solid** — re-audited + independently cross-checked (Aristotle `72891d77` NL→Lean of the
  prose HJSW statement converged on a logically-equivalent theorem and proved `Collinear ℝ ⟺ cross-det=0`).

## ✅ State (all observed this lap)
- `lake build` **GREEN, 8287 jobs**, confirmed after every commit (pre-commit green-gate); tree clean at `486bd42`.
- **Full axiom ledger re-verified from real `#print axioms`**: 6 complete threads + NTL audit surface +
  `6/5`/`15/16` = `[propext, Classical.choice, Quot.sound]`; `..._three_halves_sub` = `+ weakPNT`;
  `..._five_fourths` = `+ sorryAx`.
- **PNT-port bricks 1–2 landed, axiom-clean, green** in
  `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/Asymptotics.lean`: the 5 Asymptotics lemmas
  (`isLittleO_const_id_{cocompact,atTop,atBot}`, `eventually_natCast`, `isBigO_natCast`) + the Log/Basic
  patch `tendsto_pow_log_div_pow_atTop`. Zero API drift on the patch layer so far.
- Synthesis docs refreshed & committed: STATUS.md, PENDING_WORK.md (`## Reflection — 2026-06-19` at top),
  thin HANDOFF.md pointer, project-local memory (`weakpnt-discharge-map`).

## 🎬 Next actions
1. **First: check if wait-and-cite is now possible.** `grep -rn "WienerIkehara\|psi.*atTop" .lake/packages/mathlib/Mathlib`
   — if mathlib landed it, cite from mathlib and delete `axiom weakPNT` (flagship → axiom-clean). Done.
2. **Else, if continuing the port:** next bricks in dependency order (small/self-contained first) —
   PNTAnd `Mathlib/Analysis/Asymptotics/Uniformly.lean` (164), then its `Mathlib/NumberTheory/Chebyshev.lean`
   patch (303), then `Defs.lean` slice → `Fourier.lean` (70) → `SmoothExistence.lean` (107) → `Wiener.lean`
   (4118, the bulk) → the `WeakPNT''` slice of `Consequences.lean`. Drop `@[blueprint]`/`import Architect`
   (build-tooling attributes for a toolchain we do not run; not authorship metadata)
   throughout. Add each to the existing `NumberTheory/PrimeNumberTheorem/` dir; build green per brick.
3. **Do NOT** re-run the lake dep-test, re-grind elementary Chebyshev constants for nagura/`5/4`, or bump
   the project toolchain.

## ⚠️ Gotchas
- **Namespace shadowing:** don't name a file's namespace with a final component equal to a mathlib namespace
  you `open` (I hit this — `namespace ...Asymptotics` + `open Asymptotics` failed to find root `Asymptotics`
  lemmas). Drop the suffix or use `_root_.`.
- `isLittleO_log_rpow_atTop` / `isLittleO_log_rpow_rpow_atTop` live at **root** namespace in our pin (NOT
  `Real.`, NOT `Asymptotics.`).
- The pre-commit hook runs `lake build` and emits many pre-existing lint warnings (push_neg deprecation,
  unused vars) + the expected `nagura_prime` sorry warning — these are normal; look for "Build completed".

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/Asymptotics.lean` — the started PNT port (bricks 1–2).
- `src/LeanFormalizations/Combinatorics/NoThreeInLine/PrimeGap.lean` — `axiom weakPNT` + the whole `3/2−o(N)`
  build are at its end (after `maxNoThreeInLine_ge_five_fourths`).
- `PENDING_WORK.md` top (`## Reflection — 2026-06-19`) — the durable direction call + full port sizing/plan.
- `STATUS.md` — refreshed ledger + where-it-stands. `~/src/PrimeNumberTheoremAnd/` — port source.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this clone IS the chosen thread. Absorb the
context above, then start at Next action #1: grep the mathlib pin for a native Wiener–Ikehara / `ψ∼x` (the
preferred wait-and-cite discharge of `weakPNT`); if absent, continue the port at brick 3 (Uniformly).**
