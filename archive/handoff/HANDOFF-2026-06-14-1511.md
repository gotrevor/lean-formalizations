# Handoff: Curtis 1990 — COMPLETE and axiom-clean (crux closed)

**Date**: 2026-06-14 15:11 UTC · **Branch**: main · **HEAD**: ffdaec2

## 🎯 What we're doing
Formalizing Curtis 1990 (the Frobenius number of a triple is not algebraic over its
generators ⇒ no closed/finite-list formula). The prior handoff had it gated on ONE
deep `sorry` (`substCurve_eq_zero`, "Step A"). **This session closed that crux.** The
whole repo is now `sorry`-free and axiom-clean. The directed target is done; what
remains are optional extensions (see Next actions).

## 🧠 Context to carry forward
**The crux fell to a reformulation, not a grind.** The prior plan (and DIRECTION.md)
assumed Step A needed Curtis's Lemma 1 (Dirichlet + Farey adjacency, to build a
*converging coprime* sequence) + a projective/limit argument — a multi-lap analysis
build, with Lemma 1 out to Aristotle. **All of that was unnecessary.** Key realization:
`IsAdmissible` and `Lemma2` never use `gcd(x,y)=1` — only `p∤y` and `x∤y`. So:
- Fix ONE prime `x ≡ 1 (mod p)` with `x > p·(D+1)`, `D = F.totalDegree`. The interval
  `((p−k)x, (p−k+1)x)` has length `x`, so it contains **no** multiple of `x` (⇒ `x∤y`
  free) and `≥ D+1` integers `y ≡ p−k+1 (mod p)`.
- Each `(p,x,y)` is admissible with Frobenius number `(k−2)x+y−p` (Lemma 2) ⇒
  `substCurve F p k` vanishes at `(x,y)` (via the `hF` graph hypothesis).
- `D+1` such primes (Dirichlet: `Nat.exists_prime_gt_modEq_one`) give a `(D+1)²`
  "staircase" grid of zeros; **double univariate root-counting + `MvPolynomial.funext`**
  (new lemma `grid_vanish`) forces the curve `= 0`.

This replaced the entire limit machinery. The Aristotle Lemma-1 job (`80d9166c`) is now
**obsolete** — don't harvest it; the proof needs no Lemma 1.

Extensions added after the crux (all axiom-clean): faithfulness anchors
(`Anchors.lean`, `⟨3,7,8⟩` Frobenius=5, proved directly AND via Lemma 2); the **ℤ/ℚ
(any ℂ-algebra) corollary** (push the formula along `algebraMap`, lemma `eval₂_comp`);
the **n≥3 generalization** (the paper's full title, by reduction to n=3); and
`grid_vanish` generalized to any infinite field `K` (reusable impossibility-theorem infra).

**DIRECTION.md is now partly stale** — it's all about "spike the sole gate / park if a
wall." The gate is closed; treat DIRECTION's framing as historical. The live status is
HANDOFF.md (master, refreshed) + PENDING_WORK.md (next targets).

## ✅ State (all observed this session)
- `lake build` **green**, 8255 jobs. Repo is `sorry`-free, no `axiom` decls.
- `#print axioms` on all 6 public results = `[propext, Classical.choice, Quot.sound]`
  (no `sorryAx`): `no_polynomial_relation`, `no_finite_polynomial_formula`,
  `_int`, `_rat`, `no_finite_polynomial_formula_multivar`, `Anchors.frobeniusNumber_3_7_8`.
- 7 commits this session (HEAD `ffdaec2`), all green, working tree clean.
- Remaining lint warnings are ALL in `Lemma2.lean` (Aristotle-generated) and are
  intentionally left (mathematical hypotheses + fragile `simp_all` blocks).

## 🎬 Next actions
Curtis is complete; these are genuine *optional* extensions (details in PENDING_WORK.md).
None is blocked. If Trevor gives a new direction, follow that instead.
1. **Upstream to mathlib** `Mathlib.NumberTheory.FrobeniusNumber` (the natural home,
   sequel to the n=2 Chicken-McNugget theorem). This is an **outward-facing push —
   Trevor's call, not autonomous.** Policy is known (reference corpus
   `2026-06-07-mathlib-ai-contribution-policy.md`): disclosed + expert-supervised is fine.
2. Optional: combine the two extensions (n≥3 over any ℂ-algebra) — mechanical, low value.
3. Optional: a "Zariski-dense graph / no proper hypersurface" repackaging of
   `no_polynomial_relation` (near-trivial; the docstring already says it).

## ⚠️ Gotchas
- `aeval_C` leaves `algebraMap K K a` un-reduced; `simp only [map_*, aeval_C, aeval_X]`
  mysteriously fails to distribute the hom (reports the lemmas "unused"). Fix: rewrite
  `aeval`→`eval` first (`← aeval_eq_eval`, or `aeval_eq_eval`), then **explicit `rw`**
  `[map_sub, map_add, map_mul, eval_C, eval_X]` — explicit rw works where simp didn't.
  See `eval_substCurve_eq` in Engine.lean (4th-coordinate case uses `show` + this rw).
- omega can't directly refute `¬∃ a b c, 5 = 3a+7b+8c` (dives into `bmod_div_term`);
  peel `have c = 0 := by omega; have b = 0 := by omega; omega` first.
- Don't harvest Aristotle job `80d9166c` (Lemma 1) — obsolete; the proof bypasses it.

## 📁 Key files
- `Curtis/Engine.lean` — engine; `substCurve_eq_zero` (the closed crux), bridge
  `eval_substCurve_eq`, Step B, spine.
- `Curtis/GridVanish.lean` — the grid-vanishing lemma (over any infinite field).
- `Curtis/Statement.lean` — audit surface: all headline theorems + ℤ/ℚ + n≥3.
- `Curtis/Anchors.lean` — faithfulness witnesses.
- `Curtis/Lemma2.lean` — Brauer–Shockley value (Aristotle, verified). Leave its lint.
- `PENDING_WORK.md` / `HANDOFF.md` — current status + next targets.

---
**→ Next session: Curtis 1990 is DONE (axiom-clean). This doc IS the chosen thread —
don't summarize it back to Trevor, don't offer other KB projects, don't wait. If Trevor
has set a new direction, do that; otherwise the only remaining Curtis work is the
mathlib-upstream push, which is Trevor's outward-facing call — confirm with him before
pushing anything. Do not re-attempt the crux or harvest the obsolete Aristotle Lemma-1
job.**
