# Handoff: Mertens' FIRST + SECOND theorems COMPLETE; Mertens' 3rd started

**Date**: 2026-06-19 13:31 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `82e2117`

## 🎯 This lap (review lap → 17 commits, all green + axiom-clean)
Began as a fresh-mind review lap; corrected stale `weakPNT`-as-axiom framing in STATUS/HANDOFF (it was
discharged a prior lap — the flagship `maxNoThreeInLine_ge_three_halves_sub` is **unconditional &
axiom-clean**). Then drove the unlocked PNT layer hard, all in
`src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/Mertens.lean`:

- **Mertens' FIRST theorem COMPLETE** — sharp prime form `∑_{p≤N}(log p)/p = log N + O(1)`
  (`mertens_first_prime`, `abs_primeSumDiv_sub_log_le`), vonMangoldt form, and `~ log` capstones. All
  axiom-clean, mathlib-absent.
- **Mertens' SECOND theorem COMPLETE** — `mertens_second : (∑_{p≤N} 1/p − log log N) =O[atTop] 1`,
  axiom-clean, mathlib-absent. Via Abel summation `sum_mul_eq_sub_integral_mul₁` (`mertens_second_identity`)
  + `log log` main term + `O(1)` remainder. Supporting lemmas: `hasDerivAt_log_log`/`integral_inv_log_mul`,
  `hasDerivAt_inv_log`/`integral_inv_mul_sq_log`, `log_sub_log_floor_le`, `abs_primeSumDiv_floor_sub_log_le`,
  `integrableOn_primeSumDiv_floor_div`, `primeSumDiv_div_log_tendsto_one`, bridges `sum_primeLogDivCoeff_eq`
  /`sum_inv_log_mul_primeLogDivCoeff_eq`.
- **Mertens' THIRD theorem STARTED** — `primeProd N := ∏_{p≤N}(1−1/p)`, `one_sub_inv_pos_of_prime`,
  product-to-sum bridge `log_primeProd_eq` (`log ∏(1−1/p) = ∑ log(1−1/p)`).

## 🧠 State
- `lake build` **GREEN, 8296 jobs** (verified every commit; pre-commit green-gate too).
- `src/` carries **ZERO math axioms** — every headline `[propext, Classical.choice, Quot.sound]`.
- Open `sorry`s (both non-blocking, off every headline): `nagura_prime` (superseded) and
  `prelim_decay_2/3` (dead-code island in `Wiener.lean`).
- **Aristotle:** `11a2dc8c` RUNNING on the Mertens-3rd correction bound `|log(1−x)+x| ≤ x²` (0<x≤½).
  `c6d615ee` IDLE — it returned a `prelim_decay_2` proof but **it does NOT compile on our pin** (`sorryAx`;
  a `rw [integral_const_mul]` fails at its line 149 — smul/mul drift, Aristotle defaults to mathlib v4.28.0
  vs our v4.29.1). Math is right but version-drifted; NOT ported (low value — dead code). Archive at
  `/tmp/aris_x/project_aristotle/Problem.lean` is gone on reboot; re-download from `c6d615ee` if wanted.

## 🎬 Next actions (Mertens' 3rd `∏(1−1/p) ~ e^{−γ}/log x`)
1. **Poll `11a2dc8c`** (`aristotle show 11a2dc8c-…`). If it returns `log_one_sub_add_self_abs_le`
   (`|log(1−x)+x| ≤ x²`): **VERIFY on our pin** (build + `#print axioms` — Aristotle's v4.28.0 ≠ ours, so
   expect possible drift) before trusting; if it fails, prove locally (upper half `log(1−x)+x ≤ 0` is
   `Real.log_le_sub_one_of_pos` on `1−x`; lower half `−x² ≤ log(1−x)+x` needs a Taylor/`exp` estimate).
2. **Correction series**: with the bound, `∑_{p≤N} (log(1−1/p) + 1/p)` is Cauchy/bounded (comparison
   `|·| ≤ 1/p² ≤ summable_log_div_sq`-style). Combined with `log_primeProd_eq` and Mertens' 2nd
   (`primeRecipSum N = ∑1/p`): `log primeProd N = −primeRecipSum N + ∑(log(1−1/p)+1/p) = −log log N − M + o(1)`.
   First milestone: **Mertens' 3rd up to the constant** `(log primeProd N + log log N) =O[atTop] 1`.
3. **The `e^{−γ}` constant** (`M = γ`, Meissel–Mertens) is the deep part — relate to `ζ(s)` Euler product
   as `s → 1⁺` / the Euler–Mascheroni constant. Separate (multi-)lap.
4. Whenever Aristotle idles AND an open lemma exists, architect the next bounded one and submit.

## ⚠️ Gotchas (this lap)
- `open MeasureTheory intervalIntegral` makes `integral_const_mul`, `one_div_le_one_div_of_le` **ambiguous**
  → qualify (`intervalIntegral.integral_const_mul`, `_root_.…`).
- Under `setIntegral_congr_fun` / `IntegrableOn.congr_fun`, the integrand is an un-β-reduced `(fun x=>…) t`
  → `simp only []` before `rw`. `Finset.sum_filter_of_ne` obligation likewise → `show`.
- `field_simp` alone closes the remainder integrand rewrite (no trailing `ring`); `gcongr` for `a/d ≤ b/d`.
- mathlib `integrableOn_mul_sum_Icc` is the clean way to get step-function `t↦(∑_{k≤⌊t⌋}c k)·g t`
  integrability. `sum_mul_eq_sub_integral_mul₁` (NOT `₀`) for the Abel sum (`1/log` undiff. at `t=1`).
- `Real.log_nonneg`/`log_le_log` need `1 ≤ ·`/`0 < ·` (positivity can't supply from hyps); `rcases b=0` in
  `sum_le_tsum` nonneg proofs.

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/PrimeNumberTheorem/Mertens.lean` — Mertens 1st (complete), 2nd
  (complete), 3rd (started).
- `STATUS.md` (refreshed), `PENDING_WORK.md` (Mertens-3rd plan at top), `HANDOFF.md` (thin pointer).

---
**→ Next session: poll Aristotle `11a2dc8c`, then drive Mertens' 3rd "up to the constant" (action #2).
Both Mertens 1st & 2nd are done & axiom-clean; the product-to-sum bridge is in place.**
