# Handoff: Davies planar Kakeya — measurable-selection route spine BUILT; crux = measurable selection

**Date**: 2026-06-19 15:30 UTC · **Branch**: `kakeya-davies` · **HEAD**: `0da1bf3`

## 🎯 What we're doing
Prove `davies_kakeya_2d : KakeyaSetConjectureDim 2` (planar Kakeya ⟹ `dimH = 2`). The whole job is the
lower bound, machine-checked **down to one cited axiom** `Engine.kakeya_subresolution_content` (the
Case-B / sub-resolution residual). `#print axioms davies_kakeya_2d = [propext, Classical.choice,
Quot.sound, kakeya_subresolution_content]` — unchanged this lap (the new work is an *alternative*
route built alongside, not yet wired into the headline).

## 🧠 Context to carry forward (the mental model — read this)
**This lap I re-derived Case B from scratch and pinned the crux precisely.** The previous framing
("Hausdorff-vs-box gap, multi-scale Córdoba diverges for d>1") was correct but pessimistic. The sharp
truth (full argument in `Kakeya2D/CASE_B_ANALYSIS.md`, three independent confirmations):

- The discrete fixed-net L² sum genuinely fails for d>1 (single fixed direction too lossy; the
  scale-matched *average* decays correctly but the net changes with scale — DO NOT relitigate it).
- **But the proof closes the instant you integrate over the *continuum* of directions:**
  `∫₀¹ ∑ⱼ ℓⱼ(θ) dθ ≥ 1` → pigeonhole to a dominant scale `j*` → discrete count at `j*`'s OWN
  resolution `2⁻ʲ*`. The continuum is "the scale-matched net at every resolution at once", so the
  net-scale circularity that forced the discrete `min(·,J)` cap (and hence Case B) never arises.
- **The ONLY blocker to that integral is measurability of `θ ↦ ℓⱼ(θ)`** = a *measurable base-point
  selection* `θ↦a(θ)` (`IsKakeya` gives only `Classical.choice`). That is the genuine crux, and it is
  a real mathlib gap (no KRN/Jankov–von Neumann; `SetTheory/Descriptive` is just `Tree.lean`).
- **Key simplification discovered:** NO new "continuum Córdoba count" is needed. Once `j*` is the
  *uncapped* dominant scale, the count is the already-proven discrete `caseA_content` at resolution
  `2⁻ʲ*`, fed by a *continuous shift-average* to extract a base angle `α`. So the honest route =
  **one deep axiom (selection)** + the all-proven spine + a wiring assembly.

I built the **entire measure-theoretic spine** this lap (all axiom-clean, in
`Kakeya2D/MeasurableRoute.lean`):
1. `measurable_coveredLength` — keystone: `θ ↦ vol{t∈[0,1]: a θ + t•w θ ∈ F}` measurable (Fubini).
2. `exists_continuum_dominant_scale` — cap-free dominant scale `j` with
   `scaleWeight j ≤ ∫_{[0,1]} vol{covered by scale-j pieces}`.
3. `exists_shift_ge_integral` — continuous shift-average: some `α∈[0,2⁻ʲ)` has
   `2ʲ·∫_{[0,1]}f ≤ ∑_{i<2ʲ} f(α+i·2⁻ʲ)`.
These chain into the existing `NetThinning.caseA_content`. I did NOT attempt the wiring (W) — it's a
~150-line assembly mirroring `Engine.kakeya_hausdorffContentBound` but at the uncapped scale, with
delicate matching (zero-diameter pieces, the genuine `ediam` window, finite/infinite-fiber split,
aligning `ℓ_{j*}`↔`A_i`↔caseA's `hscov`). Left it for a fresh lap to avoid a half-finished state.

## ✅ State (verified via real `lake build` this lap)
- Full library green, **8298 jobs** (observed at commits `48f3cd9` and via pre-commit green-gate on
  `0da1bf3`). `MeasurableRoute.lean` builds; all 3 new lemmas `#print axioms`-clean
  (`[propext, Classical.choice, Quot.sound]`).
- 6 commits this lap (`0455775` keystone, `94db0d8` continuum dominant scale, `ab60487` docs,
  `48f3cd9` shift-average, `0da1bf3` docs; plus `3444f67` PENDING). `davies_kakeya_2d` axiom set
  unchanged — the discrete `kakeya_subresolution_content` path is still the live critical path.
- `ON-LINE-REQUEST.md` UPDATE 5 filed: the sharp ask is now measurable selection (ask 1) and/or
  Davies' 1971 projection argument (ask 4); the continuum-Córdoba ask was withdrawn.

## 🎬 Next actions
1. **Harvest** any `ON-LINE-FINDINGS-*.md` answering UPDATE 5 (measurable selection / Davies
   projection). `git mv` into `archive/findings/` once consumed.
2. **(W) Build the wiring** `kakeya_hausdorffContentBound_of_measurableSelection` — takes a measurable
   base-point selection as a *hypothesis* (zero new axioms), proves the headline content bound from
   the spine + `caseA_content`. All pieces are proven; this is assembly, not new math. Template =
   `Engine.lean` lines 222–386 (replace the cap/`exists_dominant_shift`/Case-B with uncapped
   `g = dyadicIdx`, `exists_continuum_dominant_scale`, `exists_shift_ge_integral`; drop the Case-B
   branch entirely). Watch the gotchas listed below. This gives a hypothesis-gated headline and is the
   right next concrete win.
3. **(S) Attack the measurable selection** (the deep crux): KRN/Jankov–von Neumann for the
   closed-valued `B(θ)={a : segment(a,θ)⊆E}`, or a bespoke explicit selection (e.g. argmin-‖a‖ over
   `B(θ)`). mathlib has nothing — likely needs new infra; keep banging across laps. Reference-gated.

## ⚠️ Gotchas
- **Do NOT relitigate the fixed-net L² sum for d>1** (proven dead end — see CASE_B_ANALYSIS §C1).
- For (W): `g n = dyadicIdx (ediam(U n)).toReal` is *uncapped*; zero-`ediam` pieces must be parked at
  a scale that won't break `caseA_content`'s `ediam_lo` window (Engine's cap sidestepped this — the
  uncapped version needs explicit handling, e.g. send `ediam=0` to its own bucket or exclude from `s`
  while preserving the measure-zero containment).
- `exists_shift_ge_integral` needs `∫_{Icc 0 1} f ≠ ⊤`; for `f = ℓ_{j*}` this holds (ℓ ≤ vol(Icc)=1).
- Lean API facts I learned (also flushed to memory): `measurable_measure_prodMk_right` for fiber
  measures; `ae_eq_of_ae_le_of_lintegral_le` for "∃ point ≥ average" (avoids ℝ≥0∞ subtraction);
  `lintegral_finset_sum` needs explicit `(f := …)` or HOU fails; `map_add_right_eq_self` +
  `setLIntegral_map` for lintegral translation; `Ico_ae_eq_Icc` (NoAtoms) for Icc↔Ico.
- Pre-existing dormant `sorry` at `Logic/FastGrowing/Basic.lean:121` (NOT Kakeya — ignore).
- `push_neg` deprecation warnings are cosmetic and repo-wide; don't churn on them.

## 📁 Key files
- `Kakeya2D/MeasurableRoute.lean` — the 3 new spine lemmas (NEW this lap).
- `Kakeya2D/CASE_B_ANALYSIS.md` — the sharpened crux analysis + the refined route + (W)/(S) plan.
- `Kakeya2D/Engine.lean` — `kakeya_subresolution_content` (live axiom) + `kakeya_hausdorffContentBound`
  (the discrete assembly = the template for (W)); `caseA_content` is in `NetThinning.lean`.
- `PENDING_WORK.md` §A0‴ — this milestone + the (W)/(S) plan. `ON-LINE-REQUEST.md` UPDATE 5 — sharp ask.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context, then start at Next action #1 (harvest findings), then #2 (build the wiring (W) — all pieces
are proven). The measurable-selection route's spine is done; the crux is now ONE named true theorem
(measurable base-point selection). Do NOT relitigate the fixed-net L² sum for d>1.**
