# Handoff: Transcendence thread — `e` PROVED axiom-clean; π unconditional mod 1 cited axiom

**Date**: 2026-06-16 00:36 UTC · **Branch**: main · **HEAD**: `b76d463`

## 🎯 What we're doing
Continuing the constructible-numbers/Wantzel thread's only open extension: making
**squaring the circle unconditional**. That needed `Transcendental ℚ Real.pi`. This lap
(a) shipped the narrowing — π-transcendence from a single cited Hermite–Lindemann axiom —
and then (b) genuinely **chipped the wall**: proved transcendence of `e` (and `eⁿ/eᵃ/e^q`)
end-to-end and axiom-clean from mathlib's analytic part of Lindemann–Weierstrass.

## 🧠 Context to carry forward
- **The cited axiom is faithful and minimal.** `HermiteLindemann.lean` has
  `axiom hermite_lindemann {α:ℂ} (IsAlgebraic ℚ α) (α≠0) : Transcendental ℚ (exp α)` and a
  machine-checked `transcendental_pi` (Euler: π alg ⟹ iπ alg ⟹ exp(iπ)=−1 contradiction).
  The networked findings (`archive/findings/ON-LINE-FINDINGS-2026-06-15-pi-transcendence.md`)
  **independently verified this reduction is correct** and confirm the axiom = mathlib PR
  #28013's `transcendental_exp` over ℤ.
- **e-transcendence needs NO symmetric functions** — that's why it was the right local chip.
  The Hermite polynomial `∏_{k=1}^m (X−k)` has integer roots `1..m`, so the whole
  `exp_polynomial_approx` assembly stays elementary. Full proof in `ETranscendental.lean`;
  technique saved to repo memory `e-transcendence-via-exp-polynomial-approx.md`.
- **π is different and is the genuine remaining wall.** It needs the *algebraic part*
  (symmetric functions over the Galois conjugates of iπ). The findings **recommend AGAINST
  re-deriving it locally** — instead adopt mathlib **PR #28013** (Yuyang Zhao: full L-W,
  `transcendental_pi/_e/_exp/_log`, `linearIndependent_exp`) when mathlib is next bumped past
  its merge, then delete the axiom and bridge ℤ↔ℚ (`isAlgebraic_algebraMap_iff` /
  `transcendental_algebraMap_iff`). PR is OPEN/awaiting-author, not merged. No bump available
  this run (no egress).
- **Why I stopped extending transcendence**: the accessible *axiom-clean* frontier (e^q for
  any rational q≠0) is **saturated**. Everything further (π, log, general algebraic α) needs
  either the symmetric-function build (recommended-against) or depends on the axiom (not
  axiom-clean). Continuing would be padding or the big recommended-against build.

## ✅ State (all verified this lap)
- `lake build` **green = 8270 jobs** (only pre-existing harmless `Curtis/Lemma2.lean` lint
  warnings). `src/` is **sorry-free again**.
- **`e_transcendental`, `transcendental_exp_nat/_int/_rat`, `exists_intPoly_aeval_eq_zero`,
  and all 4 infra lemmas: `#print axioms` = `[propext, Classical.choice, Quot.sound]`** (0
  math axioms). Verified individually.
- `squaring_the_circle_impossible_uncond` & `transcendental_pi`: `#print axioms` = trust base
  **+ `hermite_lindemann` only** (the one 🟡 cited axiom). Conditional
  `squaring_the_circle_impossible` stays trust-base-clean.
- 11 commits this lap (`67c4bb2`…`b76d463`): narrowing → e reduction → infra lemmas →
  full Hermite assembly → e^n/e^a/e^q → findings harvest → docs.
- Aristotle job `e502fd22` (full e-transcendence) was **canceled** — proved locally; no job
  in flight. Other Aristotle jobs all IDLE/obsolete.

## 🎬 Next actions
Pick the highest-value open thread (all are genuine; none needs Trevor):
1. **Power-tower sharp `iff` lower direction** (`PENDING_WORK.md` item C) — the cleanest
   *open, non-blocked* math target: divergence for `0<x<e^(-e)` via a genuine attracting
   2-cycle. Doesn't need a mathlib bump. Fresh real-analysis work (touch
   `RealAnalysis/PowerTower/EngineLower.lean`); see memory `power-tower-2cycle-crux.md`.
2. **π, the local route** (`PENDING_WORK.md` item B paths 1–3) — only if you decide the
   symmetric-function build is worth it despite the findings' adopt-PR recommendation. The
   `e` assembly is the reusable analytic core; the NEW piece is symmetric functions over
   conjugates (mathlib has Vieta `coeff_eq_esymm_roots_of_card`,
   `MvPolynomial.Symmetric.FundamentalTheorem`). Architect a bounded Aristotle leaf for it.
3. **On a future mathlib bump**: execute the PR #28013 swap to kill the axiom (see Context).

## ⚠️ Gotchas
- `lake env lean <file>` is the fast way to `#print axioms` (write a tmp file importing the
  module, then `rm`). A `grind`/`simp_all` can silently inject `sorryAx` — the axiom check is
  non-negotiable after any port.
- The `Curtis/Lemma2.lean` lint warnings are intentional (mathematical hypotheses kept for the
  audit surface) — **leave them**.
- `Finset` was added to the `open` line in `ETranscendental.lean`; harmless with `Filter`.

## 📁 Key files
- `src/LeanFormalizations/NumberTheory/Transcendence/ETranscendental.lean` — the lap's
  centerpiece: full axiom-clean transcendence of e + the e^q family.
- `src/LeanFormalizations/NumberTheory/Transcendence/HermiteLindemann.lean` — the cited axiom
  + `transcendental_pi` + the PR-#28013 replacement note.
- `src/LeanFormalizations/Geometry/Constructible/Statement.lean` —
  `squaring_the_circle_impossible_uncond`.
- `STATUS.md` (refreshed, real axiom ledger) · `PENDING_WORK.md` (items B/C with attack paths)
  · `archive/findings/ON-LINE-FINDINGS-2026-06-15-pi-transcendence.md` (harvested).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or
wait for instructions, and don't offer other projects from the KB — this doc IS the chosen
thread. The transcendence/squaring-the-circle work this lap is COMPLETE to its accessible
axiom-clean frontier; absorb the context, then pick up at "Next actions" — default to #1
(power-tower sharp iff), the cleanest open non-blocked target.**
