# Handoff: C3 Cichoń identity PROVED (axiom-clean) — next crux = Hardy↔fastGrowing bridge

**Date**: 2026-06-19 08:23 UTC · **Branch**: `no-three-in-line` · **HEAD**: `a65ec90`
(+ uncommitted handoff). `src/` is **sorry-free, 0 math axioms**, `lake build` 🟢 (8288 jobs).

## 🎯 What happened this lap (the big one)
**The C3 borrowing crux is fully discharged.** The lone disclosed `sorry`
`hstep_oadd_one_zero` — the genuine borrowing predecessor of `ω^E`, the heart of Cichoń's
theorem identifying the Goodstein descent with the Hardy descent — is now a real
machine-checked proof. The whole C3 chain is axiom-clean:
`#print axioms goodsteinLength_eq_hardy = [propext, Classical.choice, Quot.sound]` (likewise
`hstep_toONote`, `hstep_oadd_one_zero`). So **the Cichoń identity
`goodsteinLength m = H_{seqONote m 0}(2) − 2` is fully proved.**

### How it was closed (all in `Logic/Goodstein/Growth.lean`, all axiom-clean)
The frontier-invariant method (my refinement of the prior `Good`-scaffold plan):
- `toOrdinal_pow` / `toOrdinal_oadd` — ordinal twins of `toONote_pow`/`toONote_oadd`.
- `Canon b o` (structural: every coeff `≤ b`) and `Good b o` (3-clause: `Canon` except ≤1
  coeff `= b+1` parked at the active descent frontier — lowest term, deeper tail, or, when
  `o = ω^e`, inside the exponent). `Good_of_Canon`, `Canon_toONote`.
- `canon_repr` : `Canon` NF `E` ⟹ `toOrdinal (b+1) (evalNat b E) = repr E` — proved by
  structural induction; the leading-term remainder bound for `toOrdinal_oadd` comes from `NF`
  + the engine's `toOrdinal` **strict monotonicity** (`StrictMono.lt_iff_lt`), so **no separate
  `evalNat` mono+bound mutual recursion was needed** (the key simplification).
- `canon_round_trip` : `toONote (b+1) (evalNat b E) = E` for `Canon` NF `E` (via `repr_inj`).
- `Canon_pred` : a `Good` *successor*'s predecessor is fully `Canon` (the `b+1` is forced into
  the finite lowest term, which `pred` decrements to `≤ b`).
- `Good_fundSeq` : `Good` is preserved by the limit descent at the working index `b` (6
  `fundamentalSequence` `inr` cases; the limit-into-exponent case recurses; structure mirrors
  `evalNat_fundSeq`).
- `hstep_pred_pow` : the **general predecessor** `hstep (oadd E 1 0) b = toONote (b+1)
  ((b+1)^(evalNat b E) − 1)` for every NF `E ≠ 0` with `Good b E`, by WF recursion on `repr E`
  (`WellFoundedLT.induction`). Limit case closes via IH on `f b` + `evalNat_fundSeq`; successor
  case peels the coeff (`hstep_oadd_coeff`), applies IH to `E' = pred E`, reconstructs via
  `canon_round_trip`. `hstep_oadd_one_zero` is the `E = toONote b L` instance.

The parallel Aristotle job `77c99f0e` (grinding the pre-narrowing general goal) was **cancelled**
(subsumed by the local proof).

## 🎯 Next crux: the domination corollary — Hardy ↔ fastGrowing bridge (the "B4 trap")
The identity gives `goodsteinLength m = H_{toONote 2 m}(2) − 2`. The headline of the expedition
(DIRECTION.md C3) is "**`goodsteinLength` eventually dominates every `fastGrowing o`**". This
needs to relate the Hardy diagonal `H_{toONote 2 m}(2)` (large *index*, fixed arg) to
`fastGrowing o m` (fixed index, large *arg*) — the Hardy↔fast-growing duality.

**Plan (verified numerically this lap, exit-0 `native_decide` on all of it):**
1. **Bridge inequality** `fastGrowing_le_hardy_pow : NF α → fastGrowing α n ≤ hardy (oadd α 1 0) n`
   (i.e. `f_α ≤ H_{ω^α}`; one-sided, robust to mathlib's `ω[n]=n+1` shift). By WF recursion on
   `repr α`:
   - **base** `α=0`: `f_0(n)=n+1 = H_1(n)` ✓.
   - **limit** `α` (`fundamentalSequence α = inr g`): `f_α(n)=f_{g n}(n) ≤ H_{ω^{g n}}(n) =
     H_{ω^α}(n)` via `fundSeq_oadd_one_of_limit` + IH. **Clean.**
   - **successor** `α = succ α'`: `f_α(n) = (f_{α'})^[n] n`; `H_{ω^α}(n) = H_{ω^{α'}·(n+1)}(n)`
     (via `fundSeq_oadd_one_of_succ`); need `(f_{α'})^[n] n ≤ H_{ω^{α'}·(n+1)}(n)`, which follows
     from the **iteration law** below + iterate-monotonicity (`f ≤ g` ptwise & `Monotone g` ⟹
     `f^[k] ≤ g^[k]`) + one extra expansive iterate (`le_hardy`).
2. **THE LINCHPIN — Hardy iteration law**
   `hardy (oadd e ⟨k+1⟩ 0) n = (hardy (oadd e 1 0))^[k+1] n`  (i.e. `H_{ω^e·(k+1)} =
   (H_{ω^e})^[k+1]`). **Fed to Aristotle: job `26c3a445`** (self-contained file, inline `hardyH`).
   Its inductive step needs the **Hardy additive law** `H_{a+b}(n) = H_a(H_b(n))` for
   CNF-valid sums. Proof route: induction on `b` via `fundamentalSequence`, reducing to two
   facts about `ONote.add ∘ fundamentalSequence`:
   - (F1) `fundSeq b = some b' → fundSeq (a+b) = some (a+b')`,
   - (F2) `fundSeq b = inr g → fundSeq (a+b) = inr (fun n => a + g n)`.
   **⚠ ABSORPTION CAVEAT (verified): the additive law is FALSE without a side condition** —
   `H_{1+ω}(n)=2n+1 ≠ H_1(H_ω(n))=2n+2` (left-absorption `1+ω=ω`). The side condition needed:
   `a`'s trailing exponent `≥` `b`'s leading exponent (no absorption). For the iteration law
   `a = ω^e·k, b = ω^e` (same exponent) the condition holds, so it's safe there. Whoever proves
   F1/F2 must carry that side condition or restrict to the same-exponent case.

**Attack-path alternatives** in `PENDING_WORK.md` (NEXT CRUX section): (1) inequality not identity
[above, recommended]; (2) pin exact small Hardy values; (3) diagonal domination directly.

## 🎬 Next actions
1. **Check Aristotle `26c3a445`** (`aristotle list`; one-shot). If it returns a proof of
   `hardyH_iter`, VERIFY in-kernel + `#print axioms`, then port onto the repo's real `hardy`
   (the inline `hardyH` is definitionally the repo's `hardy`).
2. Whether or not Aristotle lands it, **prove the iteration law locally** (path above): the
   clean sub-lemma is the Hardy additive law with the absorption side condition; prove F1/F2 from
   `ONote.add`'s def. Then assemble `fastGrowing_le_hardy_pow` (base+limit done, successor via
   iteration + iterate-monotone).
3. Then the **arg/index duality** for the final domination headline: combine
   `fastGrowing_le_hardy_pow` + `hardy_le_of_lt` (index monotone) + `goodsteinLength_eq_hardy`
   + the fact `toONote 2 m`'s repr exceeds any fixed `ω^α` for large `m` (via `toOrdinal` strict
   monotonicity → unbounded). State the headline as a thin audit-surface theorem (DIRECTION.md).

## ⚠ Gotchas (this lap)
- `repr.eq_2` is NOT a usable identifier here; use plain `simp` to unfold `(oadd e n r).repr`.
- `Ordinal.induction` is deprecated → use `WellFoundedLT.induction`.
- `Order.lt_succ` (not `lt_succ`) for `a < succ a`.
- `⟨b+1-1, _⟩` vs `⟨b, _⟩` (ℕ+): defeq but `rw`'s auto-`rfl` may not fire — close with an
  explicit `rfl` (or `exact (toONote_single …).symm`, as the finite base case does).
- `n.pos` (for `n : ℕ+`) is defeq to `1 ≤ (n:ℕ)` — usable directly where `1 ≤ c` is wanted.
- Aristotle: `submit <prompt> --project-dir <dir>` — the FILE goes in `--project-dir`, the
  positional arg is the prompt TEXT (not a path).

## 📁 Key files
- `src/LeanFormalizations/Logic/Goodstein/Growth.lean` — C3, now sorry-free. New block:
  `toOrdinal_pow`…`hstep_pred_pow` (lines ~507–845), then `hstep_oadd_one_zero` (proved),
  `hstep_toONote`, `goodsteinLength_eq_hardy`.
- `src/LeanFormalizations/Logic/FastGrowing/Hardy.lean` — `hardy`, `hstep`, `hardy_hstep`,
  `hardy_le_of_lt`, `le_hardy`, `hardy_monotone` (reuse for the bridge).
- `PENDING_WORK.md` (NEXT CRUX section) · `DIRECTION.md` (C3 = domination headline).
- Aristotle file: `/tmp/hardy_iter_aristotle.lean` (job `26c3a445`).

---
**→ Next session: the headline C3 *identity* is DONE. Start the domination corollary at the
Hardy iteration law (Aristotle `26c3a445` + local). Mind the absorption side condition.**
