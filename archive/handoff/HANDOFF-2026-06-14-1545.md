# Handoff: Curtis verified complete; next umbrella target chosen (constructible numbers)

**Date**: 2026-06-14 ~15:45 UTC · **Branch**: main · **HEAD**: `fe18f0a` (pre-commit of these docs)

## 🎯 What happened this lap (a review/course-correction lap)
A fresh-mind review lap. No proof code written — Trevor called a stop at a clean point
right after the strategic decision, before the new target's code began.

1. **Verified Curtis 1990 is genuinely complete & axiom-clean** — not just trusting the
   baton. Ran real `#print axioms` on all 7 headlines (`no_polynomial_relation`,
   `no_finite_polynomial_formula`, `_of_algebra`, `_int`, `_rat`, `_multivar`,
   `Anchors.frobeniusNumber_3_7_8`): every one = `[propext, Classical.choice, Quot.sound]`,
   no `sorryAx`, no custom axioms. `lake build` green (8255 jobs). `src/` has zero
   `sorry`/`admit`/`axiom`. **Curtis's directed target is done.**

2. **Strategic pivot decided.** Curtis's only leftovers are outward-facing (mathlib
   upstream — Trevor's call) or trivial (Zariski repackaging). The README declares this
   repo an umbrella for impossibility / no-formula meta-theorems, and allow-stop=0 means
   keep driving. Probed mathlib: **Liouville (elementary integrals), Lindemann–Weierstrass,
   and Abel–Ruffini are already in mathlib** — but **constructible-number theory is NOT**
   (the `Constructible.lean` files are all topology/spectra). So the chosen new target is
   **compass-and-straightedge impossibility (doubling the cube)**: on-theme, absent from
   mathlib, with an axiom-clean algebraic core and a hard geometric bridge to chip on.

3. **De-risked the tooling** for Layer 1 (all present): `Module.finrank_mul_finrank`
   (tower law), `IntermediateField.adjoin.finrank` (= `minpoly.natDegree`),
   `IntermediateField.finrank_bot = 1`, Eisenstein irreducibility
   (`Polynomial.IsEisensteinAt.irreducible`) and `X_pow_sub_C` Kummer irreducibility.

4. **Wrote STATUS.md** (the durable overview the review lap wanted; didn't exist before)
   with the verified axiom ledger, and updated PENDING_WORK.md to lead with the new
   target's attack plan.

## 🎬 Next actions (in priority order)
1. **Build Layer 1 of the constructible-numbers target.** Full attack plan in
   `PENDING_WORK.md` (top section). Shape: `IsSqrtTower : IntermediateField ℚ ℝ → Prop`
   (inductive: `⊥`, then adjoin `a` with `a*a ∈ K`); `Constructible x := ∃ K, IsSqrtTower K
   ∧ x ∈ K`; prove `IsSqrtTower K → ∃ n, finrank ℚ K = 2^n`; witness `∛2` has degree 3
   (`X³-2` irreducible via Eisenstein); conclude `¬ Constructible (∛2)` ⟹ cube not doubled.
   Create under `src/LeanFormalizations/Geometry/Constructible/`. Aim axiom-clean.
2. After Layer 1: the **geometric bridge** (Layer 2 — the real hard wall) — faithful
   point/line/circle definition + "geometric ⟹ quadratic tower". This becomes the crux to
   bang on each lap.
3. Cheap follow-ons once Layer 1 lands: trisection (`cos 20°` degree 3), Gauss–Wantzel,
   squaring the circle (π transcendental is already in mathlib via Lindemann).

## ⚠️ Notes / gotchas
- Don't reopen Curtis — verified done. Don't harvest the obsolete Aristotle Lemma-1 job
  (`80d9166c`). Mathlib-upstream of Curtis is Trevor's outward-facing call, not autonomous.
- Aristotle: nothing genuinely open right now (Curtis done; the new target has no `sorry`
  yet). Once Layer 1 has a self-contained open sub-lemma, that's the thing to feed it.
- `DIRECTION.md` is historical (all about Curtis's now-closed crux) — superseded by
  STATUS.md + this baton + PENDING_WORK.
- HEAD was `fe18f0a` at lap start; the build state described here is from that commit (the
  only change this lap is these three docs: STATUS.md, PENDING_WORK.md, this handoff).

## 📁 Key files
- `STATUS.md` — durable overview + axiom ledger (NEW this lap; refresh on review laps).
- `PENDING_WORK.md` — leads with the constructible-numbers attack plan (Layer 1/Layer 2).
- `Curtis/` — the completed, axiom-clean Curtis 1990 formalization (audit surface =
  `Curtis/Statement.lean`).

---
**→ Next session: start coding Layer 1 of the constructible-numbers target per
PENDING_WORK. Curtis is done; do not reopen it.**
