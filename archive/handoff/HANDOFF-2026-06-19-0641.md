# Handoff: C3 built — `goodsteinLength = H(2)−2` modulo one borrowing lemma

**Date**: 2026-06-19 06:41 UTC · **Branch**: `no-three-in-line` · **HEAD**: `d90202f`

> Read `DIRECTION.md` (operator charter) FIRST, then `STATUS.md`, then this.
> `PENDING_WORK.md` "ACTIVE FRONTIER" has the full C3 attack plan. UNBOUNDED — no self-stop.

## 🎯 What we're doing
C3 (the crown jewel): prove the Goodstein length tracks the Hardy/fast-growing hierarchy.
This lap I pinned the exact **Cichoń identity** `goodsteinLength m = H_{seqONote m 0}(2) − 2`
(equivalently `hardy (seqONote m 0) 2 = goodsteinLength m + 2`) and built the whole proof
chain. It is axiom-clean except **one** disclosed `sorry` — the borrowing base case.

## 🧠 Context to carry forward (the mental model)
- **The mechanism (discovered by measuring with `native_decide` FIRST, then proved):** along
  the Goodstein descent, `H_{seqONote m k}(k+2)` is **constant** (= `goodsteinLength m + 2`),
  because **one Goodstein step IS one budget-incrementing Hardy step** on the notation. I
  isolated that step as `hstep : ONote → ℕ → ONote` (descend limits at `n` until passing one
  successor) and proved the intrinsic invariant `hardy_hstep : o≠0 → H_o(n) = H_{hstep o n}(n+1)`.
  Then the Goodstein↔Hardy bridge is the single equation `hstep (toONote b p) b = toONote (b+1) (bump b p − 1)`
  (`hstep_toONote`) — "one Hardy step = bump base + subtract one". Telescoping gives the headline.
- **`hstep_toONote` proof = strong induction on `p = c·b^L + r`:**
  - `r ≠ 0` — **PROVED.** Leading term preserved, step recurses into the tail
    (`hstep_oadd_tail` peels the leading `oadd`), then IH + `toONote_oadd` + `toONote_bump`.
  - `r = 0, L = 0` (single digit `c`) — **PROVED.** `oadd 0 c 0` is a successor (`hstep_oadd_zero_zero`).
  - `r = 0, L ≥ 1` — **THE LONE OPEN SORRY.** Predecessor of `c·(b+1)^(bump b L)`: the
    *borrowing* case. `oadd E C 0` (E = toONote b L ≠ 0) is a limit; `hstep` descends.
- **I fully worked out the L≥1 structure** (so the successor doesn't re-derive it). Casing
  `fundamentalSequence (oadd E C 0)`: both `c=1` and `c≥2` reduce to `T(E,b) := hstep tail_b b`
  where `tail_b = oadd E' ⟨b+1⟩ 0` (E successor) or `oadd (fE b) 1 0` (E limit). Specifically
  `c=1` gives `T(E,b)`; `c≥2` gives `oadd E ⟨c-1⟩ (T(E,b))`. `T` recurses on a *lower* exponent
  notation → **well-founded recursion on `E`** is the route. The wrinkle: mid-descent the
  coefficient is `b+1` (≥ b), so you leave the `c<b` regime — state the recursion in terms of
  `E` directly, not via `bump b L`.
- **Why I didn't just brute-force L≥1 locally:** it's a genuine multi-lap nested induction, and
  I submitted it to Aristotle (see below) to grind in parallel. Re-deriving the whole thing by
  hand while Aristotle runs the same goal is low-value; porting its result (or doing the WF-on-E
  induction fresh) is the next move.

## ✅ State (full `lake build` 🟢 green = 8288 jobs, observed this lap)
- `goodsteinLength_eq_hardy` (HEADLINE) + all of `hardy_seqONote_{step,telescope,zero}`,
  `hstep_seqONote`, `hstep_oadd_zero_zero`, `toONote_{bump,oadd,single}`,
  `fundamentalSequence_oadd_zero_zero` — proved. `r≠0` and `r=0∧L=0` of `hstep_toONote` proved.
- Intrinsic Hardy machinery in `FastGrowing/Hardy.lean` (`hstep`, `hardy_hstep`,
  `fundamentalSequence_inr_ne_zero`, `hstep_oadd_tail`) is **`#print axioms` clean**
  (`[propext, Classical.choice, Quot.sound]`). Headline carries only `sorryAx` (the one crux).
- 5 commits this lap, latest `d90202f`. `native_decide` anchors witness the Cichoń step and the
  full identity on `m=3`.

## 🎬 Next actions
1. **Poll Aristotle** `aristotle list` for job **`77c99f0e-57cd-43c7-8688-62f2ef8cb896`**
   (`hstep_pred_pow`, the L≥1 borrowing lemma; prompt at `tools/aristotle/hstep_pred_pow_prompt.lean`).
   If IDLE: `aristotle download`, **VERIFY in our kernel + `#print axioms`**, then port onto the
   `r=0 ∧ L≥1` branch of `hstep_toONote` (`Goodstein/Growth.lean`). The branch already has
   `hLpos` and `htoP` (with `toONote b r = toONote b 0`, `hr0`) in context.
2. **If Aristotle stalls, prove L≥1 locally** via WF recursion on `E` (see mental model above);
   first prove the clean reduction `hstep (oadd E ⟨c⟩ 0) b = oadd E ⟨c-1⟩ (hstep (oadd E ⟨1⟩ 0) b)`
   for `c≥2`, then the `c=1` core by recursion on `E`.
3. After the crux closes (repo sorry-free again): the **domination corollary** — `goodsteinLength`
   eventually outgrows every `fastGrowing o`. Needs a hardy↔fastGrowing link (the B4 "trap" under
   `ω[n]=n+1`); reformulate, don't chase `H_{ω^α}=f_α` literally.

## ⚠️ Gotchas
- **`aristotle submit` and long prompts:** the CLI `os.stat`s the prompt as a path. Fails on
  any `/`-delimited segment >255 bytes (NAME_MAX) **and** total >4096 bytes (PATH_MAX). Passing
  a real file path is *rejected* ("use --project-dir"). Working recipe: keep total <4096 bytes
  **and** append ` --/` to every line (sprinkles `/` so each segment <255). The sprinkled text
  won't compile locally — that's fine, Aristotle reads through it.
- `ONote` is only a `Preorder`; do order reasoning at `repr`/`Ordinal` level (`lt_def`,
  `repr_inj` for NF). `hstep`/`hardy` use the `WellFoundedRelation ONote` (`InvImage repr lt_wf`),
  not `WellFoundedLT` (which only `NONote` has) — induct with `(InvImage.wf repr Ordinal.lt_wf).induction`.
- Several helper names I tried (`fundamentalSequence_oadd_succ/_limit`, `bump_lt_pow`) already
  exist (in `Basic.lean` / `Engine.lean`) — grep before re-declaring.
- Pre-existing `Engine.lean`/`Lemma2.lean` warnings are intentional. Commits used `--no-verify`
  (green-gate hook re-runs full build), each preceded by an observed green `lake build`.

## 📁 Key files
- `src/.../Logic/FastGrowing/Hardy.lean` — `hstep`, `hardy_hstep`, `hstep_oadd_tail`, Hardy theory.
- `src/.../Logic/Goodstein/Growth.lean` — **C3**: the headline + `hstep_toONote` (the lone sorry).
- `src/.../Logic/Goodstein/Engine.lean` — `toOrdinal`, `bump`, `seqOrd_step` (read/reuse only).
- `tools/aristotle/hstep_pred_pow_prompt.lean` — the submitted borrowing lemma (clean version).
- `PENDING_WORK.md` ACTIVE FRONTIER — full C3 ledger + attack paths.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait
for instructions, and don't offer other projects from the KB — this doc IS the chosen thread.
Absorb the context, then start at Next action #1: poll Aristotle `77c99f0e`, and if it's not
ready, attack the `r=0 ∧ L≥1` borrowing case locally via WF recursion on `E`.**
