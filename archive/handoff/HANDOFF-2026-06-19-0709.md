# Handoff: Davies planar Kakeya — the entire single-scale Córdoba→count chain built; only cross-scale thinning left

**Date**: 2026-06-19 07:09 UTC · **Branch**: `kakeya-davies` · **HEAD**: `3795aa9`

## 🎯 What we're doing
Prove `davies_kakeya_2d : KakeyaSetConjectureDim 2` (every planar Kakeya set has `dimH = 2`).
Upper bound done; lower bound reduced (K1, axiom-clean) to `μH[d] S ≠ 0` for all `d<2`, then (K5,
axiom-clean reduction in `Cover.lean`) to the lone `sorry` **`Engine.kakeya_hausdorffContentBound`**:
the Hausdorff content lower bound `∃ r>0, c≠0, every fine cover S⊆⋃tₙ has ∑ₙ ediam(tₙ)^d ≥ c`.
**Stay in `Kakeya2D/` only.**

## 🧠 Context to carry forward (read this — it's the whole mental model)
This lap I built **every component lemma** of the multi-scale Córdoba-for-Hausdorff estimate. The
crux decomposes as (a′) dyadic scale pigeonhole, (b) direction pigeonhole, (c) localized Córdoba —
**all three are now DONE + axiom-clean**, plus the fully-assembled single-scale piece-count
`cover_count_lower`. What remains is ONE thing: the **cross-scale orchestration** that feeds the
single-scale count, and it is genuinely **reference-gated** (see below).

**The single-scale chain (all proved, all `#print axioms` = [propext, Classical.choice, Quot.sound]):**
- `Cover.exists_pullback_cover` — covered unit segment ⟹ `1 ≤ ∑ₙ volume(Tₙ)` + pullback pieces
  `Tₙ⊆[0,1]` (refines `one_le_tsum_ediam_of_covers`, exposes covered *length* before collapsing).
- `Cover.scaleWeight j = 1/(2(j+1)(j+2))`, `tsum_scaleWeight_lt_one` (telescoping, sum 1/2<1).
  **Polynomial decay `≳1/j²` is mandatory** — geometric `2⁻ʲ` weights give covered-length `≳δ` which
  the `δ=2⁻ʲ` factor kills in assembly. Don't "simplify" to geometric.
- `Cover.exists_dominant_scale` (a′) — `1≤∑ₙfₙ`+scale fn `g` ⟹ ∃ j, `scaleWeight j ≤ ∑_{g n=j} fₙ`
  (regroup via `ENNReal.tsum_fiberwise` + `exists_index_ge_of_tsum_lt`).
- `Cover.exists_global_dominant_scale` (b) — finite net `s`, profiles `L k·` with `1≤∑ⱼL k j` ⟹
  ∃ j, `s.card·scaleWeight j ≤ ∑_{k∈s} L k j` (sum to ≥|s| via `Summable.tsum_finsetSum`).
- `CordobaL2.volume_thickening_sets_ge` (c-core) — arbitrary measurable `R k ⊆ T_k^full`, `R k⊆E` ⟹
  `(∑vol R k)² ≤ vol(E)·6πδ·2N(1+logN)`. Overlaps transfer via `R k ⊆ full tube`. The right object
  is the δ-thickening of the covered SET (not a single sub-segment — covered part is a union).
- `TubeFractional.volume_thickening_covered_ge` — `vol(cthickening δ φ(A)) ≥ 2δ·vol(A)`, general
  measurable A (frame box). `tube_smul_subset`/`affineSegment_smul_subset` — fractional ⊆ full tube.
- `CordobaL2.cordoba_cover_count` — net pts `a k`, covered sets `A k⊆[0,1]`, container `P`, `φₖ(A k)⊆P`
  ⟹ `(∑ₖ 2δ·vol(A k))² ≤ vol(cthickening δ P)·6πδ·2N(1+logN)`.
- `Cover.volume_thickening_biUnion_le` + `Cover.cover_count_lower` — the piece-count form:
  finite piece set `s` (each ediam≤ρ), `φₖ(A k)⊆⋃_{n∈s}Uₙ` ⟹
  `(∑ₖ 2δ·vol(A k))² ≤ |s|·(ρ+δ)²·vol(disc)·6πδ·2N(1+logN)`. Reads off `M=|s| ≳ (∑vol A k)²/((ρ+δ)²logN)`.

**The remaining obstruction — the net-scale circularity (reference-gated).** `cover_count_lower`
needs the net `δ*`-separated with `N≈1/δ*`, `δ*=2⁻ʲ*`; but `j*` is the pigeonhole OUTPUT, needing the
net as INPUT. `sum_overlap_le` ties tube width = separation = δ, so one fixed net can't serve all
scales (a net of N dirs is only `2⁻ʲ`-separated for `j≤log₂N`).

**Dead ends I verified this lap (don't re-walk):**
- *Nested-net summation* (avoids thinning): nets ARE nested (`net_{j'}⊆net_j` for j≥j'), and with all
  pieces ediam≤r=2⁻ᴶ⁰ you get `∑_{j≥J₀} S_j ≥ N_{J₀}≈1/r` cleanly. BUT combining with the per-scale
  `S_j² ≤ M_j·C·(1+j)` via Cauchy–Schwarz **diverges** (∑2^{jd}(1+j) blows up; pieces arbitrarily
  small ⟹ j unbounded). It gives only the coarse count `N_{J₀}`, losing the fine `2^{j*}` factor.
  Confirmed useless. The fine count genuinely requires thinning so `S_{j*}≈N_{j*}≈2^{j*}`.
- The single-scale Minkowski bound alone never controls `∑ediam^d` (infinitely many tiny pieces).
So thinning is unavoidable: the precise *net-thinning + covered-length-retention* combinatorial lemma
is the lone gap. **`ON-LINE-REQUEST.md` (2026-06-19 UPDATE) asks for its exact structure** — harvest
any `ON-LINE-FINDINGS-*` FIRST next lap.

## ✅ State (all verified via real `lake build` this lap)
- Full library **green, 8296 jobs**. 6 commits this lap (`17dfa48`..`3795aa9`).
- Every new lemma `#print axioms`-clean. `davies_kakeya_2d` axioms = `[propext, sorryAx, …]` (single
  sorry = `kakeya_hausdorffContentBound`).
- 0 custom axioms; no axiom-smuggling.
- Aristotle: slot held by another project's RUNNING job (`083292d5`); remaining Kakeya work is
  measure/combinatorics (Aristotle-weak) AND has no precise open statement — correctly not fed.

## 🎬 Next actions
1. **Harvest first:** check repo root for `ON-LINE-FINDINGS-*.md`; if present, read + act, then
   `git mv` into `archive/findings/`. The findings answer the net-scale-circularity question.
2. **If findings present:** formalize the net-thinning + retention lemma per the reference, then wire
   `exists_global_dominant_scale` (or per-direction `exists_dominant_scale_of_covers`) + `cover_count_lower`
   into `Engine.kakeya_hausdorffContentBound`. The arithmetic to close: `M≳δ^{-2}/poly` ⟹
   `∑ediam^d ≥ M·(δ/2)^d ≳ δ^{-(2-d)}/poly ≥ c` for `r` small (`d<2`).
3. **If NO findings yet:** attack the net-thinning lemma directly — extract a `2⁻ʲ*`-separated subnet
   of a fine net (one direction per angular cell) retaining covered length `≳1/poly`. This is the
   only genuinely-new piece; everything it plugs into is built. A disclosed-`sorry` checkpoint on a
   *correctly-stated* thinning lemma is fine; don't fabricate a statement you're unsure of.

## ⚠️ Gotchas (flushed)
- Cover.lean opens `Set MeasureTheory Metric` (NOT `Finset`, NOT `scoped Real`): write `Finset.range N`
  and `Real.pi` explicitly there. CordobaL2 opens `Finset` so bare `range`/`π` work — and `open Finset`
  shadows `inter_subset_inter` with the Finset one; use `Set.inter_subset_inter`.
- Local `thickening S δ := Metric.cthickening δ S` (Discretize.lean), `thickening_def` is `@[simp]`.
  So local thickening = closed thickening — no open/closed mismatch; `cordoba_cover_count`'s
  `Metric.cthickening δ P` is defeq to `thickening P δ`.
- `ENNReal.mul_lt_mul_left h0 hinf bc : b*a < c*a` (the constrained factor `a` is on the RIGHT despite
  the name) — commute as needed.

## 📁 Key files
- `…/Kakeya2D/Cover.lean` — reduction + both pigeonholes (a′,b) + `cover_count_lower` + thickening subadditivity.
- `…/Kakeya2D/CordobaL2.lean` — `volume_thickening_sets_ge`, `cordoba_cover_count` (c, assembled).
- `…/Kakeya2D/TubeFractional.lean` — `volume_thickening_covered_ge` (numerator), tube containments.
- `…/Kakeya2D/Engine.lean` — `kakeya_hausdorffContentBound` (the lone `sorry`) + wiring.
- `PENDING_WORK.md` §A (rewritten), `ON-LINE-REQUEST.md` (2026-06-19 UPDATE = the precise blocker).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context above, then start at Next action #1: check for `ON-LINE-FINDINGS-*.md`, harvest it, and wire
the net-thinning step into `kakeya_hausdorffContentBound`. All the single-scale bricks are built and
axiom-clean — you are assembling, not re-deriving.**
