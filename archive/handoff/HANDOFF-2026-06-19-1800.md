# Handoff: Davies planar Kakeya — the wiring (W) is COMPLETE; sole open input is (S)

**Date**: 2026-06-19 18:00 UTC · **Branch**: `kakeya-davies` · **HEAD**: `<latest, post the docs commit>` (this lap's last feat = `7797183`)

## 🎯 What we're doing
Prove `davies_kakeya_2d : KakeyaSetConjectureDim 2` (planar Kakeya ⟹ `dimH = 2`, Davies 1971). Whole
job = the lower bound. Headline is machine-checked **down to ONE cited math axiom**
`kakeya_subresolution_content` (Case-B residual): `#print axioms davies_kakeya_2d = [propext,
Classical.choice, Quot.sound, kakeya_subresolution_content]`. Build green (8299 jobs).

## 🧠 Context to carry forward
This was a deep-reflection lap; read `PENDING_WORK.md` §🧘 Reflection-2026-06-19 + `STATUS.md` first.
The mental model:
- The discrete net route (`Engine.kakeya_hausdorffContentBound`) is used at **J=1**, so its lone axiom
  carries essentially the *whole* lower bound, and the discrete route provably **cannot escape Case B**
  (any cover by sub-resolution pieces triggers it for every fixed J). So the honest narrowing is the
  **measurable-selection / continuum route**, NOT more discrete patching.
- That route is now built **end-to-end and axiom-clean**. The continuum integral `∫₀¹ ∑ⱼ ℓⱼ ≥ 1`
  dissolves the net-scale circularity; its only blocker was measurability of `θ↦ℓⱼ(θ)` = a measurable
  base-point selection. With that as a *hypothesis*, the whole content bound follows with no Case B.
- **Do NOT** relitigate the fixed-net L² sum for d>1 (proven dead end, `CASE_B_ANALYSIS.md` §C1).
- **Do NOT** pivot to Davies' duality proof — it throws away the 95%-built Córdoba ladder.

## ✅ State (verified via real `lake build` + `#print axioms` this lap)
- **(W) DONE:** `kakeya_hausdorffContentBound_of_measurableSelection` (`Kakeya2D/Wiring.lean`) PROVEN +
  axiom-clean (`[propext, Classical.choice, Quot.sound]` — the selection is a HYPOTHESIS `hsel`, NO new
  axioms). Assembly: closed-piece reduction → `exists_continuum_caseA_numerator` (cap-free dominant
  scale + base angle) → genuine sub-fiber `s0={n:g n=j ∧ ediam>0}` (window via `dyadicIdx_window`) →
  zero-`ediam` pieces transported away via `volume_coveredFiber_biUnion_subsingleton_zero` (null
  covered length) → `caseA_content`, with the finite/infinite-fiber split. NO Case B.
- New axiom-clean bricks this lap (`MeasurableRoute.lean`): `exists_continuum_caseA_numerator`,
  `volume_coveredFiber_subsingleton_zero`, `volume_coveredFiber_biUnion_subsingleton_zero`.
- Headline axioms unchanged (still via `Engine` until the rewire). Kakeya lane: 0 code sorries.
- Last green build: full library 8299 jobs (green-gate passed on every feat commit this lap).

## 🎬 Next actions
1. **(S) Discharge the measurable selection** — the SOLE open input. Prove the exact `hsel` shape that
   `Wiring.lean` takes: for the Fσ set `⋃ closure(coverₙ)`, a measurable `a : ℝ → Plane` with the unit
   segment over `θ∈[0,1]` in `⋃ Cₙ`. Routes: Jankov–von Neumann / KRN for the closed-valued
   `B(θ)={a:segment(a,θ)⊆E}`, or a bespoke **argmin-‖a‖** selection (may sidestep full KRN). mathlib has
   NO measurable-selection infra (`SetTheory/Descriptive` = `Tree.lean`). Reference-gated
   (`ON-LINE-REQUEST.md` UPDATE 5 ask 1). Keep banging — "needs deep machinery" is a hypothesis to test.
2. **Then switch the headline:** add `axiom kakeya_measurable_selection` (the exact `hsel` shape),
   instantiate it, route `two_le_dimH` through `kakeya_hausdorffContentBound_of_measurableSelection`,
   retiring `kakeya_subresolution_content` — a faithfulness upgrade (citable named theorem vs. murky
   residual). This makes the headline's lone axiom a clean Jankov–von Neumann statement.
3. **Free Aristotle slot** exists (all jobs IDLE, other projects). Could architect a self-contained (S)
   or a faithfulness NL→Lean check — don't rush a sloppy submission (corpus: Aristotle weak on measure
   theory; isolate the combinatorial skeleton, supply measure bricks as axioms).

## ⚠️ Gotchas
- **Binder inference:** a `∀ i, … (i:ℝ) … A i` where the `(i:ℝ)` Nat→ℝ cast appears BEFORE any
  ℕ-forcing use of `i` infers `i:ℝ` (then `A i` fails with "expected ℕ"). Annotate `∀ i : ℕ`. Cost me
  one build cycle in `Wiring.lean` (`hvol_eq`, `hscov`).
- `set A := fun i => {…}` does NOT fold applied/beta-reduced occurrences in a pre-existing goal, and
  `simp only [hAdef]` on the big lambda blew up `isDefEq` to timeout. Fix: a membership-iff
  `hAmem i u : u ∈ A i ↔ … := Iff.rfl` + `ae_eq_set`/`measure_congr` for the transport, avoiding `simp`.
- `EMetric.diam_eq_zero_iff` → use `Metric.ediam_eq_zero_iff` (deprecation). `one_ne_top` →
  `ENNReal.one_ne_top`. `push_neg`/`mul_le_mul_left'` warnings are cosmetic, repo-wide — don't churn.
- Pre-existing dormant `sorry` at `Logic/FastGrowing/Basic.lean:121` — NOT Kakeya, ignore.

## 📁 Key files
- `Kakeya2D/Wiring.lean` — **(W) the proven wiring** (this lap). `Kakeya2D/MeasurableRoute.lean` — the
  spine + glue + negligibility helpers. `Kakeya2D/NetThinning.lean` — `caseA_content`, `dyadicIdx`.
- `Kakeya2D/Engine.lean` — the live `kakeya_subresolution_content` axiom + discrete assembly.
- `Kakeya2D/CASE_B_ANALYSIS.md` — why the continuum route; `STATUS.md` (ledger) · `PENDING_WORK.md`
  (§Reflection + §A0‴) · `ON-LINE-REQUEST.md` UPDATE 5 (the (S) ask).

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. (W) is DONE;
absorb the context, then start at Next action #1: attack (S), the measurable base-point selection — the
sole open input that gates rewiring the headline to a clean, citable axiom. Do NOT relitigate the
fixed-net L² sum, and do NOT re-attempt (W).**
