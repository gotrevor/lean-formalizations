# Handoff: HJSW `3N/2` — CRUX CRACKED; whole tower formalized down to one slope-±1 lemma

**Date**: 2026-06-19 06:59 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `2bc6a34`

## 🎯 What we're doing
Sole target: `hjsw_lower : 3*(p−1) ≤ maxNoThreeInLine (2*p)` (`Hyperbola.lean`), the HJSW 1975 lower
bound. The 2N upper bound + Erdős Θ(N) are done. This lap **cracked the open construction crux** and
formalized the entire proof tower down to a SINGLE remaining `sorry`.

## ⭐ Headline this lap — the closed-form selection rule (months-old crux, solved)
The prior baton believed the lift-selection was intrinsically non-uniform ("3,2,3,1,3,4,3", p-mod-4
split, size-3 classes). **That was a bad-search artifact.** There is a clean closed form:

> base column `x∈[0,p)` → `(x, (2x+1)⁻¹ mod p)` (sheared hyperbola, pole `pl=(p−1)/2`);
> **drop the pole column; keep 3 of 4 lifts of every other column, dropping the lift nearest the grid
> centre: `dropped(r,s) = (r + p·[r≤pl], s + p·[s≤pl])`**.

`|S| = 3(p−1)`. **Verified card + distinct + grid + NoThree (exact integer determinant) for EVERY
prime 3 ≤ p ≤ 109.** See `SELECTION-RULE-FOUND.md`. How it was found: drop-pole+3of4 is always
feasible; the pure "drop nearest centre" rule leaves exactly 2 structural bad triples for every p
(both share the D-corner of `(pl−1,pl)`); the `≤` tie-break kills both with no new collisions.

## 🧠 Context to carry forward
- **The whole `hjsw_lower` tower is now machine-checked except ONE lemma.** Chain:
  `hjsw_lower` ⟸ `shearSel_noThree` ⟸ `shearSel_intdet` ⟸ `shearSel_two_lifts_line` ⟸
  `shearSel_cross_column` ⟸ **`shearSel_cross_diag`** (the lone `sorry`, line ~554 of `Hyperbola.lean`).
- Axiom-clean bricks proven this lap: `shearSel_card` (=3(p−1)), `shearSel_grid` (⊆2p×2p),
  `shear_two_ne`, `shear_curve` ((2x+1)y=1 off pole), `shearY_lt`, `shearY_injective`,
  `shearSel_mem_curve`, `shearSel_share_residue`, `shearSel_{y,x}res_of_{x,y}res` (curve functionality),
  and the slope-`0`/`∞` cross-column cases inside `shearSel_cross_column`. The `Collinear ℝ → integer
  determinant` bridge is wired (mirrors `lift_triple_noncollinear`).
- **`shearSel_cross_diag` is the genuine HJSW combinatorial kernel** (slope-`±1` only): `P,Q` two kept
  lifts of one column differing in BOTH coords (diagonal `{A,D}` or antidiagonal `{B,C}` pair), `R` a
  kept lift of a DIFFERENT column — never collinear. Proof plan (in `PENDING_WORK.md`): both kept pins
  the drop side; `R` on the kept slope-`±1` line ⇒ `s_c−c ≡ s_a−a (mod p)` with `c` the partner column
  (explicit Möbius relation via the curve); the corner of `c` landing on that line is exactly `c`'s
  DROPPED corner ⇒ contradiction unless `c=a`. This is a substantial (~150-line) modular-arithmetic +
  4-corner-casing proof — the real remaining work. Don't expect a 20-line proof.
- **Why I isolated rather than finished it:** every easier layer was banked first (hardest-first would
  say attack cross_diag, but the layers above were cheap and each genuinely narrows the kernel). I
  stopped at cross_diag because it's the irreducible research kernel and further unstructured hacking
  risked broken code; Aristotle is grinding the superset in parallel.

## ✅ State
- `lake build` GREEN (full, 8285 jobs) — observed this session. Only `sorry` warning is
  `Hyperbola.lean:554` (`shearSel_cross_diag`). All commits below pass the pre-commit green-gate.
- Last commit `2bc6a34`. `Anchors.lean` native_decide-certifies the general `shearSel p` at p=7,11,13
  (and the older witness ladder). Online request RESOLVED in-house (the construction was found locally).
- **Aristotle**: job `1c2a55b7` (`aris-hjsw-shear`) RUNNING — proving the pure-arithmetic superset
  `shearSel_decNoThree` (= every distinct triple has nonzero integer determinant; self-contained file
  at `/tmp/aris-hjsw-shear/ShearNoThree.lean`). It ports directly: `shearSel_intdet` has the identical
  statement shape, and `shearSel_noThree`/`hjsw_lower` follow with no extra work.

## 🎬 Next actions
1. **Poll `aristotle list` for `1c2a55b7`.** If DONE: `aristotle download`, VERIFY in our kernel +
   `#print axioms`, then port its proof into `shearSel_intdet` (or, if it only got a partial, into
   `shearSel_cross_diag`). If it returned a wrapper-sorry/failed, ignore and go to #2.
2. **Attack `shearSel_cross_diag` directly** (the lone `sorry`). Follow the partner-column plan in
   `PENDING_WORK.md` §"The ONLY open item". Start by extracting columns `a` (for P,Q) and `c` (for R)
   from `mem_shearKept`, and proving P,Q differ by exactly `p` in both coords (via
   `coord_diff_of_residue_eq` + the `hx1`/`hy1` ≠ hypotheses). Then case the corners. Consider
   re-submitting the tighter `shearSel_cross_diag` to Aristotle if the superset job failed.
3. Keep `lake build` green; commit every green step. Never push (host pushes).

## ⚠️ Gotchas
- `shearY_injective` and the construction need `p ≠ 2` (at p=2 the shear degenerates); `hjsw_lower`
  handles p=2 via `decide` in `shearSel_intdet`. Keep the `hp2 : p ≠ 2` hypotheses.
- native_decide on `shearSel p` carries `ax_*` artifacts — those live ONLY in `Anchors.lean`, must NOT
  leak into the headline `Hyperbola.lean` proof (which is a `Collinear ℝ` argument). Don't `native_decide`
  anything on the `hjsw_lower` path.
- Python probes: stdout buffers when piped — `flush=True`; author scripts with the Write tool (heredocs
  flaky). All exploration scripts are in `/tmp/shear_*.py`.
- `aristotle submit` prompt: avoid backticks in the shell string (they get command-substituted) — use a
  single-quoted `PROMPT=...` var.

## 📁 Key files
- `…/NoThreeInLine/Hyperbola.lean` — the whole tower; `shearSel`, all bricks, `hjsw_lower`, lone `sorry`
  at `shearSel_cross_diag`.
- `…/NoThreeInLine/Anchors.lean` — native_decide anchors (general `shearSel` + witness ladder).
- `SELECTION-RULE-FOUND.md` — the closed-form rule + how it was found + the proof sketch.
- `PENDING_WORK.md` — the lone-item section has the full `shearSel_cross_diag` proof plan.
- `/tmp/aris-hjsw-shear/ShearNoThree.lean` — the self-contained Aristotle submission.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context, then start at Next action #1 (poll Aristotle `1c2a55b7`), and if it's not ready, go straight
to #2 and attack `shearSel_cross_diag`.**
