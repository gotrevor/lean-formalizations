# Handoff: HJSW `3N/2` — COMPLETE & axiom-clean (lap-end checkpoint)

**Date**: 2026-06-19 07:41 UTC · **Branch**: `ntl-hjsw` · **HEAD**: `2365297`

## Status: DONE. Repo is `sorry`-free and 100% axiom-clean.
The sole target `hjsw_lower : 3*(p−1) ≤ maxNoThreeInLine (2*p)` is **fully proven** this lap
(`#print axioms = [propext, Classical.choice, Quot.sound]`). Full `lake build` GREEN (8285 jobs).
For the full technical write-up (proof technique, gotchas, partner lemmas) see the companion
**`HANDOFF-2026-06-19-0728.md`** — this file is just the lap-end checkpoint with the final HEAD.

## What this lap did
1. **Proved the crux** `shearSel_cross_diag` (the lone `sorry`) via `shear_diag_partner` (slope +1)
   and `shear_anti_partner` (slope −1): factor the curve → partner relation `2c+2sₐ+1≡0` /
   `2sₐ≡2c+1 (mod p)` → closed-form drop tie-break forces any third kept lift to be a DROPPED corner.
2. **Promoted to the audit surface** (`Statement.lean`), all axiom-clean:
   - `hjsw_lower_bound` — `3(p−1) ≤ max(2p)`.
   - `maxNoThreeInLine_ge_three_quarters` — general-`N` `3·⌊N/4⌋ ≤ max N` (N≥4, via Bertrand);
     ¾ is mathlib's ceiling (no Nagura / prime-gap results available).
   - `maxNoThreeInLine_two_mul_prime_bounds` — `3(p−1) ≤ max(2p) ≤ 4p`.
   - `maxNoThreeInLine_bounds` — general-`N` `3·⌊N/4⌋ ≤ max N ≤ 2N`.
3. Refreshed STATUS / PENDING_WORK / HANDOFF; closed `ON-LINE-REQUEST.md`.
4. Submitted an independent NL→Lean **faithfulness** check to Aristotle (`72891d77`).

## Exact next steps (next lap)
1. **Harvest Aristotle `72891d77`** (was RUNNING at lap end): `aristotle download 72891d77
   --destination /tmp/aris-faithful`; read ITS encoding of "no-three-collinear" + the `2p×2p` box +
   cardinality and check logical equivalence to `Statement.lean`'s `NoThreeCollinear`/`IsGridSet`/
   `hjsw_lower_bound`. Match ⇒ corroborates the audit surface; mismatch ⇒ faithfulness gap to fix.
   (Ignore jobs `1c2a55b7`/`77c99f0e` — redundant, on the now-closed crux; `5069c4b1` is a junk test.)
2. **No open obligation remains in this repo.** The full empty-inventory + three-paths-each menu for
   any *new* direction (sharper constant via prime-gap infra; stronger construction toward 2N) is in
   `PENDING_WORK.md` (top section). A genuinely new target would need Trevor to point at one.

## Build / rules
`lake build` (whole repo) or `…NoThreeInLine.Statement` / `…Hyperbola`. Commit every green; never push
(host pushes). Tree is clean at `2365297`.
