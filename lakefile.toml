name = "LeanFormalizations"
version = "0.1.0"
keywords = ["math", "number-theory", "combinatorics", "formalization"]
defaultTargets = ["LeanFormalizations"]
srcDir = "src"

[[require]]
name = "mathlib"
scope = "leanprover-community"
rev = "v4.31.0"

# Real dependency for the NumberTheory (PNT / Mertens / DivisorProblem) + NoThreeInLine.Shear.PrimeGap
# work (de-vendored from the old in-tree PNTAnd tower).
[[require]]
name = "PrimeNumberTheoremAnd"
git = "https://github.com/kim-em/PrimeNumberTheoremAnd.git"
rev = "40e5246b0cf0070875842488ccf60c461e60bc62"

[[lean_lib]]
name = "LeanFormalizations"

# Off-headline working library (NOT in defaultTargets, so the headline `lake build`
# is unchanged). Built explicitly with `lake build DaviesWip`. Holds the jvn 2nd-route
# GMT files (sorry-free) and the FastGrowing growth theory: Bachmann is sorry-free;
# Basic still carries the single open A3 sorry (fastGrowing_fundSeq_step) — so DaviesWip
# builds with one disclosed sorry warning. The other wip/ scratch stays excluded.
[[lean_lib]]
name = "DaviesWip"
srcDir = "../wip"
# Logic.FastGrowing.{Basic,Bachmann} were dropped 2026-08-24: `src/` now proves the A3 crux
# (`fastGrowing_bachmann_reach`) outright, so the wip copies are superseded duplicates that
# share the same namespace and would emit a sorry warning for an already-proved theorem.
globs = ["GeometricMeasureTheory.Capacitability", "GeometricMeasureTheory.VonNeumannSelection"]
